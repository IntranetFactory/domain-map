# === SUPERSEDED 2026-06-06: per-domain-skill restoration ===
# The per-module `system` skill grain is RETIRED (plans/per-domain-skill-restoration.md).
# Any open item that says "author/split a per-module system skill", "one system skill per
# domain_modules row", "add/PATCH skill_tools", or "<module>_agent per module" is CANCELED.
# New model: tool requirements live on `domain_module_tools` (author tools onto modules); each
# domain has exactly ONE domain-grain `system` skill (domain_id set, domain_module_id null) that
# DERIVES its toolset; starters keep their own module-anchored skill; FULL modules carry no skill;
# cross-domain value streams use `process_tools`. `skill_tools` is dropped. Per-module tool
# re-authoring is tracked in audits/_modularization-backlog.md. Do NOT author per-module skills.
# === end supersession note ===
schema_version: 2
domain: IPAAS
status: feedback_needed
next_action_by: user
last_audit: "2026-06-07"

# IPAAS (domain id 36) is UNBUILT: 0 domain_modules (M1 fail), 0 capability_domains.
# Per the unbuilt rule the BUILD is surfaced (B1A-BUILD / B1B-S1), not scaffolded, and the
# entire M1-gated cascade (B1B-S3, S4, S7, S9, S10, S11) is LEFT until the build lands.
# 2026-06-07 execute pass cured the items that stand independent of modules existing:
#   - entity_type classified on all 6 masters (B13): 469/470/471/472/474 operational_workflow,
#     473 catalog (config-shape event source).
#   - catalog_tagline + catalog_description authored on the domain row (A4 / Rule #20 override;
#     B1B-S2 dropped, B2-S2 moot: copy is written at record_status='new' for in-record review).
#   - 12 generic-synonym aliases inserted across the 6 masters (B11 partial). Vendor-keyed
#     aliases stay deferred (Rule #18) and are tracked under B1B-S6 below.

b1a:
  - id: B1A-BUILD
    summary: "Unbuilt domain: 0 domain_modules (M1 fail), 0 capabilities awaiting a build."
    finding: "Live 2026-06-07: still 0 domain_modules rows (M1 fail) and 0 capability_domains rows, so IPAAS is not deployable today. Must be built before any persona / RACI work applies. SURFACED, not scaffolded."
    action: "Run Phase A then M then B then S to author the module set, capabilities, masters, and the domain-grain system skill. If the result is multi-module, run Phase P (personas, role_modules, process_raci, lifecycle.process_id) afterward."
    blocked_by:
      - {type: user_decision, ref: B2-S1}

b1b:
  - id: B1B-S1
    summary: "Author IPAAS modules (default split: IPAAS-RECIPE-DESIGN + IPAAS-RUNTIME) + 5-7 capabilities + DMDO master rows; cures M1/M2/M4/M6 + A2. THIS IS THE BUILD (surfaced, not scaffolded)."
    finding: "Zero `domain_modules`, zero `capabilities` on domain_id=36 (confirmed live 2026-06-07). Rule #14 requires >=1 full module per domain; vendor surface implies 2 modules. Proposed: IPAAS-RECIPE-DESIGN (masters integration_recipes 469, integration_connectors 471, webhook_subscriptions 472, integration_triggers 473, integration_data_mappings 474) and IPAAS-RUNTIME (master integration_runs 470). Proposed capability set: IPAAS-RECIPE-AUTHORING, IPAAS-CONNECTOR-LIBRARY (cross-cutting candidate per B2-S6), IPAAS-DATA-MAPPING, IPAAS-WEBHOOK-SUBSCRIPTION-MGMT, IPAAS-RECIPE-EXECUTION, IPAAS-ERROR-HANDLING-AND-RETRY."
    action: "Author 2 `domain_modules` rows + 5-7 `capabilities` rows + `capability_domains` links + `domain_module_capabilities` links + 6 `domain_module_data_objects` master rows. Gated on the B2-S1 split decision."
    blocked_by:
      - {type: user_decision, ref: B2-S1}
      - {type: user_decision, ref: B2-S6}
    affected_masters:
      - {data_object_id: 469, name: integration_recipes, entity_type: operational_workflow}
      - {data_object_id: 470, name: integration_runs, entity_type: operational_workflow}
      - {data_object_id: 471, name: integration_connectors, entity_type: operational_workflow}
      - {data_object_id: 472, name: webhook_subscriptions, entity_type: operational_workflow}
      - {data_object_id: 473, name: integration_triggers, entity_type: catalog}
      - {data_object_id: 474, name: integration_data_mappings, entity_type: operational_workflow}

  - id: B1B-S3
    summary: "B4 pattern flag flips on 5 IPAAS masters + decide credential-storage (connector vs connection). JUDGMENT (b2) + DESTRUCTIVE shape change; surfaced via B2-S3."
    finding: "All pattern flags (has_personal_content, has_submit_lock, has_single_approver) on data_objects 469-474 are still default false. Audit proposes: integration_recipes.has_submit_lock=true + has_single_approver=true; integration_runs.has_submit_lock=true; integration_connectors.has_personal_content=true (or move to integration_connections per B3-S1); integration_data_mappings.has_submit_lock=true."
    action: "User confirms each flag; PATCH rows. If connection/connector split adopted (B3-S1), personal_content flag relocates to the new master. Pattern flags are workflow-shape judgments the user owns (Rule #12 note)."
    blocked_by:
      - {type: user_decision, ref: B2-S3}
      - {type: depends_on, ref: B3-S1, reason: "connection vs connector split shifts where has_personal_content lands"}

  - id: B1B-S4
    summary: "B6 hard-fail: author 6-7 intra-domain `data_object_relationships` (recipe contains triggers, recipe references connectors, recipe defines mappings, trigger produces runs, run uses connectors, webhook is_subtype_of trigger or sibling, run applies mappings). LEFT: gated on the build."
    finding: "Zero intra-domain `data_object_relationships` among IPAAS masters 469-474. The 5 user-edges from data_object 748 (Rule #10) were authored 2026-05-31 but cover only the platform-builtin direction, not the master-to-master direction."
    action: "Draft cluster-drafts block; surface mermaid; load via load_cluster_drafts.ts pattern after the build lands."
    blocked_by:
      - {type: user_decision, ref: B2-S4, reason: "webhook-as-subtype vs sibling decision shifts row count by one"}
      - {type: prerequisite_entity, ref: B1B-S1}
    affected_entities:
      - integration_recipes
      - integration_triggers
      - integration_connectors
      - integration_data_mappings
      - integration_runs
      - webhook_subscriptions

  - id: B1B-S6
    summary: "B11 partial: vendor-keyed `data_object_aliases` (Boomi, Workato, MuleSoft, ServiceNow IH, Zapier, Make.com, Tray.io, SnapLogic) across the 6 IPAAS masters. Generic synonyms DONE 2026-06-07; vendor-keyed terms DEFERRED (Rule #18: need industry_id)."
    finding: "12 generic-synonym aliases inserted 2026-06-07 (Integration Flows/Workflows, Integration Jobs/Executions, Connectors/Application Connectors, Event Subscriptions/Webhooks, Triggers/Event Triggers, Field Mappings/Data Transformations). Vendor product terms (atom, recipe, Zap, scenario, Snap, DataWeave, spoke) cannot be written as plain synonyms under Rule #18 without an industry_id."
    action: "If vendor-keyed aliases are wanted, attach an industry_id (or solution_id) per term and load via cluster-drafts. Otherwise close as the generic surface being sufficient. Depends on B2-S8 if additional vendor solutions land."
    blocked_by:
      - {type: depends_on, ref: B2-S8, reason: "if additional vendor solutions land, additional alias terms join the draft set"}
    affected_entities:
      - integration_recipes
      - integration_runs
      - integration_connectors
      - webhook_subscriptions
      - integration_triggers
      - integration_data_mappings

  - id: B1B-S7
    summary: "B12: author ~22-28 `data_object_lifecycle_states` rows across the 5 operational_workflow masters (469/470/471/472/474). LEFT: M5 requires domain_module_id, blocked on the build. (473 is now entity_type=catalog: no lifecycle required.)"
    finding: "Zero `data_object_lifecycle_states` on any IPAAS master. State machines: integration_recipes (draft -> tested -> published -> deprecated), integration_runs (queued -> running -> retrying -> succeeded/failed -> archived), integration_connectors (configured -> authenticated -> active -> credential_expired -> rotated -> revoked), webhook_subscriptions (configured -> active -> degraded -> disabled), integration_data_mappings (draft -> validated -> published). integration_triggers now classified entity_type=catalog (config-shape, no lifecycle)."
    action: "Author lifecycle states with domain_module_id pointing at the realizing module (M5 constraint) after the build lands."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S1, reason: "M5 requires domain_module_id on each lifecycle row"}
    affected_masters:
      - {data_object_id: 469, name: integration_recipes, workflow: draft_to_deprecated}
      - {data_object_id: 470, name: integration_runs, workflow: queued_to_archived}
      - {data_object_id: 471, name: integration_connectors, workflow: configured_to_revoked}
      - {data_object_id: 472, name: webhook_subscriptions, workflow: configured_to_disabled}
      - {data_object_id: 474, name: integration_data_mappings, workflow: draft_to_published}

  - id: B1B-S9
    summary: "B10b: PATCH `source_domain_module_id` on all 6 outbound IPAAS handoffs (765,766,767,768,769,801) + `target_domain_module_id` on 2 inbound (705, 748) after modules exist. LEFT: gated on the build."
    finding: "All 6 outbound IPAAS handoffs have NULL `source_domain_module_id`. Mapping derives once B1B-S1 lands: 765/766 -> IPAAS-RUNTIME (integration_runs is the source master); 767/768/769/801 -> IPAAS-RECIPE-DESIGN. Inbound 705 and 748 have NULL `target_domain_module_id`; both attribute to IPAAS-RECIPE-DESIGN (depends on B2-S5)."
    action: "PATCH pass on 8 handoff rows after B1B-S1."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S1}
      - {type: user_decision, ref: B2-S5, reason: "inbound consumer-DMDO design choice determines target_domain_module_id on 705/748"}
    affected_handoffs:
      - {handoff_id: 765, direction: outbound, target_domain: ITSM, trigger_event: integration_run.failed, null_field: source_domain_module_id}
      - {handoff_id: 766, direction: outbound, target_domain: OBS, trigger_event: integration_run.failed, null_field: source_domain_module_id}
      - {handoff_id: 767, direction: outbound, target_domain: APIM, trigger_event: integration_connector.credential_expired, null_field: source_domain_module_id}
      - {handoff_id: 768, direction: outbound, target_domain: TEST-MGMT, trigger_event: integration_data_mapping.updated, null_field: source_domain_module_id}
      - {handoff_id: 769, direction: outbound, target_domain: WORK-MGMT, trigger_event: webhook_subscription.delivery_failed, null_field: source_domain_module_id}
      - {handoff_id: 801, direction: outbound, target_domain: VSDP, trigger_event: integration_recipe.published, null_field: source_domain_module_id}
      - {handoff_id: 705, direction: inbound, source_domain: LCAP, trigger_event: extend_workflow.published, null_field: target_domain_module_id}
      - {handoff_id: 748, direction: inbound, source_domain: APIM, trigger_event: api_specification.updated, null_field: target_domain_module_id}

  - id: B1B-S10
    summary: "B8: author 6 outbound cross-domain `data_object_relationships` from IPAAS masters to target-domain masters mirroring the 6 outbound handoffs. LEFT: gated on the build + several target masters unconfirmed."
    finding: "Zero outbound cross-domain `data_object_relationships` from IPAAS masters. Proposed mirrors: 765 -> integration_runs opens service_incidents (target 47 confirmed); 766 -> integration_runs reports_to OBS master (TBD); 767 -> integration_connectors federates_with api_specifications (target 457 confirmed); 768 -> integration_data_mappings exposes_to test_cases (target 572 confirmed); 769 -> webhook_subscriptions opens work_tasks (target TBD); 801 -> integration_recipes records_in VSDP master (TBD)."
    action: "Author 6 outbound `data_object_relationships` rows; 3 of 6 need one-query verification of the target domain's current master before authoring."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S4}
      - {type: catalog_addition, scope: domains, trigger: "OBS/WORK-MGMT/VSDP target masters must be confirmed"}

  - id: B1B-S11
    summary: "B9b: author 3 intra-domain handoffs (RECIPE-DESIGN -> RUNTIME) once modules + lifecycle states exist. LEFT: gated on the build."
    finding: "Zero intra-domain handoffs. After B1B-S1 + B1B-S7 land, cross-module pairs derive: integration_recipe.published (RECIPE-DESIGN) -> RUNTIME; integration_trigger.fired (RECIPE-DESIGN) spawns integration_runs (RUNTIME); webhook_subscription.delivery_failed (RECIPE-DESIGN) -> RUNTIME retry."
    action: "Author 3 intra-domain `handoffs` rows with source_domain_id = target_domain_id = 36, integration_pattern=lifecycle_progression, friction_level=low."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S1}
      - {type: prerequisite_entity, ref: B1B-S7}

  # B1B-S8 RETIRED 2026-06-06 (per-domain-skill restoration / supersession header above).
  # The legacy domain-grain system skill 72 (`ipaas-system`, domain_id=36, domain_module_id=null)
  # is now the CORRECT shape under the new model (one domain-grain system skill that DERIVES its
  # toolset). Do NOT author per-module system skills; do NOT move/add skill_tools. Per-module tool
  # re-authoring (onto domain_module_tools) is tracked in audits/_modularization-backlog.md once the
  # build (B1B-S1) lands.

b2:
  - id: B2-S1
    summary: "Approve 2-module split (IPAAS-RECIPE-DESIGN + IPAAS-RUNTIME) + capability set, or propose alternative. THIS GATES THE BUILD."
    question: "Approve the 2-module split with RECIPE-DESIGN (masters 469, 471, 472, 473, 474) and RUNTIME (master 470), with capabilities IPAAS-RECIPE-AUTHORING, IPAAS-CONNECTOR-LIBRARY (cross-cutting candidate, see B2-S6), IPAAS-DATA-MAPPING, IPAAS-WEBHOOK-SUBSCRIPTION-MGMT, IPAAS-RECIPE-EXECUTION, IPAAS-ERROR-HANDLING-AND-RETRY? Or alternative (3-module split splitting connectors as IPAAS-CONNECTOR-MGMT or splitting RUNTIME into RUNTIME + OBSERVABILITY; single-module; other)?"
    options:
      - "approve 2-module split as proposed"
      - "3-module split (connectors out)"
      - "3-module split (observability out)"
      - "single-module"
      - "other (specify)"
    why: "Module split decision drives the build (B1A-BUILD / B1B-S1) and every other b1 item that requires module FKs (B1B-S4, B1B-S7, B1B-S9, B1B-S10, B1B-S11)."

  - id: B2-S3
    summary: "Confirm B4 pattern flag flips on data_objects 469, 470, 471, 474 + decide whether to split connection vault from connector template. DESTRUCTIVE shape change on existing rows."
    question: "Per-row, confirm or rewrite: (a) integration_recipes.has_submit_lock=true + has_single_approver=true? (b) integration_runs.has_submit_lock=true? (c) integration_connectors.has_personal_content=true OR queue integration_connections as a separate master per B3-S1 and move the flag there? (d) integration_data_mappings.has_submit_lock=true?"
    options:
      - "approve all"
      - "approve subset (specify)"
      - "split connection vault (relocate has_personal_content to integration_connections)"
      - "defer pending B3-S1 research"
    why: "Pattern flags are workflow-shape judgments the user owns; the connection-vault decision interacts with B3-S1."

  - id: B2-S4
    summary: "Decide webhook_subscriptions relationship to triggers (integration_triggers is now classified entity_type=catalog / config-shape per the 2026-06-07 execute pass, so its own lifecycle question is resolved)."
    question: "Relationship between `integration_triggers` and `webhook_subscriptions`: webhook is_subtype_of trigger (Workato shape), or sibling concepts (Boomi shape)? (The trigger lifecycle-shape sub-question is now resolved: triggers are entity_type=catalog, config-shape, no workflow.)"
    options:
      - "sibling"
      - "subtype"
    why: "Decision shifts B1B-S4 relationship row count by one."

  - id: B2-S5
    summary: "Declare inbound payload consumer-DMDOs (LCAP extend_workflows 222, APIM api_specifications 457) on IPAAS-RECIPE-DESIGN, embedded_master shells, or domain-level only."
    question: "How should IPAAS catalog its consumption of LCAP's `extend_workflows` (222) and APIM's `api_specifications` (457) that drive recipe / connector updates? (a) `consumer + optional` DMDO rows on IPAAS-RECIPE-DESIGN; (b) `embedded_master + optional` (preserves standalone-deploy story); (c) accept these as domain-level dependencies only (no DMDO row)."
    options:
      - "consumer + optional"
      - "embedded_master + optional"
      - "domain-level only"
    why: "Determines target_domain_module_id backfill on inbound handoffs 705 and 748 (B1B-S9)."

  - id: B2-S6
    summary: "Decide cross-cutting capability rename: `LCAP-INTEGRATION-CONNECTORS` (id 334) -> `INTEGRATION-CONNECTORS`, or parallel, or wait for third domain."
    question: "Existing capability id 334 was authored under LCAP. iPaaS connectors are the same surface. (a) Rename to `INTEGRATION-CONNECTORS` and link to BOTH LCAP and IPAAS via `capability_domains`; (b) leave the LCAP-prefixed capability and author parallel `IPAAS-CONNECTOR-LIBRARY` (double-counts); (c) wait until a third domain (DI / IDP / DXP) confirms the shape."
    options:
      - "rename + link both"
      - "parallel capability"
      - "wait for third domain"
    why: "Per Rule #18 cross-cutting promotion threshold is >=3 domains; today only LCAP + IPAAS but DI is likely the third."

  - id: B2-S7
    summary: "Decide canonical business function for IPAAS owner: extend `IT Infrastructure` (id 58) under `IT Operations` (id 27), or re-link IPAAS owner to id 27. DESTRUCTIVE re-attribution if (b)."
    question: "`business_function_domains` row id=84 carries `IT Infrastructure` (id 58) as owner but the canonical 20-function spine specifies `IT Operations` (id 27). (a) Is id 58 a legitimate sub-function under IT Operations (add parent_business_function_id link)? (b) Or is id 58 a parallel-tier alternative that should be re-linked to id 27?"
    options:
      - "extend (id 58 sub of id 27)"
      - "re-link IPAAS owner to id 27"
    why: "Catalog-wide spine hygiene; the agent cannot decide whether id 58 was an intentional sub-function or an accidental parallel. Owner (id 84) + contributor (id 85, Software Engineering id 26) rows already present; no additive C1 rows are clearly-correct until the owner spine is resolved."

  - id: B2-S8
    summary: "List which of Zapier, Make.com, Tray.io, Celigo, SnapLogic, IBM App Connect, Microsoft Power Automate, Jitterbit to add as IPAAS solutions and with which coverage_level."
    question: "A3 passes count-wise (4 primary solutions: ServiceNow Integration Hub, MuleSoft Anypoint Platform, Boomi Platform, Workato) but the surface is materially incomplete. Pick which vendors to add with which `coverage_level` per row. Zapier / Make.com may belong primarily in a separate CITIZEN-AUTO market (queued in _missing-domains.md)."
    options:
      - "add all 8 as primary"
      - "add subset (specify per row)"
      - "defer pending CITIZEN-AUTO market decision"

b3:
  - id: B3-S1
    candidate: integration_connections
    rationale: "Vendor evidence shows iPaaS vendors model a separate `connection` entity (configured credentials bound to a recipe) distinct from the reusable `connector` template. If confirmed, has_personal_content flag (B2-S3) moves here."
    vendor_evidence: [Workato, Boomi, MuleSoft, Tray.io]
    proposed_module: IPAAS-RECIPE-DESIGN

  - id: B3-S2
    candidate: integration_run_logs
    rationale: "Universal sub-resource of integration_runs that vendors expose as a first-class queryable entity for audit + debugging."
    vendor_evidence: [Workato, Boomi, MuleSoft, Zapier]
    proposed_module: IPAAS-RUNTIME

  - id: B3-S3
    candidate: integration_environments
    rationale: "Promotion across dev / test / prod is a core iPaaS workflow. May overlap with a cross-cutting <DOMAIN>-ENV-MGMT concept that APP-PAAS / LCAP also have."
    vendor_evidence: [Workato, Boomi, MuleSoft, SnapLogic]
    proposed_module: IPAAS-RECIPE-DESIGN

  - id: B3-S4
    candidate: integration_error_records
    rationale: "Distinct from a failed run row: a single run may produce multiple error records and errors persist after the run is archived."
    vendor_evidence: [Workato, Boomi, MuleSoft]
    proposed_module: IPAAS-RUNTIME

  - id: B3-S5
    candidate: integration_schedule_policies
    rationale: "Scheduling surface (cron, throttling, queue priority) may be config-shape on `integration_triggers` (now entity_type=catalog) or a separate master if rich enough."
    vendor_evidence: [Workato, Boomi, Zapier]
    proposed_module: IPAAS-RECIPE-DESIGN

extra_report_only_other_domains:
  - "OBS B10b owes target_domain_module_id on handoff 766 once OBS modules exist."
  - "APIM B10b owes target_domain_module_id on handoff 767 + source_domain_module_id on 748 once APIM modules exist."
  - "TEST-MGMT B10b owes target_domain_module_id on handoff 768 once TEST-MGMT modules exist."
  - "VSDP B10b owes target_domain_module_id on handoff 801 once VSDP modules exist."
  - "LCAP B10b owes source_domain_module_id on inbound handoff 705 once LCAP modules exist."
  - "LCAP B8 owes outbound `extend_workflows feeds_into <ipaas_master>` relationship row."
  - "APIM B8 owes outbound `api_specifications drives_refresh_of integration_connectors` relationship row."
  - "OBS B8 owes outbound `integration_runs reports_to <obs_master>` relationship row once OBS masters exist."
  - "VSDP B8 owes outbound relationship row mirroring handoff 801."
  - "Pre-existing U+2014 em-dash in `domains.business_logic` on row id=36 between 'delivery at scale' and 'code-dominant by definition'; routes to catalog-wide em-dash cleanup sweep."

extra_candidates_queued_to_missing_domains_md:
  - "EVENT-BROKER-PAAS (Solace PubSub+, Confluent Cloud, AWS EventBridge, Ably, Pusher, Kafka-as-a-Service)"
  - "CITIZEN-AUTO (Zapier, Make.com, IFTTT, Pipedream, n8n cloud, Pabbly Connect)"
