# domain_kind='bundle' audit (domain id 188; promoted 2026-06-16 from the cross-domain starter module
# REAL-ESTATE-AGENT, domain_module id 153). A bundle masters NOTHING: it embeds/consumes entities from
# other domains' markets (hosts: CRM, RE-BROKERAGE, CLM) and carries exactly one domain-grain skill.
# This audit validates the Rule #19 STARTER SHAPE plus the F-band skill-layer checks. Per Rule #2 a
# bundle is EXEMPT from the MARKET-shape floors (A2 capabilities, B1 masters, C1 function-owner, A3
# solutions) and uses the A1 partial carve-out (no TAM / crud_percentage / business_logic required).
# It is NOT exempt from F1/F2/F8: a bundle carries exactly one skill_type='domain' skill anchored to
# the bundle domain, with non-empty description + trigger_keywords (current rules; skill_type='system'
# and skills.domain_module_id are retired). All checks pass; nothing open.
schema_version: 2
domain: REAL-ESTATE-AGENT
status: passed
next_action_by: done
last_audit: "2026-06-17"
