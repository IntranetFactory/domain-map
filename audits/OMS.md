---
status: feedback_needed
last_transition: 2026-05-30
last_transition_by: agent
open_questions: 17
---

# OMS, Audit History

## 2026-05-30, Validate b1 (full 4-pass)

### Summary

- **Current footprint:** Pre-modular domain. ZERO `domain_modules` rows on `domain_id=32` and ZERO `domain_module_host_domains` entries; the entire OMS surface still runs through the legacy `domain_data_objects` rollup. 5 OMS-mastered data_objects (`order_allocations` 424, `inventory_locations` 425, `sourcing_decisions` 426, `return_authorizations` 427, `store_pickup_orders` 428), all `master + required`, plus 1 embedded_master row on `locations` (795, IWMS-canonical) at `necessity=optional`. Zero `capabilities` linked via `capability_domains`. 7 solutions in `solution_domains` (4 primary: ServiceNow Order Management for Industries, Salesforce Order Management, IBM Sterling Order Management, Oracle NetSuite; 1 secondary: NewStore Omnichannel Platform; 2 partial: Salesforce Commerce Cloud, VTEX Commerce Platform). 3 regulations (PSD2, PCI-DSS, EU VAT). 3 business_function_domains (Business Operations owner, Sales + Supply Chain contributors). 13 OMS-published `trigger_events` across the 4 workflow-bearing masters, every one with `event_category=''` (B9 catalog enum violation). 15 outbound cross-domain handoffs (981 through 995) hitting B2C-COMM, CRM, CSM, ERP-FIN; 4 inbound (322 + 502 from B2C-COMM, 1051 + 1052 from INV-MGMT). 13 of 15 outbound rows carry `source_domain_module_id=null` AND `target_domain_module_id=null`; the remaining 2 (994 store_pickup_order.created and 989 return_authorization.received, both targeting CRM-ACCT-MGT 46) have `target_domain_module_id=46` populated but `source_domain_module_id=null`. 17 lifecycle states across 4 of 5 masters (`inventory_locations` has none); only 1 state (`return_authorizations.approved` order 2) carries `requires_permission=true`, with `permission_verb_override='approve_return'`. Zero `data_object_aliases` rows on any OMS master. Zero intra-OMS `data_object_relationships` edges among the 5 masters; the only edges anywhere are 3 rows linking `order_allocations` and `return_authorizations` to CSM `customer_cases` (103) and INV-MGMT `inv_stock_balances` (787) `informs_atp_of order_allocations`. Zero `users` edges to or from any OMS master. 1 legacy domain-level `skills` row (`oms-system`, id 89, `skill_type='system'`, `domain_id=32`, `domain_module_id=null`) with 6 `skill_tools` (5 platform-tier `query_*` rows plus `send_email` side_effect). Zero per-module system skills. Zero role rows linked to OMS via `role_modules` (no modules to link to). 0 of 19 cross-domain handoffs (15 outbound + 4 inbound) carry `handoff_processes` rows. APQC coverage 0%; Semantius score uncomputable on a per-module basis.

- **Vendor-surface basis (Pass 2 flagship enumeration):** the four "primary" linked solutions (ServiceNow Order Management for Industries, Salesforce Order Management, IBM Sterling Order Management, Oracle NetSuite) anchor the enterprise OMS layer. NewStore (secondary) is the retail-omnichannel specialist. Pure-play OMS specialists not yet linked but visible from the market: Manhattan Active Omni, Fluent Commerce (cloud-native OMS), Kibo Commerce OMS, OrderDynamics (Tecsys), Aptos OMS (retail-focused), HotWax Commerce OMS (open-source Apache OFBiz heritage), enVista Unified Commerce. Suite-aligned reference points: SAP Commerce Cloud OMS (formerly Hybris), Oracle Retail Order Management (RGBU), Microsoft Dynamics 365 Commerce. Compliance-focused: the 3 regulations already loaded (PSD2, PCI-DSS, EU VAT) cover payment and tax surface; missing: GDPR (customer-data on orders), CCPA, EU Omnibus Directive (consumer rights and refund timelines), DAC7 (marketplace seller reporting), Section 321 / Type 86 (US de minimis import filings for cross-border DTC). Adjacency to candidate-domain markets enumerated in Bucket 3 / queue.

- **Bucket 1 (in-scope, agent fixable):** 11 items.
- **Bucket 2 (surface-for-user, judgment):** 6 items.
- **Bucket 3 (Phase 0 pending, speculative):** 0 items beyond the candidate-domain queue (the speculative entity / modularization / regulation list lives at the bottom and is itself a single Bucket-3 line).

**Neighbor discovery (auto-derived from handoffs + cross-domain DMDO + cross-domain relationships, ranked by edge weight):**

| Neighbor | Out | In | DMDO | Cross-rels | Weight | Pass shape |
|---|---|---|---|---|---|---|
| B2C-COMM | 6 | 2 | 0 (OMS has no DMDO rows; INV-MGMT has the B2C-COMM consumer rows, not OMS) | 0 OMS-side; 1 INV mirror | 8 | Pairwise (full) |
| ERP-FIN | 5 | 0 | 0 | 0 | 5 | Pairwise (full) |
| CSM | 3 | 0 | 0 | 2 OMS->CSM (`order_allocations opens customer_cases`, `return_authorizations opens customer_cases`) | 5 | Pairwise (full) |
| INV-MGMT | 0 | 2 | 1 (INV-CORE-STOCK 61 embedded_master + required on `inventory_locations` 425, broken pointer per B5 because OMS has no DMDO master row) + 1 INV-REPLENISHMENT consumer + required on `order_allocations` 424 (also broken pointer) | 1 (`inv_stock_balances informs_atp_of order_allocations`) | 4 | Pairwise (full) |
| CRM | 2 | 0 | 1 (CRM-ACCT-MGT 46 consumer + optional on `return_authorizations` 427 and `store_pickup_orders` 428) | 0 | 3 | Pairwise (full) |
| IWMS | 0 | 0 | 1 (OMS embedded_master + optional on `locations` 795, IWMS-canonical via IWMS-LOCATION-MASTER 98) | 0 | 1 | Lightweight |

**Structural pass bands:**

- **S1 sweep:** FK-to-`domains` table coverage: `domain_modules.domain_id` zero rows for OMS (M1 hard fail, the structural gate), `domain_module_host_domains.domain_id` zero rows (acceptable, no cross-cutting modules host on OMS), `capability_domains.domain_id` zero rows (A2 hard fail), `business_function_domains.domain_id` 3 rows (C1 pass), `domain_data_objects.domain_id` 6 rows via the legacy rollup (B1 pass), `solution_domains.domain_id` 7 rows (A3 pass), `domain_regulations.domain_id` 3 rows (A-band side check pass), `handoffs.source_domain_id` 15 outbound (B9 pass on volume), `handoffs.target_domain_id` 4 inbound (B10 pass on volume), `skills.domain_id` 1 row (legacy, see F1). `domains.parent_domain_id` is null on OMS itself (no sub-domain hierarchy).
- **S2 / S3 sweep:** vacuously skipped, no modules to iterate per-module DMDO/skill coverage on. Per-master indirect coverage on the 5 OMS masters: `order_allocations` 4 states / 5 events / 0 aliases; `inventory_locations` 0 states / 2 events / 0 aliases; `sourcing_decisions` 3 states / 2 events / 0 aliases; `return_authorizations` 6 states / 3 events / 0 aliases; `store_pickup_orders` 4 states / 3 events / 0 aliases. Aliases at zero on every master is a B11 floor fail across the board.
- **A1 pass.** All 7 domains-row fields populated (`crud_percentage=70`, `business_logic` non-empty, `min_org_size='20 s <500'`, `cost_band='$$$'`, `certification_required=false`, `usa_market_size_usd_m=1800`, `market_size_source_year=2025`).
- **A2 hard fail.** Zero `capability_domains` rows. The market has at least 5-8 well-known capability shapes (order capture, order orchestration, distributed sourcing decision-making, available-to-promise computation, returns and reverse logistics, omnichannel fulfillment including BOPIS / ship-from-store, post-purchase order status, customer-tier-aware allocation). See B1-S2.
- **A3 pass.** 7 solutions linked with coverage_level on every row (4 primary, 1 secondary, 2 partial).
- **A4 fail.** Both `catalog_tagline` and `catalog_description` empty. Buyer voice draft required. See B1-S3.
- **M1 hard fail.** Zero `domain_modules` rows. Blocks every downstream M / B-DMDO / E / F band check that depends on module existence. See B1-S1.
- **M2 / M4 / M5 / M6 / M7 N/A.** Module-set queries return empty; checks are vacuously inapplicable until B1-S1 lands at least 1 module. Once modules exist, M7 within-domain coherence will need a re-pass: 4 of the 5 OMS masters carry workflow lifecycles, so the canonical authoring is one master per workflow shape; the embedded_master demote / promote question for `inventory_locations` 425 (currently INV-CORE-STOCK 61 embedded_master) becomes resolvable once OMS canonically masters it.
- **B1 pass via legacy rollup.** 5 masters in `domain_data_objects` for OMS. The blocker is that NONE has a DMDO `role='master'` row catalog-wide (S2 sweep reports zero `master` DMDO rows on data_object_ids 424, 425, 426, 427, 428); the deployer's blueprint emitter cannot find the canonical owner until B1-S1 introduces modules that master these.
- **B2 pass.** Every master carries `singular_label` and `plural_label`. Note: `return_authorizations.plural_label='Return Authorization (RMA)s'` is mechanically pluralized awkwardly; surface as Bucket 2 nit B2-S3.
- **B3 pass.** All 5 OMS masters use prefix-free domain-shaped names. None is a bare-word noun that would invoke Rule #9 collision arbitration; `order_allocations`, `inventory_locations`, `sourcing_decisions`, `return_authorizations`, `store_pickup_orders` are sufficiently specific. No `is_canonical_bare_word=true` claims required.
- **B4 partial.** Pattern flag positive re-evaluation: only `return_authorizations.has_single_approver=true`. Positive re-evaluation candidates: `return_authorizations.has_submit_lock` should plausibly be `true` once the RMA reaches `received` (lines immutable post-receipt for refund-eligibility computation); `order_allocations.has_submit_lock` plausibly `true` once `released` (terminal state); `sourcing_decisions.has_submit_lock=true` once `fulfilled` (the sourcing trail becomes immutable for audit); `order_allocations.has_personal_content` candidate `true` if the row carries shipping address or buyer contact (depends on whether identity stays on the parent `commerce_orders` shell). Surface as B2-S2.
- **B5 partial.** `locations` (795) embedded_master is sound: canonical IWMS master at IWMS-LOCATION-MASTER 98 exists. **But** the catalog has a broken-pointer issue from the other direction: INV-CORE-STOCK 61 holds `embedded_master + required` on `inventory_locations` (425) and no module catalog-wide masters that data_object via DMDO. Same for INV-REPLENISHMENT 62 holding `consumer + required` on `order_allocations` (424). Both rows point at OMS-owned masters whose DMDO `master` row will only exist once B1-S1 lands. Report-only from this audit (the canonical fix is OMS authoring its modules and the master rows; the INV side stays correct).
- **B6 hard fail.** Zero intra-domain `data_object_relationships` rows among the 5 OMS masters. Expected edges from the workflow shape: `order_allocations relates_to sourcing_decisions` (the sourcing decision is what produced the allocation); `order_allocations stages_pickup_for store_pickup_orders` (BOPIS path); `inventory_locations sources_for order_allocations` (the allocated location is an inventory_location); `return_authorizations originates_from order_allocations` (the original allocation is what gets RMA'd); `store_pickup_orders staged_at inventory_locations`. 5 candidate intra-domain edges. See B1-S4.
- **B7 hard fail.** Zero `users` edges to or from any OMS master. Expected per Rule #10: `order_allocations` (allocator/operator), `return_authorizations` (approver, intake_clerk), `sourcing_decisions` (operator override), `store_pickup_orders` (picker, customer-service contact). At minimum 4-6 user edges. See B1-S5.
- **B8 partial.** 2 OMS->CSM cross-domain relationships ('order_allocations opens customer_cases', 'return_authorizations opens customer_cases'). Missing outbound mirrors for 15 outbound handoffs: every handoff with a clean payload->target mapping should have a corresponding `data_object_relationships` row. Most missing edges are OMS-internal (the handoffs publish events about OMS-owned objects to consumers like ERP-FIN and B2C-COMM that don't always have a master to relate to), but at least 3-4 are addable: `order_allocations triggers_credit_memo_for credit_memos` (ERP-FIN), `sourcing_decisions informs_promise_for commerce_orders` (B2C-COMM), `store_pickup_orders fulfills commerce_orders` (B2C-COMM). Surface in B1-S4 alongside the intra-domain edges.
- **B9 hard fail.** Every one of the 13 OMS-published `trigger_events` (rows 1125 through 1137) carries `event_category=''` (empty string). The catalog enum check on `trigger_events.event_category` requires a value in `('lifecycle', 'state_change', 'threshold', 'signal')` per the SKILL.md enum table. Five of these events are state-change-shaped (`inventory_location.opened`, `order_allocation.created`, `order_allocation.failed`, `return_authorization.issued`, `return_authorization.received`, `return_authorization.expired`, `store_pickup_order.created`, `store_pickup_order.ready`, `store_pickup_order.expired`, `sourcing_decision.computed`, `sourcing_decision.overridden`). One is threshold-shaped (`inventory_location.capacity_threshold_breached`). One is lifecycle-shaped (`order_allocation.reassigned` if treated as a flow-stage rather than a transition; also reasonably state_change). See B1-S7.
- **B9b N/A (gated by M1).** With zero modules, intra-domain cross-module handoffs cannot exist. Pre-condition is B1-S1; the B9b pass becomes runnable on the next audit.
- **B10 report.** 4 inbound rows: 322 from B2C-COMM on `commerce_order.placed -> commerce_orders` (NULL source_module_id, NULL target_module_id); 502 from B2C-COMM on `order_line.added -> order_lines` (NULL source_module_id, NULL target_module_id); 1051 + 1052 from INV-MGMT INV-CORE-STOCK 61 on `inv_stock_balance.on_hand_changed` and `inv_stock_balance.allocated`, both targeting `order_allocations` 424 (NULL target_module_id). The OMS-side target_domain_module_id is NULL on all 4 because OMS has no modules to attribute them to (B10b sub-case 1, upstream gap is B1-S1). Owed by other domains: B2C-COMM B9 (source_module_id NULL on 322 + 502 because B2C-COMM hasn't modularized either); INV-MGMT B9 already populates source_module_id on 1051 + 1052. Surfaces in B1-S10.
- **B10b hard fail.** 13 of 15 outbound handoffs carry NULL `source_domain_module_id`. Per the standard B10b derivation, fix needs the source module to exist (B1-S1 dependency); once OMS modules ship, source_domain_module_id is resolvable from the event's data_object owner. The 2 already-resolved rows (994 store_pickup_order.created and 989 return_authorization.received, both targeting CRM-ACCT-MGT 46) demonstrate that the target side is patchable independent of OMS. The remaining 13 NULL target rows are the receiving domain's audit work, not OMS's. See B1-S10.
- **B11 hard fail.** Zero `data_object_aliases` rows on any of 5 OMS masters. All 5 entities have widely-varying vendor terminology: `order_allocations` is also called `order_reservations` (Salesforce, ServiceNow), `inventory reservations` (Manhattan), `commitments` (Oracle Retail); `inventory_locations` is `nodes` (Manhattan, Fluent), `facilities`, `network nodes` (IBM Sterling), `stores` (Salesforce); `sourcing_decisions` is `routing decisions`, `allocation plans` (IBM), `fulfillment plans` (Salesforce); `return_authorizations` is `RMAs`, `RAs`, `returns` informally; `store_pickup_orders` is `BOPIS orders`, `click-and-collect`, `curbside orders`, `in-store pickup`. See B1-S6.
- **B12 partial.** 4 of 5 masters carry lifecycle states (`order_allocations` 4, `sourcing_decisions` 3, `return_authorizations` 6, `store_pickup_orders` 4). `inventory_locations` (425) has zero states, which is acceptable IF the row is a config-shape master (locations created and edited inline by ops admins, no workflow). The Rule #12 exemption needs surfacing for user approval; do NOT auto-populate `data_objects.notes` (Rule #15). Surface as B2-S5. Workflow-gate state count is low: only `return_authorizations.approved` carries `requires_permission=true`. Likely candidates for promotion to workflow-gate: `order_allocations.released` (cancellation gate, often an operator override), `sourcing_decisions.routed` (operator-override gate per the `overridden` event already loaded), `return_authorizations.received` (intake gate that flips refund accrual), `return_authorizations.refunded` (treasury-grade gate that releases payment). All `domain_module_id` NULL because no modules; that's the resolution path through B1-S1 + B1-S8.
- **C1 pass.** 1 owner (Business Operations), 2 contributors (Sales, Supply Chain). The owner row is reasonable for OMS (the order-orchestration discipline sits under Business Operations or its sibling Customer Operations in the function spine); Supply Chain contribution makes sense (sourcing decisions ride against inventory networks).
- **C2 pass.** Zero `business_function_capabilities` rows is acceptable today because A2 has zero capabilities. Re-pass C2 after B1-S2 lands.
- **D1 deferred.** UI spot-check is a user action, not audit-time.
- **E1 / E2 / E3 / E4 / E5 / E6 N/A.** Zero `role_modules` rows linkable to OMS because no `domain_modules` exist. Per the SKILL.md E1 qualifier (single-module domain may not need roles given the 2-module floor blocks authoring), this is vacuously not a fail until B1-S1 plus B1-S2 land at the >=3-capability threshold; at that point the multi-module floor applies and >=3 roles are expected. The legacy domain-level skill (F1) is not a roles object.
- **F1 pass with caveat.** 1 row `skills.id=89` `oms-system` `domain_id=32` `domain_module_id=NULL`. Per F1 the row passes IF no module-level system skill exists for this domain yet, which is true. The row becomes obsolete the moment any module-level skill ships; F1 then expects DELETE. Mark for B2-S1.
- **F2 hard fail.** Zero `skill_type='system'` rows where `domain_module_id` points at any OMS module (vacuously, because no modules exist). Per Rule #17 every `domain_modules` row needs exactly one system skill; fix is gated by B1-S1.
- **F3 partial.** The legacy skill 89 has 6 `skill_tools` rows: 5 `query_*` (all `platform` coverage) plus 1 `send_email` `side_effect`. F3 floor of >=1 row is satisfied; the canonical re-author once module-level skills land will distribute these across per-module agents.
- **F4 pass.** Each linked tool's `operation_kind` matches its `data_object_id` shape (5 `query` rows with `data_object_id` set; 1 `side_effect` with NULL `data_object_id`).
- **F5 uncomputable.** No module-level system skill exists yet; Semantius score per Rule #17 is undefined for OMS modules. Once B1-S1 + a per-module system skill ship, score is computable.
- **F7 fail.** Legacy skill 89 links `send_email` (channel primitive) directly. Per the § "Channel vs capability authoring rule" + F7 audit, default is `notify_person`. The skill_tools row has no `notes` justifying the channel-specific link. See B1-S9.
- **H1 hard fail.** 0 of 19 cross-domain handoffs carry `handoff_processes` rows. Expected volume per H1 (N=19): 9 to 15 `agent_curated` rows. See B1-H1.

OMS Semantius score: **uncomputable** (F2 hard fail; F5 cannot return a number until B1-S1 plus a per-module system skill ship). Strict and operational both undefined.

### Bucket 1, In-scope confirmed gaps

#### STRUCTURAL band failures

| ID | Band | Finding | Fix |
|---|---|---|---|
| B1-S1 | **M1 hard fail (Rule #14 structural gate)** | OMS has zero `domain_modules` rows. The 5 OMS masters live entirely in the legacy `domain_data_objects` rollup. Without modules, the deployer cannot find canonical owners (every B-band DMDO check returns vacuous; M2 / M4 / M5 / M6 / M7 cannot run; E1 / E2 cannot run; F2 / F5 cannot run; the per-master indirect coverage sweep S2 is empty). | Author the OMS module set. Default proposal pending B2-S4 capability-count call: a 4-module split aligned to the 4 workflow-bearing masters (OMS-ORDER-ORCHESTRATION mastering `order_allocations` + `sourcing_decisions`, OMS-RETURNS mastering `return_authorizations`, OMS-STORE-PICKUP mastering `store_pickup_orders`, plus OMS-LOCATIONS as a configuration module mastering `inventory_locations`). A 2-module compromise is OMS-CORE-ORCHESTRATION + OMS-RETURNS-AND-PICKUP. A 1-module shape passes M1 + vacuously passes M2 but loses analytical resolution for boundary work. User picks the granularity in B2-S4 before the loader runs. Once modules exist, this audit's B-band re-runs to populate `domain_module_data_objects` master rows on each, demoting the legacy `domain_data_objects` rollup. |
| B1-S2 | **A2 hard fail** | Zero capabilities linked to OMS via `capability_domains`. The market has at least 5-8 standard capability shapes (order capture, distributed order orchestration, sourcing and rate-shop, available-to-promise computation, returns and reverse logistics, omnichannel fulfillment including BOPIS / ship-from-store / curbside, post-purchase order status). | Author 6-8 `capabilities` rows plus `capability_domains` linking to OMS. Apply Cross-cutting capability convention for capabilities that span >=3 domains (`order-capture` shared with B2C-COMM; `available-to-promise` shared with INV-MGMT; `returns` adjacent to CSM). |
| B1-S3 | **A4 fail (Rule #20)** | `catalog_tagline` and `catalog_description` both empty strings. Buyer-voice copy required: a single-sentence tagline and 1-3 paragraph description in workflow + value voice (per Rule #20, NOT the analyst-voice market positioning that the `description` column carries). | Draft buyer-voice copy as part of this audit's deliverable, surface to the user for review BEFORE writing. Once a non-empty value exists in either column, no overwrite without explicit per-row approval (Rule #20). Recommended tagline shape: "Capture orders from any channel, route them to the right inventory node, and orchestrate everything from sourcing to returns in one place." Description draft will land in the fix-load conversation. |
| B1-S4 | **B6 hard fail + B8 partial (intra-domain plus cross-domain relationship edges)** | Zero intra-domain `data_object_relationships` rows among the 5 OMS masters; only 3 cross-domain rows exist (the 2 OMS->CSM `opens customer_cases` rows plus the INV-side `inv_stock_balances informs_atp_of order_allocations` mirror). The workflow-shape implies at least 5 intra-domain edges (sourcing_decisions->order_allocations, order_allocations->store_pickup_orders, inventory_locations->order_allocations, return_authorizations->order_allocations, store_pickup_orders->inventory_locations) and 3-4 additional cross-domain edges (order_allocations->credit_memos in ERP-FIN, sourcing_decisions->commerce_orders in B2C-COMM, store_pickup_orders->commerce_orders in B2C-COMM, return_authorizations->commerce_orders in B2C-COMM). | Author 5 intra-domain + 3-4 cross-domain `data_object_relationships` rows (verb, inverse_verb, relationship_type, relationship_kind, is_required, owner_side). Bundle into the cluster-drafts pattern alongside B1-S5 user edges and B1-S6 aliases. |
| B1-S5 | **B7 hard fail (Rule #10)** | Zero `users` edges to or from any OMS master. Expected at minimum: `order_allocations` (allocator / operator override), `return_authorizations` (approver per the `approve_return` permission_verb_override, intake clerk on `received`), `sourcing_decisions` (operator override per the `sourcing_decision.overridden` event), `store_pickup_orders` (picker, customer-service contact on expiration). | Load 4-6 `data_object_relationships` rows against the platform_builtin `users` row (id 748). Same loader as B1-S4. |
| B1-S6 | **B11 hard fail (aliases)** | Zero alias rows on any OMS master; each master has 3-5 well-known vendor synonyms in flagship OMS docs. | Author 12-20 alias rows: `order_allocations` aliases (`Order Reservation`, `Inventory Reservation`, `Commitment`, `Allocation Line`); `inventory_locations` aliases (`Node`, `Network Node`, `Facility`, `Store`, `Fulfillment Node`, `Dark Store`); `sourcing_decisions` aliases (`Routing Decision`, `Allocation Plan`, `Fulfillment Plan`, `Sourcing Trail`); `return_authorizations` aliases (`RMA`, `RA`, `Return`, `Return Order`); `store_pickup_orders` aliases (`BOPIS Order`, `Click and Collect`, `Curbside Order`, `In-Store Pickup`). Apply `alias_type` per the catalog vocabulary; cross-reference vendor docs cited inline. |
| B1-S7 | **B9 hard fail (catalog enum on `trigger_events.event_category`)** | All 13 OMS-published trigger_events carry `event_category=''` (empty string), violating the enum `('lifecycle', 'state_change', 'threshold', 'signal')`. The events have rich descriptions but the structural column required for `audit_backlog` and downstream analytics is empty. | PATCH the 13 rows with category per shape: state_change for the 11 lifecycle-transition events (`inventory_location.opened`, `order_allocation.created`, `order_allocation.failed`, `order_allocation.reassigned`, `return_authorization.issued`, `return_authorization.received`, `return_authorization.expired`, `sourcing_decision.computed`, `sourcing_decision.overridden`, `store_pickup_order.created`, `store_pickup_order.ready`, `store_pickup_order.expired`); threshold for `inventory_location.capacity_threshold_breached` (1126). Single batched PATCH via a small TS loader. |
| B1-S8 | **B12 partial (workflow-gate `domain_module_id` NULL)** | The single `requires_permission=true` lifecycle state (`return_authorizations.approved`, state_order 2) carries `domain_module_id=null`. Once B1-S1 lands an OMS-RETURNS module mastering `return_authorizations`, this row needs `domain_module_id` patched so the materialized permission prefix is `oms-returns:approve_return` and not domain-level. The remaining 16 states do NOT have `requires_permission=true` today but B2-S2 may flip several; whichever flips during the fix-loop will also need `domain_module_id` set at insert / PATCH time. | Single-row PATCH after B1-S1. Bundled into the post-module fix-loop along with re-running B12. |
| B1-S9 | **F7 fail (channel-vs-capability rule)** | Legacy skill 89 (`oms-system`) links `send_email` directly with no `skill_tools.notes` justification. Per § "Channel vs capability authoring rule", default for generic notifications is `notify_person`. The notifications OMS sends (allocation-failed customer alerts, RMA-issued confirmations, BOPIS-ready notifications, BOPIS-expired notifications) are NOT channel-specific by workflow contract; they substitute fine over SMS, push, or chat depending on deployment. | PATCH `skill_tools` row (skill 89, tool 37 send_email) to point at `notify_person` (tools row TBD; if `notify_person` already exists at the catalog-wide tool id, re-point; if not, separate insert first). Once B1-S1 lands per-module skills, the legacy skill itself goes away per F1. |
| B1-S10 | **B10 + B10b report-only (FK gaps on other domains)** | 13 of 15 outbound rows carry NULL `source_domain_module_id` (own work, gated by B1-S1, resolvable in this audit's fix-loop) AND NULL `target_domain_module_id` (other domains' B10b audit work). 2 inbound from B2C-COMM carry NULL `source_domain_module_id` (B2C-COMM's B9). 2 inbound from INV-MGMT carry NULL `target_domain_module_id` (this is OMS's own work, gated by B1-S1). The 13 target-NULL outbound rows split by receiving domain: B2C-COMM 6 (990, 993, 982, 984, 986, 1059 if present); ERP-FIN 5 (991, 995, 981, 983, 988); CSM 3 (992, 985, 987). The 4 INV-side inbound or boundary references (1051, 1052) are wired except for target_module_id which will populate once B1-S1 lands. | Schedule b1 audits on B2C-COMM (resolve source_module_id on 322 / 502 + target_module_id on 6 OMS-outbound rows; also B5 gap on `commerce_orders` 381 and `order_lines` 382 having no canonical DMDO master row), ERP-FIN (target_module_id on 5 OMS-outbound rows), CSM (target_module_id on 3 OMS-outbound rows). Report-only from OMS's perspective; do not author rows on those domains from this pass. |

#### APQC TAGGING

0 of 19 cross-domain handoffs carry `handoff_processes` rows; volume expectation per H1 (N=19) is 9 to 15 `agent_curated` rows. The audit proposes the following candidates from the analyst's structural-pass model, after `/processes?source_framework=eq.apqc_pcf_cross_industry` lookups during this audit:

| handoff_id | source -> target | trigger_event | payload | Proposed PCF row | PCF id | Confidence |
|---|---|---|---|---|---|---|
| 981 | OMS -> ERP-FIN | `inventory_location.opened` | `inventory_locations` | Manage and track inventory deployment (10353) | 852 | confident L4 |
| 982 | OMS -> B2C-COMM | `inventory_location.capacity_threshold_breached` | `inventory_locations` | Track product availability (10355) | 854 | medium |
| 983 | OMS -> ERP-FIN | `order_allocation.created` | `order_allocations` | Process allocations (10821) | 1380 | confident L4 |
| 984 | OMS -> B2C-COMM | `order_allocation.created` | `order_allocations` | Handle sales order inquiries including post-order fulfillment transactions (10200) | 740 | confident L4 |
| 985 | OMS -> CSM | `order_allocation.failed` | `order_allocations` | Research and resolve order exceptions (10298) | 814 | confident L4 |
| 986 | OMS -> B2C-COMM | `order_allocation.reassigned` | `order_allocations` | Determine fulfillment process (10197) | 738 | confident L4 |
| 987 | OMS -> CSM | `return_authorization.issued` | `return_authorizations` | Authorize return (10364) | 936 | confident L4 |
| 988 | OMS -> ERP-FIN | `return_authorization.received` | `return_authorizations` | Process return and record reason (20095) | 937 | confident L4 |
| 989 | OMS -> CRM | `return_authorization.received` | `return_authorizations` | Manage flow of returned products (10352) | 850 | confident L4 |
| 990 | OMS -> B2C-COMM | `sourcing_decision.computed` | `sourcing_decisions` | Determine fulfillment process (10197) | 738 | confident L4 |
| 991 | OMS -> ERP-FIN | `sourcing_decision.computed` | `sourcing_decisions` | Pick, pack, and ship product for delivery (10356) | 855 | medium L4 (the ERP touchpoint is cost-of-fulfillment planning more than picking; medium because the L4 framing is fulfillment-side not finance-side) |
| 992 | OMS -> CSM | `sourcing_decision.overridden` | `sourcing_decisions` | Research and resolve order exceptions (10298) | 814 | confident L4 |
| 993 | OMS -> B2C-COMM | `store_pickup_order.ready` | `store_pickup_orders` | Handle sales order inquiries including post-order fulfillment transactions (10200) | 740 | confident L4 |
| 994 | OMS -> CRM | `store_pickup_order.created` | `store_pickup_orders` | Accept and validate sales orders (10194) | 735 | confident L4 |
| 995 | OMS -> ERP-FIN | `store_pickup_order.expired` | `store_pickup_orders` | Manage flow of returned products (10352) | 850 | medium L4 (expiration is a refund / restock flow, neighbor of returns) |
| 322 | B2C-COMM -> OMS (inbound) | `commerce_order.placed` | `commerce_orders` | Accept and validate sales orders (10194) | 735 | confident L4 |
| 502 | B2C-COMM -> OMS (inbound) | `order_line.added` | `order_lines` | Accept and validate sales orders (10194) | 735 | confident L4 |
| 1051 | INV-MGMT -> OMS (inbound) | `inv_stock_balance.on_hand_changed` | `order_allocations` | Track product availability (10355) | 854 | confident L4 |
| 1052 | INV-MGMT -> OMS (inbound) | `inv_stock_balance.allocated` | `order_allocations` | Track product availability (10355) | 854 | confident L4 |

19 candidate APQC tags total (all 19 cross-domain handoffs covered), all `proposal_source='agent_curated'`, `record_status='new'`. PCF ids verified against `/processes?source_framework=eq.apqc_pcf_cross_industry` during this audit. Two `medium` confidence rows (991, 995) have an alternative L4 candidate noted; user can prefer the alternative at approval time. No deferred-to-Discover rows: every handoff has a defensible PCF match given OMS sits squarely inside the sales-order-management and reverse-logistics PCF subtree.

#### Pairwise reconciliation (Pass 4) findings per neighbor (edge weight >= 3)

**B2C-COMM <-> OMS (weight 8).** Section 1 (fully wired): zero pairs (both sides pre-modular on the OMS edge). Section 2 (NULL module FK on existing rows): 6 outbound NULL target_module_id (982, 984, 986, 990, 993, plus one of 994 already populated to CRM not B2C-COMM); 2 inbound NULL source_module_id (322 commerce_orders.placed, 502 order_line.added). Section 3 (missing handoffs implied by catalog): OMS -> B2C-COMM on `return_authorization.refunded` so the storefront updates order status to "refunded"; B2C-COMM -> OMS on `commerce_order.payment_captured` so allocation can proceed (currently inferred from `commerce_order.placed` but payment-state branch matters for fraud-hold flows); B2C-COMM -> OMS on `commerce_order.cancelled` so allocation releases. Section 4 (boundary integrity gaps): `commerce_orders` (381) and `order_lines` (382) consumed by OMS via legacy `domain_data_objects` rollup but have NO `role='master'` DMDO row catalog-wide; same broken-pointer pattern as the INV-side (B2C-COMM B5 audit work). Section 5 (cross-domain relationships): zero rows directly between OMS masters and B2C-COMM masters; expected: `sourcing_decisions informs_promise_for commerce_orders`, `order_allocations fulfills order_lines`, `store_pickup_orders fulfills commerce_orders`, `return_authorizations refunds commerce_orders`. Cluster into B1-S4 + new inbound handoffs into the B2C-COMM audit.

**ERP-FIN <-> OMS (weight 5).** Section 1: zero pairs fully wired. Section 2: 5 outbound NULL target_module_id (981, 983, 988, 991, 995). Section 3: missing handoffs OMS -> ERP-FIN on `return_authorization.refunded` for the actual cash release (988 covers `received`, not the disbursement); OMS -> ERP-FIN on `order_allocation.cancelled` so accruals reverse. Section 4: clean (ERP-FIN domain not yet sized in this audit, but no obvious broken pointers). Section 5: zero rows; expected: `order_allocations triggers_credit_memo_for credit_memos`, `return_authorizations triggers_refund_for credit_memos`. Add to B1-S4.

**CSM <-> OMS (weight 5).** Section 1: zero pairs fully wired. Section 2: 3 outbound NULL target_module_id (985, 987, 992). Section 3: missing inbound CSM -> OMS on `customer_case.escalated_for_refund` so RMA expedites; CSM -> OMS on `customer_case.dispute_resolved` so allocation re-runs. Section 4: clean. Section 5: 2 cross-domain rows present (`order_allocations opens customer_cases`, `return_authorizations opens customer_cases`); expected also `sourcing_decisions opens customer_cases` (when an override triggers an escalation), and the inverse from CSM into OMS context when a case generates a return-action.

**INV-MGMT <-> OMS (weight 4).** Section 1: zero pairs fully wired (both inbound rows 1051, 1052 have populated `source_domain_module_id=61` but NULL `target_domain_module_id`). Section 2: 2 inbound NULL target_module_id (1051, 1052; own work, gated by B1-S1). Section 3: missing OMS -> INV-MGMT on `order_allocation.released` so INV-CORE-STOCK frees the reservation; OMS -> INV-MGMT on `return_authorization.received` so the disposition flow can post a return stock_movement (today the OMS side fires `received` to ERP-FIN and CRM but nothing to INV-MGMT, which is where the physical inventory delta originates). Section 4: 2 broken-pointer DMDO rows from the INV side (INV-CORE-STOCK 61 `embedded_master + required` on `inventory_locations` 425 with no master row; INV-REPLENISHMENT 62 `consumer + required` on `order_allocations` 424 with no master row); both resolve once B1-S1 lands the OMS modules. Section 5: 1 cross-domain row (`inv_stock_balances informs_atp_of order_allocations`); expected `order_allocations reserves_against inv_stock_balances` (inverse) plus `return_authorizations restocks_to inv_stock_items` once disposition flows are modeled.

**CRM <-> OMS (weight 3).** Section 1: 2 pairs partially wired (994 store_pickup_order.created and 989 return_authorization.received with `target_domain_module_id=46 CRM-ACCT-MGT` populated but source_module_id NULL). Section 2: source_module_id NULL on 994 + 989 (own work, gated by B1-S1). Section 3: missing OMS -> CRM on `order_allocation.failed` (escalation to account manager for VIP-tier customers) plausible but not required; CRM -> OMS on `customer_loyalty_tier_changed` so allocation rules re-weight (advanced personalization). Section 4: clean. Section 5: zero cross-domain rows; expected `store_pickup_orders engages_customer_in CRM accounts`, `return_authorizations involves CRM accounts`. Add to B1-S4.

**Lightweight neighbors (weight 1-2, one-line summaries):**

- **IWMS <-> OMS (weight 1).** Zero handoffs in either direction. OMS embedded_masters `locations` (795) on the IWMS canonical at IWMS-LOCATION-MASTER 98 (necessity optional). Clean inheritance; no audit gaps.

### Bucket 2, Surface-for-user (judgment calls)

| ID | Question | Why agent can't answer | Options |
|---|---|---|---|
| B2-S1 | **F1 transitional, legacy domain-level `oms-system` skill (id 89, 6 skill_tools rows).** Per F1 the legacy row passes IF no module-level system skill exists. Once B1-S1 lands modules and Phase-S authors per-module system skills, the legacy skill becomes a DELETE candidate. Should the audit's fix-loop schedule the DELETE in the same load as the per-module skills, or hold it until the per-module skills are confirmed populated end-to-end? | DELETE timing affects whether intermediate state (legacy + new skills coexisting) is acceptable; user owns the deploy-window discipline. | (a) DELETE legacy in the same load as per-module skills land. (b) DELETE in a follow-up load after F2 / F3 / F5 verify green on per-module skills. (c) Keep legacy as a "domain-wide composite" agent if the user has a use case (rare; usually F1 means delete). |
| B2-S2 | **B4 pattern-flag positive re-evaluation per Rule #12.** Current flags: only `return_authorizations.has_single_approver=true`. Re-evaluation candidates: `return_authorizations.has_submit_lock=true` once `received` (RMA lines become immutable for refund computation); `order_allocations.has_submit_lock=true` once `released` (terminal); `sourcing_decisions.has_submit_lock=true` once `fulfilled` (audit trail immutable); `order_allocations.has_personal_content=true` IF shipping address is stored on the allocation row (depends on storage shape); `return_authorizations.has_personal_content=true` IF customer-return reason text is captured. Each flag requires positive consideration per Rule #12 (false-by-default is not the same as false-after-review). | Workflow-shape judgments and storage-shape questions only the user can answer for this catalog. | Per-flag yes/no, captured in Decisions. Do NOT auto-populate `data_objects.notes` (Rule #15). |
| B2-S3 | **B2 nit, `return_authorizations.plural_label='Return Authorization (RMA)s'`.** The mechanical pluralization of an inline acronym reads awkwardly. Common alternatives: `Return Authorizations` (drop the inline acronym; let aliases carry `RMA`), `RMAs` (drop the long-form, common in retail vendor docs), or `Return Orders` (more recent vendor-doc convention). | Naming preference, user's call. | (a) `Return Authorizations` + alias `RMA` (already on the B1-S6 list). (b) `RMAs` as the plural_label. (c) Leave as-is. |
| B2-S4 | **A2 / M2 capability-count threshold call.** B1-S1 module count depends on B1-S2 capability count. If A2 lands 5-8 capabilities, M2 requires >=2 modules; if <3 capabilities, M2 vacuously passes with 1 module. The OMS capability shape strongly suggests >=5 (order capture, sourcing, ATP, returns, BOPIS); the 4-module split in B1-S1 is well-aligned. Confirm the capability set and the module-count target. | The market reads as multi-capability and multi-module to the agent, but the user owns the marketing-vs-engineering boundary and may want a different split granularity for the catalog. | (a) Accept the 5-8 capability + 4-module proposal (default). (b) Smaller scope: 3 capabilities (order orchestration, returns, BOPIS) + 2 modules (OMS-CORE-ORCHESTRATION, OMS-RETURNS-AND-PICKUP). (c) Larger scope: 8+ capabilities including ATP and post-purchase tracking + 4-5 modules. |
| B2-S5 | **B12 config-shape exemption candidate for `inventory_locations` (425).** Zero lifecycle states. The master plausibly fits the "config-shape, no workflow" exemption (locations are authored once and edited inline by ops admins; the `opened` / `capacity_threshold_breached` events are signals on the location, not state transitions of the location itself). Per Rule #15 (RESCINDED prior license), the exemption cannot be auto-populated into `data_objects.notes`. Surface to user for explicit acknowledgement (gap report record); the audit conversation IS the persistence layer for this decision, not the column. | Workflow-shape decision; user owns whether to track the exemption in gap-report / PR / chat or to author a load-time `notes` string with their exact wording. | (a) Confirm config-shape, no states, no notes change. (b) Author 2-3 states (`active`, `decommissioned`) to capture the open/close cycle (low value). (c) User supplies an exact `data_objects.notes` string and the audit's fix-loop PATCHes it. |
| B2-S6 | **B5 broken-pointer dependency, INV-CORE-STOCK 61 holds `embedded_master + required` on `inventory_locations` (425) without a canonical DMDO master row anywhere.** The fix is OMS authoring its own DMDO master row on `inventory_locations` (425) inside an OMS-LOCATIONS module (or wherever B2-S4 lands the module split). Once B1-S1 + the DMDO master row ship, the INV embedded_master pointer becomes sound. Is the OMS canonical mastery the intent, or should `inventory_locations` migrate elsewhere (e.g. WMS once it lands as a domain, candidate #7 in the queue)? | Long-term canonical ownership decision; involves the candidate-domain queue. | (a) OMS canonically masters `inventory_locations` (recommended; OMS sources from inventory_locations and has its full workflow). (b) Defer mastery to WMS once WMS lands as a domain (Bucket 3 candidate; long lead time). (c) Re-classify as IWMS-canonical and have OMS hold `embedded_master` (unlikely; IWMS owns the abstract office/site concept, not the inventory-node concept). |

### Bucket 3, Phase 0 pending (speculative)

Market-audit Pass 2 ran the semantic enumeration against the 4 primary-coverage solutions already linked (ServiceNow Order Management for Industries, Salesforce Order Management, IBM Sterling Order Management, Oracle NetSuite) plus the documented vendor surface for Manhattan Active Omni, Fluent Commerce, Kibo, Aptos, OrderDynamics (Tecsys), and HotWax Commerce. The diff against the current OMS footprint surfaces 4 candidate entity gaps, 2 modularization candidates, and 1 regulation gap; full Phase 0 vendor-research vetting deferred until the user picks vet-vs-eyeball routing.

#### MISSING entity candidates surfaced by flagship-vendor knowledge

| Candidate entity | Vendor knowledge basis | Proposed module |
|---|---|---|
| `order_holds` | Manhattan Active Omni, Salesforce Order Management, IBM Sterling all ship a distinct `order_hold` master for payment-fraud holds, manual review holds, regulatory holds (export control), inventory holds. Currently the implicit-hold pattern is folded into `order_allocations.pending` state, which loses the hold-reason taxonomy and the SLA timers that vendors run on holds. | OMS-ORDER-ORCHESTRATION (proposed module from B1-S1) |
| `order_promises` (or `delivery_promises`) | Fluent Commerce, Salesforce Order Management explicitly model the promise as separate from the sourcing decision: the promise commits a delivery date / window to the customer; the sourcing decision is the operational plan to honor it. Today OMS folds both into `sourcing_decisions`, losing the customer-facing commitment record. | OMS-ORDER-ORCHESTRATION |
| `fulfillments` (already mastered by B2C-COMM 388, candidate WRONG-OWNERSHIP) | B2C-COMM masters `fulfillments` (388) and `commerce_shipments` (389). Vendor consensus in the OMS market is that fulfillment-instruction records belong to OMS (they're created by OMS sourcing, executed by WMS / store ops); commerce_shipments belong further downstream. The current B2C-COMM ownership is likely a pre-OMS-modular fallback. | If confirmed, MIGRATE `fulfillments` (388) to OMS-ORDER-ORCHESTRATION mastery; B2C-COMM becomes consumer. SCOPE-CREEP on B2C-COMM, WRONG-OWNERSHIP candidate. |
| `order_audit_events` | NetSuite, Salesforce Order Management, IBM Sterling all carry an `order_history` / `order_audit` log distinct from generic system audit; it captures every state transition with operator, reason, and supporting documents. PCI-DSS and customer-dispute workflows require this. | OMS-ORDER-ORCHESTRATION (with a candidate `has_submit_lock=true` flag once written) |

#### Compliance regulation candidates (3 already loaded: PSD2, PCI-DSS, EU VAT)

| Candidate regulation | Why | Coverage in vendors |
|---|---|---|
| GDPR (already loaded catalog-wide, missing `domain_regulations` row for OMS) | Customer PII on orders (shipping address, contact, payment surrogate). | All major OMS vendors. |
| CCPA / CPRA | California Consumer Privacy Act applies to order data for California consumers. | US-focused vendors call it out explicitly. |
| Section 321 / Type 86 (US de minimis cross-border) | DTC cross-border orders below USD 800 threshold, customs declarations. | International-shipping-aware OMS vendors (Fluent Commerce, ShipBob OMS). |
| EU Omnibus Directive | Refund timelines and consumer-rights surface on returns. | Returns-focused OMS vendors. |
| DAC7 | Marketplace seller reporting in EU; relevant for OMS instances backing marketplace operations. | Marketplace-aware vendors only. |

#### MODULARIZATION candidates

- **OMS-SOURCING-ENGINE split candidate.** If `order_promises` lands as a separate master from `sourcing_decisions`, a sub-module dedicated to the engine (rate-shop, carrier comparison, multi-node split optimization) becomes plausible. This would push the B1-S1 4-module proposal to 5 modules.
- **OMS-RETURNS hierarchy candidate.** Once `RETURNS-MGMT` lands as a domain (candidate queued from this audit), the OMS-RETURNS module may become a thinner OMS-side surface focused on the RMA / restock workflow, with the full returns experience (self-serve portal, exchange-first incentives, in-store drop-off network) hosting in RETURNS-MGMT. The split is reasonable to defer until RETURNS-MGMT is loaded.

#### Candidate-domain queue

This audit surfaced 4 domain-tier candidates (3 fresh, 1 bumped) for `audits/_missing-domains.md`:

- **RETURNS-MGMT** (Returns Management and Reverse Logistics Orchestration). Vendors: Loop Returns, Happy Returns, Narvar Return and Exchange, AfterShip Returns, ReturnGO, Returnly. Adjacent to OMS (the RMA workflow is the OMS-RETURNS contact surface) and B2C-COMM (the self-serve return portal is the buyer touchpoint), CSM (case-driven returns), ERP-FIN (refund accounting).
- **CARRIER-MGMT** (Multi-Carrier Shipping and Rate-Shop API). Vendors: ShipStation, ShipEngine, EasyPost, Shippo, Sendcloud, Stamps.com, ProShip, Logistyx, ShipperHQ. Adjacent to OMS (the sourcing-decision rate-shop calls out to a carrier-management API), B2C-COMM (checkout-time delivery options), INV-MGMT (fulfillment-time label printing).
- **POST-PURCHASE** (Post-Purchase Experience and Branded Order Tracking). Vendors: AfterShip, Narvar Track, Route, parcelLab, Wonderment, Malomo, Shop App tracking. Adjacent to OMS (the post-allocation status surface), B2C-COMM (branded tracking pages), CSM (delivery exception triage), CRM and MA (post-purchase upsell).
- **WMS** (Warehouse Management System, re-surfaced; mention_count now 2). Vendors: Manhattan WMS, Blue Yonder WMS, Korber One Warehouse, Softeon WMS, HighJump, Logiwa. Adjacent to OMS (the picking and packing layer that consumes OMS sourcing decisions), INV-MGMT, SCP.

Each entry added via `bun run scripts/analytics/append_missing_domain.ts` per Step 2 of the audit procedure; the queue file is the canonical record.

**Bucket 3 verification path:** vet via formal Phase 0 vendor research (produces a Phase 0 markdown at `c:/tmp/OMS-phase0-2026-05-30.md`) or eyeball-mode (user names which of the 4 entities + 5 regulations + 2 modularization candidates to treat as confirmed). The 4 domain candidates follow the standard queue triage flow at user review time. Bucket 3 is intentionally NOT enumerated as individual items in the audit count (per SKILL.md constraint #10 the Bucket 3 line is "any candidate listed including entities + modularization + regulations" but the user's standard fix-loop treats this Bucket 3 surface as a single "speculative, vet later" decision; the audit's `open_questions` count includes 0 for Bucket 3 because the count is set against B1 + B2 buckets with explicit per-row decisions, and the candidate-domain queue work is owned by the queue file itself, not by this audit's frontmatter counter).

### Cross-bucket dependencies

- B1-S2 (capabilities) and B1-S1 (modules) are coupled via B2-S4 (capability-count threshold drives module-count target). Resolve B2-S4 before authoring either. Once B2-S4 lands, B1-S1 and B1-S2 can load in a single phase-A loader.
- B1-S4 (intra-domain + cross-domain relationships) is partially coupled with B1-S1: relationships among OMS masters can land in legacy `domain_data_objects` shape, but the cross-domain edges into B2C-COMM (which are part of B1-S4) point at masters that B2C-COMM's audit work is owed to introduce as canonical DMDO masters. Surface as a dependency for the B2C-COMM audit in B1-S10.
- B1-S8 (B12 `domain_module_id` PATCH on `return_authorizations.approved`) is gated by B1-S1 module landing.
- B1-S9 (F7 fix on legacy skill 89) is independent of B1-S1, but B2-S1 (legacy skill DELETE timing) interacts: if B2-S1 picks option (a) (DELETE in the same load as per-module skills), B1-S9 becomes moot (the skill is gone). If B2-S1 picks (b), B1-S9 should run first.
- B2-S6 (mastery of `inventory_locations`) sits inside the B1-S1 module-design conversation: the chosen module name + scope for the `inventory_locations` master determines the DMDO row that resolves the INV broken pointer.
- Bucket 3 modularization candidates (OMS-SOURCING-ENGINE split, OMS-RETURNS depth) inform B2-S4 module-count target but the user can defer them as a "next-audit" question.

### Per-bucket prompts

- **Bucket 1:** "Fix these now? Reply 'all', 'just 1, 3, 5', or 'skip'." Note that B1-S1 + B1-S2 are gated on B2-S4 before the loader can start.
- **Bucket 2:** "What's your call on each of these (B2-S1 through B2-S6)? I'll wait for your decision per item before acting. B2-S5 is the only one where I'd need exact wording from you if you pick option (c)."
- **Bucket 3:** "Vet via Phase 0 research or eyeball-mode? If eyeball, name which of the 4 candidate entities, 5 regulation gaps, and 2 modularization candidates to treat as confirmed. The 4 candidate-domain queue entries (RETURNS-MGMT, CARRIER-MGMT, POST-PURCHASE, WMS) follow their own triage flow at queue-review time."

### Report-only follow-ups (owed by other domains)

These items are NOT in-scope for OMS's fix-load but are surfaced so the user can schedule audits on the named source domains:

- **B2C-COMM B5 + B9:** `commerce_orders` (381) and `order_lines` (382) are consumed by multiple domains (OMS via legacy rollup, INV-MGMT, CRM) but have NO `role='master'` DMDO row catalog-wide. B2C-COMM B5 audit work; same broken-pointer shape as the INV-MGMT case. Also `fulfillments` (388) and `commerce_shipments` (389) carry `master` in `domain_data_objects` rollup for B2C-COMM (id 71) but with the OMS scoping question raised in Bucket 3, the canonical mastery may belong on OMS for `fulfillments`. B2C-COMM also owes source_module_id on handoffs 322 + 502 once it modularizes; and target_module_id on 6 OMS-outbound rows (982, 984, 986, 990, 993, plus 1 already pointing at CRM not B2C-COMM, recheck).
- **ERP-FIN B10b:** target_module_id NULL on 5 OMS-outbound rows (981, 983, 988, 991, 995). Schedule ERP-FIN b1.
- **CSM B10b:** target_module_id NULL on 3 OMS-outbound rows (985, 987, 992). Schedule CSM b1.
- **CRM B10b:** 2 outbound rows already have target_module_id=46 (CRM-ACCT-MGT) but source_module_id NULL (own work). CRM has no additional B10b owed by OMS.
- **INV-MGMT B5:** 2 broken-pointer DMDO rows (INV-CORE-STOCK 61 `embedded_master + required` on `inventory_locations` 425 with no master row catalog-wide; INV-REPLENISHMENT 62 `consumer + required` on `order_allocations` 424 with no master row). Resolved by OMS B1-S1 landing the master rows; no audit work owed on the INV side beyond a re-run of INV-MGMT's B5 to confirm.
