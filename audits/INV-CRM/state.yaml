# === SUPERSEDED 2026-06-06: per-domain-skill restoration ===
# The per-module `system` skill grain is RETIRED (plans/per-domain-skill-restoration.md).
# Any open item that says "author/split a per-module system skill", "one system skill per
# domain_modules row", "add/PATCH skill_tools", or "<module>_agent per module" is CANCELED.
# New model: tool requirements live on `domain_module_tools` (author tools onto modules); each
# domain has exactly ONE domain-grain `system` skill (domain_id set, domain_module_id null) that
# DERIVES its toolset; starters keep their own module-anchored skill; FULL modules carry no skill;
# cross-domain value streams use `process_tools`. `skill_tools` is dropped. Per-module tool
# re-authoring is tracked in audits/_modularization-backlog.md. Do NOT author per-module skills.
# === end supersession note ===
schema_version: 2
domain: INV-CRM
status: feedback_needed
next_action_by: user
last_audit: "2026-06-07"

b1a:
  - id: B1A-M1
    summary: Missing master data_subject_requests for GDPR personal-data handling on INV-CRM-RELATIONSHIP-GRAPH
    finding: INV-CRM holds personal data on founders, executives, and LPs across all 3 modules but has zero data_subject_requests master. GDPR Articles 15-22 (right of access, rectification, erasure, restriction, portability, objection) require a structured request log. Affinity and Attio both ship this entity in their VC templates. No equivalent entity exists anywhere in the INV-CRM footprint.
    action: Author master domain_owned data_object data_subject_requests under module 10 (INV-CRM-RELATIONSHIP-GRAPH). Populate has_personal_content=true. Add domain_module_data_objects row with role=master, necessity=required. Author lifecycle states (received, in_review, fulfilled, rejected) with requires_permission on the fulfilled / rejected transitions. Author 2-3 trigger_events (data_subject_request.received, data_subject_request.fulfilled). Classify entity_type=operational_workflow.
    note: "Net-new master authoring (data_object + DMDO + lifecycle + trigger_events + relationships). Research-grade entity design, not a mechanical backfill; carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [10]
    affected_entities: [data_subject_requests]
    vendor_evidence: [Affinity, Attio for VC]
  - id: B1A-M2
    summary: Missing master meeting_records under INV-CRM-RELATIONSHIP-GRAPH
    finding: Calendar-derived meeting / interaction log is the primitive underlying relationship_records (the synthesized strength score). All 4 flagship specialists (Affinity, DealCloud, 4Degrees, Attio for VC) expose this as a first-class master. Absence means relationship score has no auditable substrate.
    action: Author master domain_owned data_object meeting_records under module 10. Pattern flags has_personal_content=true. DMDO row role=master, necessity=required. Author data_object_relationships meeting_records <-> investor_contacts (many_to_many, association) and meeting_records -> relationship_records (composition or reference, source-side). Classify entity_type (operational_record likely).
    note: "Net-new master authoring; carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [10]
    affected_entities: [meeting_records]
    vendor_evidence: [Affinity, DealCloud, 4Degrees, Attio for VC]
  - id: B1A-M3
    summary: Missing master introduction_requests; capability INVCRM-INTRODUCTION-ROUTING has zero entity backing
    finding: Capability INVCRM-INTRODUCTION-ROUTING (id 490) is linked to module 10 but has no entity behind it. Affinity Paths, 4Degrees Intros, and DealCloud Intros all model a warm-introduction request as a first-class master with requestor, target, intermediary, status, and reciprocity tracking.
    action: Author master domain_owned data_object introduction_requests under module 10. has_personal_content=true. DMDO role=master necessity=required. Author lifecycle states (requested, routed, accepted, declined, completed) and trigger events for routing / completion. Link to investor_contacts via many participant references. Classify entity_type=operational_workflow.
    note: "Net-new master authoring; carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [10]
    affected_entities: [introduction_requests]
    vendor_evidence: [Affinity, 4Degrees, DealCloud]
  - id: B1A-M4
    summary: Missing master deal_team_assignments for per-deal role-scoped staffing on INV-CRM-DEAL-PIPELINE
    finding: Per-deal staffing (deal lead, sponsor, associate, observer) cannot be expressed via global RBAC because the assignment is deal-scoped, not module-scoped. DealCloud, Affinity Lists, and Attio all model this as a junction master between vc_deals and users. The ATS hiring-team pattern is the catalog precedent.
    action: Author master domain_owned data_object deal_team_assignments under module 9. DMDO role=master necessity=required. Author junction shape (vc_deal_id, user_id, deal_role enum). Add data_object_relationships entries linking deal_team_assignments -> vc_deals (composition) and deal_team_assignments -> users (reference, owner_side=target). Classify entity_type=junction.
    note: "Net-new master authoring (junction); carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [9]
    affected_entities: [deal_team_assignments]
    vendor_evidence: [DealCloud, Affinity, Attio for VC]
  - id: B1A-M5
    summary: Missing config master pipeline_stages on INV-CRM-DEAL-PIPELINE
    finding: Per-firm or per-fund stage configurations are user-configurable in all 4 flagship products. Current INV-CRM catalog hard-codes the stage taxonomy as vc_deals lifecycle states (sourced / in_diligence / ic_review / term_sheet / closed / declined). Vendors expose this as a tenant-configurable master.
    action: Author config-shaped master pipeline_stages under module 9. has_personal_content=false. DMDO role=master necessity=required. Classify entity_type=catalog (config-shape, no workflow states needed). Author trigger_events.pipeline_stage.created / .archived if desired. Link via data_object_relationships pipeline_stages -> vc_deals (one_to_many, owner_side=source).
    note: "Net-new master authoring (config/catalog); carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [9]
    affected_entities: [pipeline_stages]
    vendor_evidence: [Affinity, DealCloud, 4Degrees]
  - id: B1A-M6
    summary: Missing master co_investor_firms for syndication and conflict checks on INV-CRM-DEAL-PIPELINE
    finding: Competing and co-investing VC / PE firms encountered during diligence are tracked as a master in DealCloud Firms and Affinity Organizations. Used for syndication invitations, conflict checks, and round-by-round attribution.
    action: Author master domain_owned data_object co_investor_firms under module 9. DMDO role=master necessity=required. Author data_object_relationships co_investor_firms <-> vc_deals (many_to_many, association) and optionally co_investor_firms -> investor_contacts for firm-side primary contacts. Classify entity_type (operational_record or catalog).
    note: "Net-new master authoring; carried out of the 2026-06-07 mechanical execute batch."
    affected_modules: [9]
    affected_entities: [co_investor_firms]
    vendor_evidence: [DealCloud, Affinity]
  - id: B1A-S4-CONFIG-SHAPE
    summary: Optional directory lifecycle states for relationship_records (752) and investor_contacts (753)
    finding: "As of 2026-06-07 the two unambiguous workflow masters (investment_memos 751, lp_prospects 754) have full lifecycle states; vc_deals (750) already had 6. relationship_records (752) is now entity_type=computed and investor_contacts (753) is entity_type=operational_record, so neither REQUIRES lifecycle states (B12 passes structurally via Rule #12). The only open question is whether either should also get OPTIONAL minimal directory states (active / archived / merged)."
    action: "Resolve B2-CONFIG-SHAPE. If the user wants optional directory states on investor_contacts (and/or relationship_records), author active / archived / merged. Otherwise no action; the entity_type classification is the exemption of record."
    blocked_by:
      - {type: user_decision, ref: B2-CONFIG-SHAPE}
    affected_masters:
      - {data_object_id: 752, name: relationship_records, target_module_id: 10, workflow: config-shape (computed); optional states only}
      - {data_object_id: 753, name: investor_contacts, target_module_id: 10, workflow: directory record (operational_record); optional states only}
  - id: B1A-S6
    summary: E-band fails completely; zero roles, zero role_modules, zero role_permissions across all 3 modules
    finding: A 3-module, 7-capability domain needs the role / persona layer. Path A (roles -> role_modules -> domain_modules.domain_id) and Path B (roles -> role_permissions -> permissions.domain_module_id -> domain_modules.domain_id) are both empty. Tier-rollup permissions on modules 9, 10, 11 are also empty.
    finding_note: "Largely subsumed by B1A-PHASE-P under the post-Plan-3 persona model; the old 4-role naming proposal is the legacy framing. The persona layer (domain_roles + role_modules + process_raci) is the canonical fix. Gated on B2-ROLE-NAMING and on the Phase-P persona authoring decision (DEFERRED)."
    blocked_by:
      - {type: user_decision, ref: B2-ROLE-NAMING}
      - {type: depends_on, ref: B1A-PHASE-P, reason: persona model supersedes the standalone role proposal}
    affected_modules: [9, 10, 11]
  - id: B1A-S7
    summary: "RETIRED (superseded 2026-06-06): per-module system skills + skill_tools across all 3 modules"
    finding: "Original item asked to author 3 per-module system skills (one per domain_modules row) with skill_tools. The per-module system-skill grain and skill_tools are RETIRED per the supersession header above (per-domain-skill-restoration). New model: tool requirements live on domain_module_tools; the domain carries exactly ONE domain-grain system skill that derives its toolset; FULL modules carry no skill. Per-module tool re-authoring is tracked in audits/_modularization-backlog.md."
    action: "Do NOT author per-module skills. Superseded; reframed as a note. The domain-grain skill + domain_module_tools authoring is a separate modularization-backlog concern, not this domain's audit obligation."
    superseded: "2026-06-06 per-domain-skill-restoration"
    affected_modules: [9, 10, 11]
  - id: B1A-PHASE-P
    summary: "Multi-module (3 modules) with 0 personas post-Plan-3 (E1 fail): author the persona / RACI layer."
    finding: "Live triage 2026-06-02: 3 domain_modules and 0 domain_roles reaching this domain via role_modules. Plan 3 deleted the old _core personas, so E1 fires on this multi-module domain."
    action: "Author domain_roles (operational personas) function-anchored per references/roles.md section 7, wire role_modules reach across the 3 modules, add process_raci responsibility rows, and wire data_object_lifecycle_states.process_id. ATS is the worked reference (scripts/loaders/load_ats_personas_pilot.ts)."
    status_note: "DEFERRED per the 2026-06-07 run instructions (personas are not auto-authored). Candidate personas: Deal Partner (deal lead, modules 9+10), Investment Associate (diligence support, modules 9+10), IR Lead (LP fundraising, module 11), Platform Admin (cross-module). Function anchor: Investment Management (function 85, already the domain owner)."
    extra_phase_p_triage: "2026-06-02"

b1b:
  - id: B1B-B1
    summary: Zero intra-domain handoffs across the 3-module domain; trigger-event dependency now satisfied
    finding: B9b - 3 modules but zero intra-domain handoffs rows. Expected pairs (4) - RELATIONSHIP-GRAPH -> DEAL-PIPELINE on a warm-introduction signal, DEAL-PIPELINE -> RELATIONSHIP-GRAPH on deal-closure decay, RELATIONSHIP-GRAPH -> LP-CRM on relationship-signal-informs-LP outreach, DEAL-PIPELINE -> LP-CRM on closed-deal portfolio-awareness enrichment. Pass 4 reconciliation also identified a 5th candidate INV-CRM -> FUND-ADMIN on lp_prospect.committed (payload lp_commitments).
    blocked_by_resolved: "B1A-S3 trigger events landed 2026-06-07 (lp_prospect.committed, vc_deal.* events now exist), so the handoffs.trigger_event_id dependency is satisfied. Remaining blocker: the 5 handoffs are still only source-count-only; each needs an exact (source_module, target_module, data_object, trigger_event, integration_pattern, friction_level) tuple before insert. Not yet a fully-specified mechanical list."
    affected_handoffs:
      - {count: 5, scope_note: "4 intra-domain plus 1 cross-domain INV-CRM -> FUND-ADMIN on lp_prospect.committed", enumeration_status: source_count_only}
  - id: B1B-S8
    summary: GDPR / CCPA / SEC Investment Advisers Act not yet in /regulations and not yet linked; blocked on user scoping
    finding: CAN-SPAM was linked (regulation_id 59, applicability conditional) in the 2026-05-31 continuation. GDPR, CCPA, and SEC Investment Advisers Act (Rule 204-2) are not in the regulations catalog and require both base-row authoring and a scoping decision before any domain_regulations row lands.
    blocked_by:
      - {type: user_decision, ref: B2-REGULATION-SCOPE}
      - {type: catalog_addition, scope: domains, trigger: regulations base rows GDPR / CCPA / SEC-IAA need authoring before domain_regulations links}
    affected_entities: [regulations, domain_regulations]

b2:
  - id: B2-CONFIG-SHAPE
    summary: Decide optional directory lifecycle states for relationship_records and investor_contacts
    question: "As of 2026-06-07, relationship_records (752) is entity_type=computed and investor_contacts (753) is entity_type=operational_record, so neither requires lifecycle states (Rule #12 exemption is in place at the typed-column layer). Should either ALSO get minimal directory state machines (active / archived / merged)?"
    options:
      - Confirm both as config-shape; no lifecycle states authored on either (current state).
      - Author minimal directory states (active / archived / merged) on investor_contacts only; relationship_records stays config-shape.
      - Author minimal directory states on both.
    why: The structural exemption is now recorded via entity_type (Rule #12 / #15). The remaining call is purely whether optional directory states add value; it is no longer a B12 failure.
  - id: B2-REGULATION-SCOPE
    summary: Decide which of GDPR / CCPA / SEC Investment Advisers Act / CAN-SPAM are in scope for INV-CRM
    question: CAN-SPAM is already linked (applicability conditional). Which of GDPR, CCPA, and SEC Investment Advisers Act (Rule 204-2) should be added to the regulations catalog and linked via domain_regulations? Scoping decision drives B1B-S8.
    options:
      - All four are in scope; author GDPR / CCPA / SEC IAA base rows then link all three.
      - Drop CAN-SPAM as out of scope; un-link it. Keep GDPR / CCPA / SEC IAA.
      - Drop firm-level privacy (GDPR / CCPA) as out of scope of the CRM product itself; keep CAN-SPAM and SEC IAA.
      - Keep only CAN-SPAM (current state).
  - id: B2-ROLE-NAMING
    summary: Approve role / persona naming convention before any persona loader runs
    question: Confirm or rename the 4-persona proposal - Deal Partner, Investment Associate, IR Lead, Platform Admin. Variants to consider - Partner vs Deal Partner (partner is the canonical VC title), a Principal tier between Partner and Associate, IR Partner for IR head.
    options:
      - Approve the 4-persona list as proposed.
      - Approve with the Partner rename (drop Deal prefix).
      - Add a Principal persona between Partner and Associate.
      - Provide a custom persona list.
    why: The persona / role layer is empty; naming is wide-open and gates B1A-S6 / B1A-PHASE-P.
  - id: B2-MODULARIZATION-STABILITY
    summary: Decide IDENTITY-RECON and PORTFOLIO-AWARENESS module splits
    question: (a) Should capability INVCRM-IDENTITY-RESOLUTION (linked to module 10) split into its own module INV-CRM-IDENTITY-RECON? (b) Should portfolio-awareness on the LP-CRM side (which embeds portfolio_companies from PORT-MONIT) split off as INV-CRM-PORTFOLIO-AWARENESS?
    options:
      - Defer both splits (current 3-module structure stays).
      - Split IDENTITY-RECON only.
      - Split PORTFOLIO-AWARENESS only.
      - Split both.
    why: Bucket 3 #7 (B3-FUNDS) may grow LP-CRM scope materially; that vote informs (b).
  - id: B2-PAIRWISE-AUDIT-SCHEDULE
    summary: Decide whether to schedule FUND-ADMIN (weight 3) and PORT-MONIT (weight 2) audits as separate Validate runs
    question: Edge-weight table - FUND-ADMIN 3, PORT-MONIT 2, CAP-TABLE-LEDGER 0 (semantic adjacency only). FUND-ADMIN was Pass-4 inline; PORT-MONIT was light summary. Schedule full Validate audits?
    options:
      - Schedule both FUND-ADMIN and PORT-MONIT next.
      - Schedule FUND-ADMIN only (PORT-MONIT defers).
      - Defer both.

b3:
  - id: B3-EMAIL-THREADS
    candidate: email_threads
    rationale: Universal inbox-sync primitive; Affinity, Attio, and 4Degrees all expose thread-level records. relationship_records is the synthesized strength score; email_threads is plausibly the load-bearing substrate for explainability. Could alternatively be config-shape if the design is store-derived-signals-only.
    vendor_evidence: [Affinity, Attio for VC, 4Degrees]
    proposed_module: INV-CRM-RELATIONSHIP-GRAPH
  - id: B3-NOTES
    candidate: crm_notes
    rationale: Free-text per-entity master across all 4 specialists. Naming collision risk with the notes column convention; load as crm_notes or relationship_notes per Rule #9 naming arbitration.
    vendor_evidence: [Affinity, DealCloud, 4Degrees, Attio for VC]
    proposed_module: INV-CRM-DEAL-PIPELINE or INV-CRM-RELATIONSHIP-GRAPH
  - id: B3-SIGNAL-ALERTS
    candidate: signal_alerts
    rationale: Vendor-specific shape - Affinity calls these alerts, others signals. Inbound signal layer feeds the relationship graph (funding rounds, hiring velocity, leadership changes). Could be folded into a future DEAL-FLOW-SIGNAL domain instead.
    vendor_evidence: [Affinity, 4Degrees]
    proposed_module: INV-CRM-RELATIONSHIP-GRAPH
  - id: B3-MANDATES
    candidate: mandates
    rationale: DealCloud-specific master (LP allocation mandates, sector mandates). Less universal across other 3 specialists. If loaded, may need its own module MANDATES or fold into LP-CRM.
    vendor_evidence: [DealCloud]
    proposed_module: INV-CRM-LP-CRM or new MANDATES module
  - id: B3-DOCUMENTS
    candidate: documents
    rationale: Universal but heavily overlapped with DLP / DMS. Whether INV-CRM masters or just consumes is a scoping call vs the platform document layer.
    vendor_evidence: [Affinity, DealCloud, 4Degrees, Attio for VC]
    proposed_module: INV-CRM-DEAL-PIPELINE
  - id: B3-ACTIVITY-LOGS
    candidate: activity_logs
    rationale: Universal audit trail master across all 4 vendors. May fold into platform-level activity logging instead of being mastered per-domain.
    vendor_evidence: [Affinity, DealCloud, 4Degrees, Attio for VC]
    proposed_module: INV-CRM-DEAL-PIPELINE
  - id: B3-FUNDS
    candidate: funds
    rationale: FUND-ADMIN canonically masters funds. Question is whether INV-CRM-LP-CRM should hold a consumer or embedded_master row so deal-team-side LP-CRM can scope by fund without requiring the full FUND-ADMIN install. Outcome affects Bucket-2 #5 modularization stability call.
    vendor_evidence: [DealCloud, Juniper Square, Allvue Platform]
    proposed_module: INV-CRM-LP-CRM
