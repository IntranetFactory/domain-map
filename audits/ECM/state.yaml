# === SUPERSEDED 2026-06-06: per-domain-skill restoration ===
# The per-module `system` skill grain is RETIRED (plans/per-domain-skill-restoration.md).
# Any open item that says "author/split a per-module system skill", "one system skill per
# domain_modules row", "add/PATCH skill_tools", or "<module>_agent per module" is CANCELED.
# New model: tool requirements live on `domain_module_tools` (author tools onto modules); each
# domain has exactly ONE domain-grain `system` skill (domain_id set, domain_module_id null) that
# DERIVES its toolset; starters keep their own module-anchored skill; FULL modules carry no skill;
# cross-domain value streams use `process_tools`. `skill_tools` is dropped. Per-module tool
# re-authoring is tracked in audits/_modularization-backlog.md. Do NOT author per-module skills.
# === end supersession note ===
schema_version: 2
domain: ECM
status: feedback_needed
next_action_by: user
last_audit: "2026-06-13"

# b1a fully cleared. See history.md for the 2026-06-06 and 2026-06-13 execution notes.
# 2026-06-13 agent pass cleared: B1A-B9D-VERIFY (B9d run both directions via b9d_resolver),
#   B1B-L1/L2/L5 (lifecycle states on 429/430/433), B1B-B1 (8 outbound source FKs),
#   B1B-F1 (moot: per-module-skill grain retired; id 54 is the correct domain-grain skill),
#   B2-CATALOG-UX (A4 written straight per Rule #20), B13 (entity_type on all 5 masters).
b1a: []

b1b:
  - id: B1B-B3
    summary: "Backfill `target_domain_module_id` on 10 inbound ECM handoffs now that modules exist."
    finding: >-
      All 10 inbound ECM handoffs (732, 735, 911, 912, 915, 919, 828, 829, 1028, 1031) have
      `target_domain_module_id=null`. The target module is the ECM module that holds the
      payload `data_object_id` with the strongest role. Most payloads resolve against
      `content_documents` (ECM-REPOSITORY, 242); retention / records context resolves against
      ECM-RECORDS-GOV (243). Out of scope for the modules-only pass; now unblocked.
    action: >-
      Each inbound payload is a FOREIGN-domain master that no ECM module declares any
      `domain_module_data_objects` role on (verified live 2026-06-13: zero ECM DMDO rows for
      data_objects 533/535/635/636/633/738/570/564/638). This is the B10b "No candidate /
      no-role row" sub-case: the FK cannot be backfilled mechanically. To resolve, the user
      decides per payload whether to (a) load a `consumer` DMDO row on the receiving ECM module
      (ECM-REPOSITORY 242 for document/content payloads; ECM-RECORDS-GOV 243 for legal-hold /
      records context), capturing the dependency in the catalog, or (b) accept the handoff as a
      domain-level signal with no module owner. Then PATCH `target_domain_module_id`.
    blocked_by:
      - {type: user_decision, ref: "per-payload consume-vs-domain-signal", reason: "no ECM module models any of the 10 inbound payloads; backfill needs a consumer-DMDO decision per payload, not a mechanical PATCH"}
    affected_handoffs:
      - {handoff_id: 732, direction: inbound, source_domain: IDP, target_domain: ECM, trigger_event: "extracted_record.completed", payload: extracted_records, null_field: target_domain_module_id}
      - {handoff_id: 735, direction: inbound, source_domain: IDP, target_domain: ECM, trigger_event: "document_classification_result.uncertain", payload: document_classification_results, null_field: target_domain_module_id}
      - {handoff_id: 911, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_hold.issued", payload: legal_holds, null_field: target_domain_module_id}
      - {handoff_id: 912, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "ediscovery_request.created", payload: ediscovery_requests, null_field: target_domain_module_id}
      - {handoff_id: 915, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_matter.opened", payload: in_house_legal_matters, null_field: target_domain_module_id}
      - {handoff_id: 919, direction: inbound, source_domain: LEGAL-PRACT-MGMT, target_domain: ECM, trigger_event: "court_filing.submitted", payload: external_court_filings, null_field: target_domain_module_id}
      - {handoff_id: 828, direction: inbound, source_domain: WSC, target_domain: ECM, trigger_event: "chat_attachment.shared", payload: chat_message_attachments, null_field: target_domain_module_id}
      - {handoff_id: 829, direction: inbound, source_domain: WSC, target_domain: ECM, trigger_event: "chat_channel.archived", payload: chat_channels, null_field: target_domain_module_id}
      - {handoff_id: 1028, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "in_house_legal_matter.opened", payload: in_house_legal_matters, null_field: target_domain_module_id}
      - {handoff_id: 1031, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_advice_record.issued", payload: legal_advice_records, null_field: target_domain_module_id}

  - id: B1B-B5
    summary: "Author 2 remaining intra-domain ECM retention handoffs (need net-new trigger_events first)."
    finding: >-
      Of the 3 surviving cross-module retention edges (records 243 -> repository 242), the
      retention-expiry -> content_documents handoff was authored 2026-06-13 (handoff 1439, on
      existing event 899 `document.retention_expired`). The other two have NO trigger event yet:
      `document_folder.retention_expired` (folders) and `retention_policy.applied_to_classification`
      (classification). classification -> repository dropped out as intra-module (CLASSIFY folded
      into ECM-REPOSITORY 242).
    action: >-
      Author the two missing `trigger_events` (folder retention-expiry on data_object 431;
      retention-policy-applied on 432), then INSERT the matching intra-domain `handoffs`
      (`source_domain_id=target_domain_id=91`, `integration_pattern='lifecycle_progression'`,
      `friction_level='low'`, source module 243 -> target module 242). Held back this pass
      because authoring net-new trigger-event vocabulary is a market-shape judgment surfaced as
      B2-INTRA-RETENTION-EVENTS, not a mechanical backfill.
    blocked_by:
      - {type: user_decision, ref: B2-INTRA-RETENTION-EVENTS, reason: "the two missing edges need net-new trigger_events (new event vocabulary = market-shape decision); surfaced for the user before authoring"}
    affected_handoffs:
      - {handoff_id: null, direction: intra-domain, source_domain: ECM, target_domain: ECM, trigger_event: "document_folder.retention_expired", payload: document_folders, null_field: null, scope_note: "records module 243 -> repo module 242 (folders); needs new trigger_event on 431"}
      - {handoff_id: null, direction: intra-domain, source_domain: ECM, target_domain: ECM, trigger_event: "retention_policy.applied_to_classification", payload: document_classifications, null_field: null, scope_note: "records module 243 -> repo module 242; needs new trigger_event on 432"}

  - id: B1B-S4
    summary: "Add the 6 missing flagship ECM solutions + `solution_domains` rows."
    finding: >-
      `/solution_domains?domain_id=eq.91` returns only 2 rows, both Microsoft, both at
      `coverage_level='secondary'`. Per A3 the floor is >=3 solutions and >=1 `primary`.
      Flagship ECM vendors OpenText Documentum, Hyland OnBase, M-Files, Box Content Cloud,
      Laserfiche, IBM FileNet, and Microsoft SharePoint Online are not yet linked.
    action: >-
      INSERT `vendors` (if missing) + `solutions` + `solution_domains` rows:
      OpenText Documentum (primary), Hyland OnBase (primary), M-Files (primary),
      Box Content Cloud (primary), Microsoft SharePoint Online (primary, separate from
      Syntex), Laserfiche (secondary), IBM FileNet (secondary). Apply Rule #18 to
      every description column.
    blocked_by:
      - {type: user_decision, ref: B2-VENDOR-NAMING, reason: "loader needs to confirm Rule #18 review of descriptions and any predecessor mentions"}
    affected_entities:
      - OpenText Documentum
      - Hyland OnBase
      - M-Files
      - Box Content Cloud
      - Microsoft SharePoint Online
      - Laserfiche
      - IBM FileNet

  - id: B1B-H1a
    summary: "Retag 5 LSD->ECM handoffs from legal-investigation PCFs to content cluster 8.4."
    finding: >-
      Handoffs 911, 912, 915, 1028, 1031 (LSD -> ECM) carry `agent_curated` rows pointing
      at PCFs 373 (Investigate legal aspects), 396 (Resolve disputes and litigations),
      373, 373, and 397 (Provide legal advice/counseling). These describe LSD-side
      activity, not ECM's receive-side content control. From ECM's perspective the
      activity is in cluster 8.4: 430 (Control delivered content), 429
      (Deliver approved content), or 1440 (Retain records).
    action: >-
      Once Bucket 2 #5 is decided, either DELETE + replace the 5 rows with
      911 -> 430, 912 -> 430, 915 -> 430, 1028 -> 430, 1031 -> 429, or record both
      classifications side by side via the composed `(handoff_id, process_id)` key.
    blocked_by:
      - {type: user_decision, ref: B2-LSD-RETAG}
    affected_handoffs:
      - {handoff_id: 911, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_hold.issued", payload: legal_holds, null_field: null}
      - {handoff_id: 912, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "ediscovery_request.created", payload: ediscovery_requests, null_field: null}
      - {handoff_id: 915, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_matter.opened", payload: in_house_legal_matters, null_field: null}
      - {handoff_id: 1028, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "in_house_legal_matter.opened", payload: in_house_legal_matters, null_field: null}
      - {handoff_id: 1031, direction: inbound, source_domain: LSD, target_domain: ECM, trigger_event: "legal_advice_record.issued", payload: legal_advice_records, null_field: null}

  - id: B1B-B6
    summary: "DELETE duplicate `data_object_relationships` 300 + 301; keep 587 + 588 as ECM source-side canonical."
    finding: >-
      Rows 300 (`chat_message_attachments captured_in content_documents`, owner_side=target)
      and 301 (`chat_channels archives_to content_documents`, owner_side=target) describe
      the same edges as rows 587 (`content_documents captures chat_message_attachments`,
      owner_side=target) and 588 (`document_folders archives chat_channels`,
      owner_side=target).
    action: >-
      Once the user confirms the direction in Bucket 2 #4, DELETE rows 300 and 301.
    blocked_by:
      - {type: user_decision, ref: B2-DEDUPE-WSC-RELATIONSHIPS}
    affected_entities: [data_object_relationships#300, data_object_relationships#301, data_object_relationships#587, data_object_relationships#588]

b2:
  - id: B2-INTRA-RETENTION-EVENTS
    summary: "Approve 2 net-new trigger_events for the remaining intra-domain retention handoffs (folders, classifications)."
    question: >-
      The retention-expiry -> content_documents intra-domain handoff was authored this pass
      (handoff 1439 on existing event 899). The two remaining cross-module retention edges
      (records 243 -> repository 242) need net-new `trigger_events` first:
      `document_folder.retention_expired` on data_object 431, and
      `retention_policy.applied_to_classification` on 432. Authoring new event vocabulary is a
      market-shape decision, so it is surfaced rather than executed unilaterally.
    options:
      - "Author both new events + their 2 intra-domain handoffs (recommended)"
      - "Author only the folder retention-expiry event + handoff"
      - "Leave both out for now"
    why: "New trigger-event names are net-new structure (Rule #21): surfaced in a q-file before any write. Gates B1B-B5."

  - id: B2-CONTENT-DOCUMENTS-RENAME
    summary: "Decide `content_documents` naming."
    question: >-
      The 4 other ECM masters use a 2-prefix pattern (`document_*` or `records_*`).
      `content_documents` (id 429) is the odd one. Bare `documents` is uncontested
      catalog-wide. Cross-domain blast radius: 27 `data_object_relationships` + 5
      handoffs reference id 429; all PATCHable by id.
    options:
      - "Keep `content_documents` as-is"
      - "Rename to bare `documents` with `is_canonical_bare_word=true` and rationale"
      - "Rename to `ecm_documents` for prefix consistency"
    why: "Naming arbitration belongs at insert/rename time, not later."

  - id: B2-DEDUPE-WSC-RELATIONSHIPS
    summary: "Confirm DELETE of `data_object_relationships` rows 300 and 301."
    question: >-
      Rows 300 and 301 (WSC-authored, owner_side=target) duplicate rows 587 and 588 from
      ECM's source-side authoring. Recommend DELETE 300 + 301; keep 587 + 588.
    options:
      - "DELETE 300 + 301 (recommended)"
      - "DELETE 587 + 588 instead"
      - "Keep all four (treat as separate edges)"
      - "Defer"
    why: "Both pairs describe the same two edges with verbs flipped; the WSC pair (300/301) labels the source as target which is structurally wrong."

  - id: B2-LSD-RETAG
    summary: "Decide LSD->ECM H1 retag (5 handoffs)."
    question: >-
      Handoffs 911, 912, 915, 1028, 1031 (LSD -> ECM) carry `agent_curated` rows pointing at
      LSD-side legal PCFs. From ECM's receive-side perspective the activity is in cluster 8.4
      (430 Control delivered content, 429 Deliver approved content, 1440 Retain records).
    options:
      - "DELETE + replace with content-cluster PCFs (911->430, 912->430, 915->430, 1028->430, 1031->429)"
      - "Keep the LSD-side classification (treat as the implementing process for the inbound)"
      - "Record both via separate (handoff_id, process_id) rows"
      - "Defer to LSD's next audit"
    why: "Per Rule #1 prior `agent_curated` rows are not overwritten silently. The composed key permits coexistence if the user wants both."

  - id: B2-PATTERN-FLAGS
    summary: "Re-evaluate B4 pattern flags on the 5 masters."
    question: >-
      All three pattern flags default `false` on every ECM master. Candidates:
      `content_documents.has_personal_content=true` (HR / health / contract content),
      `records_retention_policies.has_single_approver=true` (records officer sign-off),
      `document_classifications.has_submit_lock=true` (label assignment locks the row).
      Confirm or decline each. A `has_submit_lock` flip on `document_classifications` implies a
      2-state lifecycle (`draft` -> `locked`).
    options:
      - "Flip all three candidates true"
      - "Flip only the personal_content + single_approver pair"
      - "Flip only personal_content"
      - "Confirm all five masters at false (record decision in audit narrative)"
      - "Custom selection"
    why: "Affects `document_classifications` lifecycle and the workflow-gate permission derivation later."

  - id: B2-VENDOR-NAMING
    summary: "Confirm Rule #18 review on the 7 new ECM solutions before they load."
    question: >-
      B1B-S4 proposes INSERTing OpenText Documentum, Hyland OnBase, M-Files, Box
      Content Cloud, Microsoft SharePoint Online, Laserfiche, IBM FileNet. Per Rule
      #18 vendor and product names are only allowed in `vendors`, `solutions`,
      `data_object_aliases`, `tool_solutions`. Descriptions on solutions must
      characterise the product, not narrate the vendor landscape.
    options:
      - "Approve loader to draft descriptions and surface each one for per-row approval"
      - "User supplies all 7 descriptions"
      - "Drop one or more vendors from the load"
      - "Defer"
    why: "Rule #18 is non-trivial in practice; the agent does not bulk-write vendor descriptions without surfacing each."

  - id: B2-FOLDER-EXEMPTION
    summary: "Acknowledge config-shape exemption for `document_folders`."
    question: >-
      `document_folders` (id 431, mastered by ECM-REPOSITORY) reads as a config-shape master
      (folders are created, renamed, occasionally archived; no per-row workflow). Per Rule #12
      this is exempt from mandatory lifecycle states. Per Rule #15 the exemption cannot be
      annotated in `data_objects.notes`; it lives in the audit narrative only.
    options:
      - "Confirm config-shape exemption (no lifecycle states)"
      - "Author a 2-state lifecycle (active -> archived)"
      - "Defer to a later audit"
    why: "B4 + Rule #12: exemption must be positively confirmed each audit."

  - id: B2-CLASSIFICATION-EXEMPTION
    summary: "Acknowledge config-shape exemption (or 2-state) for `document_classifications`."
    question: >-
      `document_classifications` (id 432, mastered by ECM-REPOSITORY) reads as a
      classification-taxonomy master with no per-row workflow (labels exist, get applied,
      occasionally retired). Per Rule #12 the config-shape exemption may apply, but a
      `has_submit_lock=true` flip in B2-PATTERN-FLAGS would imply a 2-state lifecycle
      (`draft` -> `locked`).
    options:
      - "Confirm config-shape exemption (no lifecycle states)"
      - "Author a 2-state lifecycle (draft -> locked) if has_submit_lock flips true"
      - "Defer alongside B2-PATTERN-FLAGS"
    why: "Coupled to B2-PATTERN-FLAGS; the lifecycle shape depends on the flag decision."
  - id: B2-B9D-RETAG-821
    summary: "Re-point handoff 821 (ECM->DLP) tag from broad 13.6 \"Manage Content\" to specific 13.6.5 \"Develop and manage content\"."
    question: |
      Handoff 821 (ECM->DLP, payload content_documents) is tagged with the broad APQC process 13.6 "Manage Content". The more specific 13.6.5 "Develop and manage content" describes the same activity on the same entity and is realized work ECM already owns (ECM is the source side). Re-point the tag from 13.6 to 13.6.5?
    options:
      - "Re-point 821's handoff_processes tag 13.6 -> 13.6.5 (recommended)."
      - "Keep the broad 13.6 tag."
    why: |
      Re-pointing edits an existing handoff_processes row (destructive overwrite, Rule #21), so it needs sign-off. This supersedes the prior B2-B9D-OWN-83 "no owner" framing: the B9d resolver reclassified pid 83 as RE-TAG, not ORPHAN -- ECM owns 13.6 via handoff 821, the tag is merely coarse. Surfaced as q-ECM.md q9.
    extra_b9d_added: "2026-06-13"
  - id: B2-B9D-OWN-428
    summary: "Enterprise Content Management: name an owner for \"Develop and manage content\" (no one is responsible for it today)"
    question: |
      Data Loss Prevention forwards document classification to Enterprise Content Management to develop and manage content, but Enterprise Content Management does not yet have anyone assigned to develop and manage content, so this step has no owner. How should it be handled?
    options:
      - "Record it now as work Enterprise Content Management owns, and assign a named owner once Enterprise Content Management sets up who does this work."
      - "Leave it off the list for now."
    why: |
      Recording it now means that the moment Enterprise Content Management decides who does this work, this step already has a named owner instead of being rediscovered later as a gap. Surfaced as q-ECM.md q10.
    extra_b9d_added: "2026-06-13"
  - id: B2-B9D-OWN-429
    summary: "Enterprise Content Management: name an owner for \"Deliver approved content\" (no one is responsible for it today)"
    question: |
      Knowledge Management forwards document version to Enterprise Content Management to deliver approved content, but Enterprise Content Management does not yet have anyone assigned to deliver approved content, so this step has no owner. How should it be handled?
    options:
      - "Record it now as work Enterprise Content Management owns, and assign a named owner once Enterprise Content Management sets up who does this work."
      - "Leave it off the list for now."
    why: |
      Recording it now means that the moment Enterprise Content Management decides who does this work, this step already has a named owner instead of being rediscovered later as a gap. Surfaced as q-ECM.md q11.
    extra_b9d_added: "2026-06-13"
  - id: B2-B9D-OWN-430
    summary: "Enterprise Content Management: name an owner for \"Control delivered content\" (no one is responsible for it today)"
    question: |
      Identity Governance and Administration forwards document folder to Enterprise Content Management to control delivered content, but Enterprise Content Management does not yet have anyone assigned to control delivered content, so this step has no owner. How should it be handled?
    options:
      - "Record it now as work Enterprise Content Management owns, and assign a named owner once Enterprise Content Management sets up who does this work."
      - "Leave it off the list for now."
    why: |
      Recording it now means that the moment Enterprise Content Management decides who does this work, this step already has a named owner instead of being rediscovered later as a gap. Surfaced as q-ECM.md q12.
    extra_b9d_added: "2026-06-13"
  - id: B2-B9D-OWN-1440
    summary: "Enterprise Content Management: name an owner for \"Retain records\" (no one is responsible for it today)"
    question: |
      Audit Management forwards records retention policy to Enterprise Content Management to retain records, but Enterprise Content Management does not yet have anyone assigned to retain records, so this step has no owner. How should it be handled?
    options:
      - "Record it now as work Enterprise Content Management owns, and assign a named owner once Enterprise Content Management sets up who does this work."
      - "Leave it off the list for now."
    why: |
      Recording it now means that the moment Enterprise Content Management decides who does this work, this step already has a named owner instead of being rediscovered later as a gap. Surfaced as q-ECM.md q13.
    extra_b9d_added: "2026-06-13"

b3:
  - id: B3-DOC-WORKFLOW
    candidate: "document_workflow_definitions + document_workflow_instances"
    rationale: >-
      Universal entity pair across OpenText, Hyland, M-Files, Laserfiche, Box, FileNet. ECM-WORKFLOW
      (id 244) currently has NO master of its own (it consumes content_documents + document_versions);
      loading this pair gives it a real master and converts the consume-only module into a
      master-bearing one. Strongest missing-master candidate for the new module set.
    vendor_evidence: [OpenText Documentum, Hyland OnBase, M-Files, Laserfiche, Box Content Cloud, IBM FileNet]
    proposed_module: ECM-WORKFLOW

  - id: B3-DOC-APPROVALS
    candidate: document_approvals
    rationale: >-
      5 of 7 flagship vendors model approvals as a distinct entity. Pairs naturally with
      `document_workflow_instances` (the approval is a step in the workflow). Would also master in
      ECM-WORKFLOW.
    vendor_evidence: [OpenText Documentum, Hyland OnBase, M-Files, Box Content Cloud, IBM FileNet]
    proposed_module: ECM-WORKFLOW

  - id: B3-DOC-AUDIT-TRAIL
    candidate: document_audit_trail_entries
    rationale: >-
      Universal compliance-driven entity; supports the regulatory load already linked
      (SOX, GDPR, HIPAA, GLBA, FDA 21 CFR Part 11, eIDAS). Would master in ECM-RECORDS-GOV
      (which hosts the ECM-AUDIT capability) and back its audit-trail capability with a real entity.
    vendor_evidence: [OpenText Documentum, Hyland OnBase, M-Files, Box Content Cloud, IBM FileNet, Microsoft SharePoint Online]
    proposed_module: ECM-RECORDS-GOV

  - id: B3-METADATA-PROPS
    candidate: metadata_property_definitions
    rationale: >-
      Anchors the metadata-driven ECM model (M-Files's whole architecture). Universal at
      OpenText, Hyland, SharePoint as "managed properties" or "term store". Would master in
      ECM-REPOSITORY alongside content_documents and document_classifications.
    vendor_evidence: [M-Files, OpenText Documentum, Hyland OnBase, Microsoft SharePoint Online]
    proposed_module: ECM-REPOSITORY

  - id: B3-DOC-RENDITIONS
    candidate: document_renditions
    rationale: >-
      Universal across vendors (PDF, preview, thumbnail rendering). Vendor shape varies
      between separate entity and computed attribute. Would master in ECM-REPOSITORY.
    vendor_evidence: [OpenText Documentum, Hyland OnBase, M-Files, Box Content Cloud, IBM FileNet, Microsoft SharePoint Online]
    proposed_module: ECM-REPOSITORY
