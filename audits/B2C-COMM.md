---
status: feedback_needed
last_transition: 2026-05-30
last_transition_by: agent
open_questions: 22
---

# B2C-COMM, Audit History

## 2026-05-30, Validate b1 (full 4-pass)

### Summary

- **Current footprint:** **0 `domain_modules` rows** (M1 hard-fail; no full module, no starter, no cross-host). 8 capabilities (`COMM-CATALOG-MERCH`, `COMM-CART-CHECKOUT`, `COMM-STOREFRONT`, `COMM-ORDER-CAPTURE`, `COMM-PROMOTIONS`, `COMM-SEARCH-DISC`, `COMM-B2B-COMM`, `COMM-HEADLESS-API`). 10 mastered data_objects on `domain_data_objects` only (`commerce_orders` 381, `order_lines` 382, `carts` 383, `commerce_products` 384, `commerce_storefronts` 385, `checkouts` 386, `payment_transactions` 387, `fulfillments` 388, `commerce_shipments` 389, `coupons` 390); 5 consumers / contributors (`customers` 97, `customer_events` 111, `audience_segments` 113, `customer_subscriptions` 106, `customer_invoices` 107, `marketing_campaigns` 116, `crm_leads` 99). 9 solutions linked (8 primary: Salesforce Commerce Cloud, Shopify Plus, Adobe Commerce, BigCommerce, SAP Commerce Cloud, commercetools Composable Commerce, Oracle Commerce, VTEX Commerce Platform; 1 secondary: Oracle NetSuite). 10 regulations (GDPR, CPRA, PCI-DSS, EU Data Act, EU VAT Directive, EU DSA, COPPA, CAN-SPAM, ADA, Section 508). 25 trigger_events on the 10 masters. 17 outbound cross-domain handoffs (to CRM, SUB-MGMT, CDP, CSM, OMS, ERP-FIN, MA, LOYALTY). 16 inbound cross-domain handoffs (from OMS, CDP, INV-MGMT, HCMS, SUB-MGMT, LOYALTY, DXP, PIM). **0 intra-domain handoffs** (B9b vacuous: zero modules). **0 lifecycle states** across all 10 masters (B12 hard-fail). **0 `data_object_aliases`** across all 10 masters (B11 broad miss). **0 intra-domain `data_object_relationships` among the 10 masters** (B6 hard-fail). 1 legacy domain-level system skill (id 24, `b2c-comm-system`, `domain_module_id=NULL`) with 23 `skill_tools` rows. No roles (E1 vacuously passes under M1 fail). No permission bundles authored.

- **Vendor-surface basis (Pass 2 flagship enumeration):** Shopify Plus, BigCommerce, Salesforce Commerce Cloud B2C (Demandware), Adobe Commerce (Magento), commercetools, SAP Commerce Cloud (Hybris), Oracle Commerce Cloud, VTEX, Centra, Spryker, Elastic Path, Saleor, Vue Storefront / Frontastic, Nacelle, Shopware. Long tail / SMB: WooCommerce, Squarespace Commerce, Wix Stores, Ecwid, PrestaShop. Adjacency cluster (composable / headless / orchestration): Vue Storefront, commercetools, Saleor, Elastic Path, Shopware. Payments specialists (Stripe, Adyen, Braintree, Klarna, Affirm, Afterpay) and tax engines (Avalara, Vertex, Sovos) are vendor adjuncts to B2C-COMM but their primary categories live on payments / tax-compliance domains, not commerce-of-record.

- **Neighbor discovery (Pass 3, ranked by edge weight = outbound + inbound + DMDO touch points):**

| Neighbor | Outbound | Inbound | DMDO touch | Weight | Pass shape |
|---|---|---|---|---|---|
| OMS | 2 (commerce_order.placed, order_line.added) | 5 (sourcing_decision.computed, store_pickup_order.ready, inventory_location.capacity_threshold_breached, order_allocation.created, order_allocation.reassigned) | none | 7 | Pairwise (full) |
| CRM | 3 (customer.signed_up, commerce_order.placed, payment.declined) | 0 | `customers` contributor, `crm_leads` contributor | 5 | Pairwise (full) |
| CDP | 2 (storefront.interaction, commerce_product.published) | 3 (segment.activated, customer_journey.stage_entered, customer_attributes.refreshed) | none (audience_segments consumer) | 6 | Pairwise (full) |
| MA | 3 (cart.abandoned, checkout.started, coupon.redeemed) | 0 | `marketing_campaigns` consumer | 4 | Pairwise (full) |
| ERP-FIN | 3 (commerce_order.refunded, commerce_order.confirmed, fulfillment.delivered) | 0 | none | 3 | Pairwise (full) |
| LOYALTY | 3 (commerce_order.placed, fulfillment.delivered) | 1 (redemption_transaction.completed) | none | 4 | Pairwise (full) |
| SUB-MGMT | 2 (order.subscription_purchase, commerce_order.placed) | 1 (customer_invoice.issued) | `customer_subscriptions` consumer, `customer_invoices` consumer | 5 | Pairwise (full) |
| CSM | 1 (commerce_order.completed) | 0 | none | 1 | Lightweight |
| HCMS | 0 | 2 (content_entry.published, content_release.published) | none | 2 | Lightweight |
| INV-MGMT | 0 | 1 (inv_stock_item.created) | none | 1 | Lightweight |
| DXP | 0 | 1 (personalization_rule.activated) | none | 1 | Lightweight |
| PIM | 0 | 2 (pim_product.published, pim_product.discontinued) | none | 2 | Lightweight |

- **Structural pass bands:** A1 partial-fail (em-dash in `business_logic`, see B1-S1). A2 / A3 pass. **M1 hard-fail** (zero modules, blocks M2-M7 cascade). **M-band cascade:** M2-M7 all vacuously fail or pass only because there are no modules to check; capability orphan-coverage (M4 / M6) is structurally indeterminate. **B-band:** B1 passes (10 masters); **B2 pass** (all 10 masters carry both labels); **B3 partial-fail** (4 bare-word claims carry `is_canonical_bare_word=true` with rationale: `carts`, `checkouts`, `fulfillments`, `coupons`; the other 6 are prefixed and pass automatically; **`payment_transactions` and `commerce_shipments` are not bare-word, no action; `commerce_orders` / `commerce_products` / `commerce_storefronts` / `order_lines` use a domain prefix or compound noun and pass**); **B4 partial-fail** (every master has `has_personal_content=false` and `has_submit_lock=false` and `has_single_approver=false`; `payment_transactions` and `commerce_orders` and `checkouts` should plausibly carry `has_personal_content=true` per PCI / PII content); **B5 N/A** (no embedded_masters); **B6 hard-fail** (zero `data_object_relationships` among the 10 masters, despite an obvious chain `carts → checkouts → commerce_orders → order_lines / payment_transactions / fulfillments → commerce_shipments`, and `commerce_storefronts → commerce_products`, and `coupons → checkouts / commerce_orders`); **B7 hard-fail** (zero `users` edges on any master, despite `commerce_orders.customer`, `carts.shopper`, `commerce_storefronts.merchandiser`, etc.); **B8 partial-pass** (a few cross-domain edges exist on shared masters via foreign domains, but no outbound `commerce_orders ↔ <target_master>` edges authored from B2C-COMM); **B9 partial-fail** (8 `trigger_events` carry empty `event_category` per Rule #13 enum: 506 `order_line.added`, 507 `commerce_product.published`, 508 `commerce_product.discontinued`, 509 `commerce_product.price_changed`, 510 `fulfillment.created`, 511 `fulfillment.shipped`, 512 `fulfillment.delivered`, 513 `coupon.issued`, 514 `coupon.redeemed`, total 9 rows; also two `commerce_storefront.created` 323 and `commerce_shipment.created` 321 carry `lifecycle` which is fine); **B9b vacuous** (M1 fail blocks intra-domain modularization); **B10 inbound report-only** (16 inbound; not B2C-COMM's fix); **B10b hard-fail on outbound** (all 17 outbound handoffs have `source_domain_module_id=NULL`; not derivable until M1 lands); **B10b hard-fail on inbound** (all 16 inbound handoffs have `target_domain_module_id=NULL`; same dependency); **B11 broad miss** (zero `data_object_aliases` across all 10 masters, despite vendor synonyms `commerce_products ↔ Items / SKUs / Catalog Items / Products`, `carts ↔ Baskets / Sessions`, `commerce_orders ↔ Sales Orders / Web Orders`, `commerce_storefronts ↔ Sites / Channels / Sales Channels`, `checkouts ↔ Checkout Sessions / Cart Sessions`, `coupons ↔ Promo Codes / Vouchers / Promotions`); **B12 hard-fail** (zero `data_object_lifecycle_states` across all 10 masters). **C1 pass** (Marketing owner, Sales contributor). **C2** unverified (no capability-level overrides loaded; `COMM-B2B-COMM` plausibly owned by Sales, not Marketing). **D1** UI spot-check deferred. **E1-E6 vacuous** (M1 cascade). **F1 fail** (legacy domain-level system skill id 24 with `domain_module_id=NULL`); **F2 fail** (no module-level system skill, since there are no modules); **F3 partial-pass** (skill 24 has 23 `skill_tools`); **F4 pass** (operation_kind invariants OK on inspected sample); **F5 uncomputable** (Semantius score per-module impossible under M1 fail); **F7 fail** (`send_email` linked as a `skill_tools` row on skill 24 with no workflow-specific justification, and `execute_payment` linked with notes "Payment capture is external (handled by a payment gateway)" which is fine but `send_email` should likely be `notify_person`). **H1 hard-fail** (catalog quality: 0 of 33 cross-domain handoffs `record_status='approved'`; 0 `agent_curated`; 7 tagged via `discovery_substring` (5) or `discovery_override` (2), all `record_status='new'`; 26 untagged). Process-health side-bar: 0 `agent_curated`.

- **Vendor-surface gap signal (Pass 2 cross-check):** the current 10-master footprint covers the order-capture spine (cart → checkout → order → payment → fulfillment → shipment) plus product / storefront / coupon. The flagship surface adds: **product variants / product_options** (every flagship platform has SKUs distinct from products: Shopify variants, BigCommerce variants, commercetools variants), **price_books / pricing_tiers** (commercetools, Adobe Commerce, B2B-friendly), **inventory_availability** (Shopify, BigCommerce; the `inventory-availability service` mentioned in `business_logic` is masterless), **tax_calculations / tax_rates**, **shipping_methods / shipping_zones / shipping_rates**, **payment_methods / payment_gateways**, **gift_cards** (distinct from coupons), **returns / RMAs / refunds_requests** (separate workflow from `commerce_order.refunded`), **product_reviews / product_ratings**, **wishlists / saved_carts**, **product_categories / collections** (catalog tree), **catalog_attributes** (the attribute schema beyond `commerce_products`), **storefront_themes / theme_versions**, **customer_addresses / shipping_addresses** (PII-bearing reference data), **B2B-specific:** `b2b_accounts / company_accounts / buyer_groups / quotes / requisition_lists / contracts`. These are all candidates for Bucket 3.

- **Bucket 1 (in-scope, agent fixable):** 12 items.
- **Bucket 2 (surface-for-user, judgment):** 6 items.
- **Bucket 3 (Phase 0 pending, speculative):** 4 items.
- **Candidates queued to `_missing-domains.md`:** 0 (every surfaced market is either an existing domain or a sub-feature of B2C-COMM proper).

### Vendor surface basis

Shopify Plus and BigCommerce represent the SaaS multi-tenant SMB-to-mid-market storefront platforms. Salesforce Commerce Cloud B2C (Demandware), Adobe Commerce (Magento), Oracle Commerce Cloud, and SAP Commerce Cloud (Hybris) anchor the enterprise / multi-brand suite cluster. commercetools, Saleor, Elastic Path, Centra, Spryker, and Shopware represent the composable / headless cluster (MACH-aligned). VTEX is the unified commerce platform with strong B2B + B2C coverage. Vue Storefront / Frontastic / Nacelle are storefront / orchestration layers, which gives them visibility into checkout and cart state objects without owning the order spine. WooCommerce, Squarespace Commerce, Wix Stores, Ecwid, and PrestaShop are the long-tail SMB layer. No compliance-specialist vendor is included as a flagship in B2C-COMM: regulatory pressure (PCI-DSS, GDPR, CPRA, ADA) is met by every flagship as a baseline; compliance specialists in commerce (CookiePro / OneTrust for consent, Stripe / Adyen for PCI-scope offloading) live on their own domains.

### Bucket 1, In-scope confirmed gaps

#### STRUCTURAL band failures

| ID | Band | Finding | Fix |
|---|---|---|---|
| B1-S1 | **A1, em-dash in `business_logic`** | `domains.business_logic` for B2C-COMM contains a U+2014 em-dash byte (between "service" and "the runtime"). CLAUDE.md forbids em-dashes everywhere in this project. | PATCH `/domains?id=eq.71` with `business_logic = "Cart, promotion engine, search/merchandising ranking, and inventory-availability service: the runtime an e-commerce site depends on."` (replace em-dash with a colon). |
| B1-S2 | **M1, zero `domain_modules`** | B2C-COMM has zero rows in `domain_modules` and zero rows in `domain_module_host_domains`. Rule #14 mandates every `domains` row has at least 1 `module_kind='full'` row, and domains with ≥3 capabilities (B2C-COMM has 8) require at least 2 full modules. Every M-band check downstream (M2-M7) is structurally indeterminate; B9b is vacuous; B10b is uncurable on outbound side; E-band is vacuous; F2-F5 are uncomputable. **This is the headline finding for B2C-COMM and blocks every other in-scope cascade.** | Hand-author the module set. Proposed split based on capability shape and flagship-vendor surface: (a) **`B2C-COMM-CATALOG-MERCH`** masters `commerce_products`, `commerce_storefronts`, and `coupons`; realizes `COMM-CATALOG-MERCH`, `COMM-STOREFRONT`, `COMM-SEARCH-DISC`, `COMM-PROMOTIONS`. (b) **`B2C-COMM-ORDER-CAPTURE`** masters `carts`, `checkouts`, `commerce_orders`, `order_lines`, `payment_transactions`; realizes `COMM-CART-CHECKOUT`, `COMM-ORDER-CAPTURE`, `COMM-HEADLESS-API`. (c) **`B2C-COMM-FULFILL-SHIP`** masters `fulfillments`, `commerce_shipments`; realizes the fulfill / ship slice of `COMM-ORDER-CAPTURE` (cross-cuts; can fold into module (b) if the user prefers a 2-module shape). (d) Optional: **`B2C-COMM-B2B`** as a starter / lite variant for `COMM-B2B-COMM` (B2B accounts, requisition lists, quotes), if Bucket 3 b2b candidates land. The 3-module split (a / b / c) is the recommended floor; the 4-module split adds B2B-COMM. |
| B1-S3 | **B6, zero intra-domain master-to-master relationships** | None of the 10 masters carry `data_object_relationships` edges to one another. The flagship surface implies a clear chain: `carts has_many order_lines` (pre-checkout), `carts becomes commerce_orders` (at checkout commit), `commerce_orders has_many order_lines`, `commerce_orders has_many payment_transactions`, `commerce_orders has_many fulfillments`, `fulfillments has_many commerce_shipments`, `commerce_storefronts has_many commerce_products`, `coupons applies_to commerce_orders / order_lines`, `checkouts converts_to commerce_orders`. Without these, the architect-pass for B2C-COMM has no skeleton. | Author 9-12 relationship edges via the cluster-drafts loader (see plan §9.1 Step 5). Owner_side for each is the carrier of the FK (e.g. `order_lines.commerce_order_id` puts owner_side on `commerce_orders`). |
| B1-S4 | **B7, zero `users` edges on every master** | No `data_object_relationships` row links any of the 10 masters to the platform-built-in `users` row. Yet every commerce master implies user actors: `commerce_orders.customer / shopper`, `carts.shopper`, `checkouts.shopper`, `commerce_storefronts.merchandiser / merchant_admin`, `commerce_products.product_owner`, `coupons.created_by`, `payment_transactions.cashier_or_customer`. | Author per-master user edges per Rule #10. Bundle into the same cluster-drafts load as B1-S3. |
| B1-S5 | **B9 missing event_category** | 9 `trigger_events` on B2C-COMM masters carry empty `event_category` (Rule #13 enum must be one of `lifecycle / state_change / threshold / signal`): 506 `order_line.added`, 507 `commerce_product.published`, 508 `commerce_product.discontinued`, 509 `commerce_product.price_changed`, 510 `fulfillment.created`, 511 `fulfillment.shipped`, 512 `fulfillment.delivered`, 513 `coupon.issued`, 514 `coupon.redeemed`. | PATCH each: 506 → `state_change` (line added to a parent cart / order). 507 / 510 → `lifecycle` (publication / creation are lifecycle stages). 508 → `state_change` (discontinue is a state transition). 509 → `state_change` (price change is a state transition). 511 / 512 → `state_change` (shipping / delivery are state transitions on `fulfillments`). 513 → `lifecycle` (coupon issuance is a lifecycle stage). 514 → `state_change` (coupon redemption is a state transition). |
| B1-S6 | **B11, broad alias miss across all 10 masters** | Zero `data_object_aliases` rows for any of the 10 masters, despite well-known vendor / industry synonyms (see Summary header). | Author alias rows: `commerce_products` (`Items`, `SKUs`, `Catalog Items`, `Products`), `carts` (`Baskets`, `Shopping Sessions`), `commerce_orders` (`Sales Orders`, `Web Orders`), `commerce_storefronts` (`Sites`, `Channels`, `Sales Channels`), `checkouts` (`Checkout Sessions`, `Cart Sessions`), `coupons` (`Promo Codes`, `Vouchers`, `Promotions`), `fulfillments` (`Pick Tickets`, `Allocations`), `commerce_shipments` (`Parcels`, `Packages`), `payment_transactions` (`Charges`, `Captures`, `Authorizations`), `order_lines` (`Line Items`, `Order Items`). Bundle into the cluster-drafts loader. |
| B1-S7 | **B12 hard-fail, no lifecycle states on any of 10 masters** | All 10 masters have zero `data_object_lifecycle_states` rows. The flagship surface has clear lifecycles on at least 7 of them: `carts` (active, abandoned, converted, expired), `checkouts` (started, address_entered, shipping_quoted, payment_authorized, completed, abandoned), `commerce_orders` (placed, confirmed, fulfilled, completed, refunded, cancelled), `payment_transactions` (authorized, captured, declined, refunded, voided), `fulfillments` (created, picked, packed, shipped, delivered, returned), `commerce_shipments` (created, in_transit, delivered, exception, returned), `commerce_products` (draft, published, discontinued, archived). `coupons` (issued, redeemed, expired, revoked). Config-shape exemption candidates: `order_lines`, `commerce_storefronts` (the storefront is config-heavy, may exempt via published / unpublished only). | Author state machines per the lifecycle-research workflow (Phase B). Pattern: initial state + workflow gates marked `requires_permission=true` + `permission_verb_override` for non-obvious verbs. Tie each state to the realizing module from B1-S2. Workflow-gate states become module-level permissions at materialization time. |
| B1-S8 | **B10b hard-fail, outbound `source_domain_module_id=NULL` on 17 outbound handoffs** | All 17 outbound handoffs (66, 67, 75, 322, 323, 324, 325, 326, 327, 328, 329, 330, 331, 502, 504, 505, 506) carry `source_domain_module_id=NULL`. This is downstream of B1-S2 (no modules exist to point at). | Cured automatically once B1-S2 lands and the per-master `domain_module_data_objects` rows place each master in the right module. Then run the backfill from [scripts/loaders/backfill_ats_handoff_modules_2026_05_23.ts](../../scripts/loaders/backfill_ats_handoff_modules_2026_05_23.ts) adapted for B2C-COMM. |
| B1-S9 | **B10b hard-fail, inbound `target_domain_module_id=NULL` on 16 inbound handoffs** | All 16 inbound handoffs carry `target_domain_module_id=NULL`. Same dependency on B1-S2. | Cured after B1-S2 + the inbound-side backfill. Payload-to-module map: `customers` (97) → catalog/orders depending on the receiving module's role (likely B2C-COMM-ORDER-CAPTURE consumer); `commerce_products` (384) → B2C-COMM-CATALOG-MERCH; `content_entries` / `content_releases` / `personalization_rules` / `customer_journeys` / `customer_attributes` / `redemption_transactions` / `inv_stock_item` / `customer_invoices` / `sourcing_decisions` / `store_pickup_orders` / `order_allocations` / `inventory_locations` map to whichever module declares the consumer DMDO row. |
| B1-S10 | **F1, legacy domain-level system skill `b2c-comm-system` (id 24, `domain_module_id=NULL`)** | F1 requires retiring legacy `domain_id`-only system skills once any module-level skill exists for the domain. Since no module-level skill exists yet (M1 cascade), this stays as the transitional state until B1-S2 lands. Once modules land, this row must be retired and replaced with one `system` skill per module (Rule #17). The 23 existing `skill_tools` rows partition naturally across the 3 proposed modules. | Sequenced after B1-S2. DELETE row 24 once `B2C-COMM-CATALOG-MERCH-AGENT`, `B2C-COMM-ORDER-CAPTURE-AGENT`, and `B2C-COMM-FULFILL-SHIP-AGENT` system skills are loaded and their `skill_tools` rows redistributed. Partition: catalog-merch agent gets `query_products`, `query_storefronts`, `query_coupons`, `create_product`, `update_product`, `create_coupon`; order-capture agent gets `query_orders`, `query_order_lines`, `query_carts`, `query_checkouts`, `query_payment_transactions`, `query_customers`, `query_customer_subscriptions`, `create_order`, `update_order_status`, `refund_order`, `create_cart`, `create_checkout`, `execute_payment`; fulfill-ship agent gets `query_fulfillments`, `query_shipments`, `create_shipment`. |
| B1-S11 | **F7, `send_email` linked without workflow-specific justification** | `skill_tools` row on skill 24 links `send_email` (tool id 37, `side_effect`) but the channel-justification `notes` column does not capture a workflow rationale that proves email cannot be substituted by `notify_person`. The Channel-vs-capability rule defaults generic notifications (`order confirmations`, `shipment notifications`, `cart-abandonment cascades`, all listed in the current notes) to `notify_person`. Two of the four populated notes on skill_tools rows (id 24's rows for `send_email` and `query_customers`) carry justification text that should also be re-checked under Rule #15. | After B1-S2 / B1-S10, when re-creating skill_tools rows under the new module skills, link `notify_person` instead of `send_email` for the generic notification surface. If a specific workflow (e.g. PCI-DSS-mandated transactional payment confirmation in a particular jurisdiction) genuinely requires email, keep `send_email` and propose specific `notes` wording to the user for per-row approval. |
| B1-S12 | **Rule #15 pollution check on `skill_tools.notes`** | 4 of 23 `skill_tools` rows on skill 24 carry populated `notes` text: (`send_email`) "Order confirmations, shipment notifications, cart-abandonment cascades", (`query_customers`) "Cross-reads CRM/CDP customer master", (`query_customer_subscriptions`) "Replenish-style recurring-purchase coordination", (`execute_payment`) "Payment capture is external (handled by a payment gateway)". Per Rule #15 every `notes` column is empty by default and may only be populated with explicit per-row user approval. Were these user-approved at load time or auto-populated? | Surface as B2-S2 (cannot resolve without user input). Default on auto-populated: PATCH all 4 rows' `notes` to empty string and append a Rule #15 incident to `references/skill-changelog.md`. The current text is exactly the "channel-justification prose on skill_tools rows" pattern called out as a forbidden Rule #15 case. |

#### B1-H1 APQC TAGGING (proposed candidates)

H1 is a single Bucket 1 item even though it proposes ~22 row inserts (covering the 26 untagged + targeted replacements for the coarse `discovery_override`-tagged rows). Counts as one B1 row in the bucket summary.

**Strategy.** Most of the existing tags (`discovery_substring` and `discovery_override`) point at L1 / L2 nodes that are correct ancestors but too coarse. The audit proposes tighter L3 / L4 children where the cross-industry PCF carries them. Where the cross-industry PCF has no match (e.g. cart-abandonment as a marketing-trigger primitive, or storefront-personalization signals), defer to Discover Pass 3.

**Outbound (B2C-COMM-published, target = downstream domain):**

| handoff_id | source → target | trigger_event | payload | Proposed PCF (search hint) | Confidence |
|---|---|---|---|---|---|
| 66 | B2C-COMM → CRM | customer.signed_up | customers | Establish customer relationships (10141) or sub-process of 10101 Develop and manage customer relationships | confident L3 |
| 67 | B2C-COMM → SUB-MGMT | order.subscription_purchase | customer_subscriptions | Manage sales orders (10185) child: Process customer orders (10186) | confident L4 |
| 75 | B2C-COMM → CDP | storefront.interaction | customer_events | Capture customer information (already covered; consider Manage customer master data 11216) | defer (no clean PCF match: storefront-interaction is a digital-experience signal) |
| 322 | B2C-COMM → OMS | commerce_order.placed | commerce_orders | Process customer orders (10186) child: Accept and validate sales orders (10742) | confident L4 |
| 323 | B2C-COMM → CRM | commerce_order.placed | commerce_orders | Manage sales orders (10185) | confident L3 |
| 324 | B2C-COMM → LOYALTY | commerce_order.placed | commerce_orders | Manage customer loyalty programs (10173) | confident L3 |
| 325 | B2C-COMM → ERP-FIN | commerce_order.confirmed | payment_transactions | Process customer credits (10743) or Process and distribute payments (10862) | confident L4 (already 1422 on h329) |
| 326 | B2C-COMM → ERP-FIN | commerce_order.refunded | commerce_orders | Process customer credits (10743) child: Issue customer credit (10866) | confident L4 |
| 327 | B2C-COMM → MA | cart.abandoned | carts | Develop and manage marketing campaigns (10157) or Manage prospects/leads (10162) | confident L3 |
| 328 | B2C-COMM → MA | checkout.started | checkouts | Develop and manage marketing campaigns (10157) | confident L3 |
| 329 | B2C-COMM → CRM | payment.declined | payment_transactions | (already 1422 Process and distribute payments): keep or refine to a customer-comms child | keep existing |
| 330 | B2C-COMM → CSM | commerce_order.completed | commerce_orders | Manage customer service requests (10117) or Manage customer feedback (already covered as 6 at h66 substring match) | confident L3 |
| 331 | B2C-COMM → SUB-MGMT | commerce_order.placed | commerce_orders | Manage sales orders (10185) child: Process customer orders (10186) | confident L4 |
| 502 | B2C-COMM → OMS | order_line.added | order_lines | Accept and validate sales orders (10742) | confident L4 |
| 504 | B2C-COMM → CDP | commerce_product.published | commerce_products | Manage product master data (11217) or Plan product/service offerings (10005) | confident L4 |
| 505 | B2C-COMM → LOYALTY | fulfillment.delivered | fulfillments | (already 738 Determine fulfillment process): refine to Manage customer loyalty programs (10173) instead, since loyalty doesn't determine fulfillment | refine |
| 506 | B2C-COMM → MA | coupon.redeemed | coupons | Develop and manage marketing campaigns (10157) child: Manage promotions and discounts | confident L4 |

**Inbound (B2C-COMM-consumed, source = upstream domain):**

| handoff_id | source → target | trigger_event | payload | Proposed PCF (search hint) | Confidence |
|---|---|---|---|---|---|
| 80 | CDP → B2C-COMM | segment.activated | customers | (already 133 substring match on "Establish goals, objectives, and measures…" which is L3 marketing-strategy, not segment-activation; refine) → Manage marketing campaigns (10157) child | refine |
| 93 | HCMS → B2C-COMM | content_entry.published | content_entries | Manage marketing content (11018) or Manage product/service offerings (10006) | confident L4 |
| 96 | HCMS → B2C-COMM | content_release.published | content_releases | Manage marketing content (11018) | confident L4 |
| 480 | CDP → B2C-COMM | customer_journey.stage_entered | customer_journeys | (already 1814 Create customer journey maps L5): keep, this is a tight match | keep existing |
| 492 | SUB-MGMT → B2C-COMM | customer_invoice.issued | customer_invoices | Process customer credits (10743) child: Issue customer credit / invoice | confident L4 |
| 498 | LOYALTY → B2C-COMM | redemption_transaction.completed | redemption_transactions | Manage customer loyalty programs (10173) | confident L3 |
| 503 | OMS → B2C-COMM | fulfillment.delivered | fulfillments | (already 738): keep | keep existing |
| 529 | CDP → B2C-COMM | customer_attributes.refreshed | customer_attributes | Manage customer master data (11216) or Manage customer information (10130) | confident L4 |
| 812 | DXP → B2C-COMM | personalization_rule.activated | personalization_rules | Develop and manage marketing campaigns (10157) or Personalize customer experience | defer (no clean PCF, this is a digital-experience primitive) |
| 982 | OMS → B2C-COMM | inventory_location.capacity_threshold_breached | inventory_locations | Operate warehousing (10746) or Manage warehouse operations | confident L4 |
| 984 | OMS → B2C-COMM | order_allocation.created | order_allocations | Process customer orders (10186) child: Allocate inventory | confident L4 |
| 986 | OMS → B2C-COMM | order_allocation.reassigned | order_allocations | Same as 984 | confident L4 |
| 990 | OMS → B2C-COMM | sourcing_decision.computed | sourcing_decisions | Operate warehousing (10746) | defer (sourcing-orchestration primitive, may belong on a custom Discover row) |
| 993 | OMS → B2C-COMM | store_pickup_order.ready | store_pickup_orders | Process customer orders (10186) or Determine fulfillment process (10197) | confident L4 |
| 1059 | INV-MGMT → B2C-COMM | inv_stock_item.created | commerce_products | Manage product master data (11217) | confident L4 |
| 1234 | PIM → B2C-COMM | pim_product.published | commerce_products | Manage product master data (11217) | confident L4 |
| 1237 | PIM → B2C-COMM | pim_product.discontinued | commerce_products | Manage product master data (11217) | confident L4 |

**Deferred to Discover Pass 3 (no clean cross-industry PCF match):**

- 75 `storefront.interaction` (digital-experience signal, no L3 / L4 PCF for storefront-engagement events)
- 812 `personalization_rule.activated` (DXP-layer signal)
- 990 `sourcing_decision.computed` (sourcing-orchestration primitive)

Deferral count: 3 of 33 (~9%, within the expected ~20% defer rate).

### Bucket 2, Surface-for-user (judgment calls)

1. **B2-S1, `payment_transactions.has_personal_content`** Per B4, the flag is currently `false` across every master including `payment_transactions`. PCI-DSS treats payment-token / cardholder-name / cardholder-address content as PII. Should we PATCH `has_personal_content=true` on `payment_transactions` (and plausibly on `commerce_orders` and `checkouts` for shipping_address PII)? Options: (a) flip to `true` on all three, no notes needed (flags are structural); (b) flip on `payment_transactions` only; (c) leave as `false` and treat PII content as out-of-scope (PCI-DSS handled by gateway, addresses on a separate `customer_addresses` future entity).

2. **B2-S2, `skill_tools.notes` Rule #15 review** B1-S12 lists 4 populated `skill_tools.notes` rows. Were they user-approved at load time or auto-populated? If auto-populated, default is to PATCH each `notes` back to empty string and append a Rule #15 incident to `references/skill-changelog.md`. If the user wants the wording preserved, they need to explicitly approve each string. Options: (a) revert all 4 to empty; (b) approve all 4 as-is; (c) approve some, revert others (list which).

3. **B2-S3, parent_domain_id pointing at CRM** B2C-COMM (id 71) has `parent_domain_id=69` (CRM). Digital Commerce is not a sub-domain of CRM under the point-solution test (Shopify Plus, BigCommerce, commercetools sell as standalone markets, not as CRM extensions). Options: (a) clear `parent_domain_id` to NULL; (b) keep the CRM parent link as a curated affinity / cluster signal; (c) set to a different parent that better fits (no obvious candidate, OMS is a sibling not a parent).

4. **B2-S4, `COMM-B2B-COMM` capability ownership** `business_function_domains` has Marketing as owner / Sales as contributor on B2C-COMM proper. The `COMM-B2B-COMM` capability is B2B-shaped (account hierarchies, requisition lists, quote-to-order) and plausibly Sales-owned, not Marketing. Should we author a `business_function_capabilities` override for this capability? Options: (a) yes, override to Sales-owner / Marketing-contributor on `COMM-B2B-COMM`; (b) no, the domain-level RACI is sufficient; (c) split B2B into its own module (B1-S2 module set option) and let module-level ownership carry the override.

5. **B2-S5, 3-module vs 4-module split for B1-S2** B1-S2 proposes a 3-module split (`B2C-COMM-CATALOG-MERCH`, `B2C-COMM-ORDER-CAPTURE`, `B2C-COMM-FULFILL-SHIP`) with optional 4th `B2C-COMM-B2B` if Bucket 3 b2b candidates land. Should the agent take the 3-module split as the default, or hold for the user's call between (a) 3-module, (b) 4-module including B2B, (c) 2-module (fold fulfill / ship into order-capture; modular B2B becomes a separate starter / lite path)? The flagship pattern across vendors: Shopify Plus / Adobe Commerce do not separate fulfill / ship as a module (they bundle); commercetools / Saleor expose fulfillment as a separate API but not as a market sub-shape; SAP Commerce Cloud explicitly carves out a fulfillment add-on. Recommendation: 3-module split is the safer default; B2B as a starter only if research lands.

6. **B2-S6, master partitioning for `fulfillments` / `commerce_shipments`** If the user accepts the 2-module split (folding fulfill / ship into order-capture), `fulfillments` and `commerce_shipments` master in `B2C-COMM-ORDER-CAPTURE`. If 3-module split, they master in `B2C-COMM-FULFILL-SHIP` and `B2C-COMM-ORDER-CAPTURE` ships embedded shells. Confirm preferred split before B1-S2 loads.

### Bucket 3, Phase 0 pending (speculative)

1. **B3-S1, missing master cluster: catalog substrate** The flagship surface (every Shopify / BigCommerce / Adobe Commerce / commercetools / SAP Commerce Cloud schema) carries: `product_variants` (the SKU-level row under `commerce_products`), `product_categories` / `collections` (the catalog tree), `catalog_attributes` (the attribute schema). The current footprint masters `commerce_products` but not the SKU-level row that holds size / color / quantity / pricing. This is the single biggest gap in the catalog-merch slice. Vendor evidence: Shopify Plus Variant, BigCommerce Variant, commercetools ProductVariant, Adobe Commerce ConfigurableProduct + SimpleProduct, Saleor ProductVariant. Verification path: Phase 0 vendor research, focus on Shopify / commercetools / Adobe Commerce documentation. Confidence: high (this is structurally required across every flagship).

2. **B3-S2, missing master cluster: payments / pricing / tax** The flagship surface carries: `payment_methods` (the per-customer saved methods), `price_books` / `pricing_tiers` (the B2B-friendly multi-list pricing), `tax_calculations` / `tax_rates` (the tax engine integration spine), `gift_cards` (distinct from coupons, carrying stored value), `shipping_methods` / `shipping_zones` / `shipping_rates`. Vendor evidence: Shopify (TaxLine, Discount, ShippingLine), BigCommerce (TaxClass, PriceList, GiftCertificate), commercetools (TaxCategory, ShippingMethod, ShippingZone). Verification path: focus on tax / shipping APIs for the top 5 flagships. Confidence: medium-high (some of these may live on adjacent domains: tax_engines on a TAX-COMPLIANCE domain, payment_methods on PAYMENTS; defer if Phase 0 shows clean ownership boundaries elsewhere).

3. **B3-S3, missing master cluster: post-purchase / returns / reviews** Flagship surface: `returns` / `RMAs` / `refunds_requests` (distinct from `commerce_order.refunded` event; the request is its own lifecycle), `product_reviews` / `product_ratings` (UGC), `wishlists` / `saved_carts` (engagement / re-engagement). Vendor evidence: Shopify (Return / RefundRequest / ProductReview pre-Yotpo-integration era), BigCommerce (RmaRequest), Adobe Commerce (RmaRequest, ProductReview), commercetools (Return / ReturnInfo). Verification path: focus on returns and reviews APIs. Confidence: medium (returns may belong on OMS in some architectures; reviews may belong on a separate VOC / customer-feedback domain).

4. **B3-S4, missing master cluster: B2B commerce surface** Flagship surface: `b2b_accounts` / `company_accounts`, `buyer_groups`, `quotes`, `requisition_lists`, `contracts` (B2B negotiated price contracts), `purchase_approvals`, `customer_addresses` (PII-bearing reference data, B2B and B2C both). Vendor evidence: Salesforce Commerce Cloud B2B, SAP Commerce Cloud (Hybris B2B), commercetools B2B-Commerce, BigCommerce B2B Edition, Adobe Commerce B2B. Verification path: focus on each flagship's B2B documentation. Confidence: high if B2C-COMM keeps `COMM-B2B-COMM` capability; if the user splits B2B out as its own domain (`B2B-COMM`), the candidate goes to that new domain's Phase 0 instead. The `_missing-domains.md` queue does not have a `B2B-COMM` candidate today; option to surface (B2C-COMM-audit-derived) is open.

### Cross-bucket dependencies

- **B1-S2 (M1 fix) gates B1-S8, B1-S9, B1-S10** All three are downstream of the missing modules. Cannot fix without modules in place first.
- **B1-S3 (intra-domain relationships) can be partially authored before B1-S2** The verb-and-owner-side edges are master-to-master and module-independent. But the cluster-drafts loader idiom typically also wires module-level edges; safer to defer until after B1-S2.
- **B2-S5 / B2-S6 gate B1-S2** The user's call on 2-vs-3-vs-4 module split determines the loader payload for B1-S2. Hold the M1 fix until the user picks.
- **B2-S1 (has_personal_content flag) independent** of B1-S2 cascade. Can be PATCHed standalone.
- **B2-S2 (notes Rule #15 review) independent.** Resolves to PATCH-to-empty or approve-as-is; doesn't affect any structural work.
- **B2-S3 (parent_domain_id) independent.** Affects only the parent-domain tree.
- **B2-S4 (B2B capability ownership) interacts with B3-S4** If B3-S4 vendor research validates a separate B2B-COMM domain, the B2B capability moves there entirely and the override question disappears. If B2B stays inside B2C-COMM, the override is the right answer.
- **B3 candidates (B3-S1..4) inform B1-S2 module shape** If B3-S1 (product_variants / categories) lands, `B2C-COMM-CATALOG-MERCH` grows; if B3-S2 (tax / payments / shipping) lands, ownership decisions for tax / payments may shift the module split. The agent recommends running Phase 0 on B3-S1 and B3-S4 first since they have the highest blast radius on the module shape.

### Per-bucket prompts

- **Bucket 1 (12 items, B1-S1..S12, plus B1-H1 APQC tagging):** Fix these now? Reply "all", "just <list>", or "skip". The recommended order is B1-S1 (em-dash patch, trivial) → user decision on B2-S5 / B2-S6 → B1-S2 (modules) → B1-S3 / B1-S4 (relationships, users edges) → B1-S5 (event_category) → B1-S6 (aliases) → B1-S7 (lifecycle states) → B1-S8 / B1-S9 (module-FK backfills) → B1-S10 (legacy skill retirement) → B1-S11 (channel-vs-capability fix) → B1-S12 (Rule #15 revert, awaiting B2-S2 answer) → B1-H1 (APQC tagging insert). B1-S1 can be fixed standalone today.
- **Bucket 2 (6 items, B2-S1..S6):** What is your call on each? I need an explicit per-item decision before acting on B1-S2 (B2-S5 / B2-S6 gate the module shape), B1-S12 (B2-S2 gates the notes revert), and the lifecycle / personal-content flags (B2-S1).
- **Bucket 3 (4 items, B3-S1..S4):** Vet via Phase 0 vendor research, or eyeball-mode? If eyeball, name which candidates ring true (likely B3-S1 product_variants / categories rings true to every flagship-vendor reader). If Phase 0, recommend the order B3-S1 → B3-S4 → B3-S3 → B3-S2 by blast radius on the module shape.

### Report-only follow-ups (owed by other domains)

The 16 inbound handoffs to B2C-COMM each have a B10b `source_domain_module_id=NULL` that's owed by the source domain's b1 audit. The fix happens when those source domains are themselves reviewed. Not B2C-COMM's fix to make.

| handoff_id | source domain | source module owner | What is owed |
|---|---|---|---|
| 80, 480, 529 | CDP | TBD | B10b: set `source_domain_module_id` on the CDP side; also CDP B9 on these events |
| 93, 96 | HCMS | TBD | B10b: set `source_domain_module_id` on the HCMS side |
| 492 | SUB-MGMT | TBD | B10b: set `source_domain_module_id` |
| 498 | LOYALTY | TBD | B10b: set `source_domain_module_id` |
| 503, 982, 984, 986, 990, 993 | OMS | TBD | B10b: set `source_domain_module_id` (6 rows) |
| 812 | DXP | TBD | B10b: set `source_domain_module_id` |
| 1059 | INV-MGMT | TBD | B10b: set `source_domain_module_id` |
| 1234, 1237 | PIM | TBD | B10b: set `source_domain_module_id` |

Also report-only: the symmetric B8 inbound `data_object_relationships` rows are owed by the source domains (each upstream master needs an outbound edge to the B2C-COMM payload). Not loaded from this audit pass.

### Pairwise reconciliation notes

- **OMS (weight 7, full pairwise):** OMS publishes 5 inbound (sourcing, allocations, capacity, store-pickup) into B2C-COMM, all on OMS-mastered payloads (`sourcing_decisions`, `order_allocations`, `inventory_locations`, `store_pickup_orders`). B2C-COMM should consume these via consumer DMDOs once B1-S2 lands. OMS receives B2C-COMM's `commerce_order.placed` and `order_line.added` outbound. Boundary check: clean, no shared master overlap. B2C-COMM masters `commerce_orders`; OMS treats it as a consumer (verify on OMS's pass). No scope-creep flagged.
- **CRM (weight 5, full pairwise):** B2C-COMM publishes 3 outbound (customer.signed_up, commerce_order.placed, payment.declined) to CRM; zero inbound. CRM receives `customers` (97) which B2C-COMM contributes (`role=contributor`). Boundary check: clean. B2C-COMM contributes `crm_leads` (99) but only emits via `customer.signed_up` indirection. No overlap on CRM-mastered entities.
- **CDP (weight 6, full pairwise):** B2C-COMM publishes `storefront.interaction` and `commerce_product.published` to CDP. CDP publishes `segment.activated`, `customer_journey.stage_entered`, `customer_attributes.refreshed` into B2C-COMM. Shared substrate: `audience_segments` consumed by B2C-COMM; `customers` shared as contributor (B2C-COMM) / consumer (CDP). Boundary check: clean. Recommend B1-S2's `B2C-COMM-CATALOG-MERCH` consume `customer_journeys` / `customer_attributes`.
- **MA (weight 4, full pairwise):** B2C-COMM publishes 3 outbound (cart.abandoned, checkout.started, coupon.redeemed); zero inbound. `marketing_campaigns` consumer on B2C-COMM. Boundary check: clean.
- **ERP-FIN (weight 3, full pairwise):** B2C-COMM publishes 3 outbound (commerce_order.refunded, commerce_order.confirmed, fulfillment.delivered). Boundary check: clean. ERP-FIN does not publish back into B2C-COMM (correct: ERP-FIN is a sink).
- **LOYALTY (weight 4, full pairwise):** B2C-COMM publishes `commerce_order.placed`, `fulfillment.delivered`; LOYALTY publishes `redemption_transaction.completed` back. Boundary check: clean.
- **SUB-MGMT (weight 5, full pairwise):** B2C-COMM publishes `order.subscription_purchase`, `commerce_order.placed`; SUB-MGMT publishes `customer_invoice.issued` back. `customer_subscriptions` and `customer_invoices` are SUB-MGMT-mastered, B2C-COMM-consumed. Boundary check: clean.
- **Lightweight (CSM, HCMS, INV-MGMT, DXP, PIM):** Single-row or two-row touches with no DMDO overlap or boundary tension. No specific findings beyond the per-handoff APQC tags in B1-H1.

### Decisions

_(awaiting user input per bucket)_

### Fixes applied

_(none yet, audit only)_
