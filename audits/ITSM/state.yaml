# === SUPERSEDED 2026-06-06: per-domain-skill restoration ===
# The per-module `system` skill grain is RETIRED (plans/per-domain-skill-restoration.md).
# Any open item that says "author/split a per-module system skill", "one system skill per
# domain_modules row", "add/PATCH skill_tools", or "<module>_agent per module" is CANCELED.
# New model: tool requirements live on `domain_module_tools` (author tools onto modules); each
# domain has exactly ONE domain-grain `system` skill (domain_id set, domain_module_id null) that
# DERIVES its toolset; starters keep their own module-anchored skill; FULL modules carry no skill;
# cross-domain value streams use `process_tools`. `skill_tools` is dropped. Per-module tool
# re-authoring is tracked in audits/_modularization-backlog.md. Do NOT author per-module skills.
# === end supersession note ===
schema_version: 2
domain: ITSM
status: feedback_needed
next_action_by: user
last_audit: 2026-06-07

b1a:
  - id: B1A-H1-DISCOVER-DEFER
    summary: 2 cross-domain inbound ITSM handoffs have no clean PCF match; defer to Discover Pass 3
    finding: >
      H1 execute pass 2026-06-07 tagged 9 of the 11 genuinely-untagged ITSM cross-domain handoffs
      (the prior 16-id snapshot was stale: handoff 9 is a non-ITSM Onboarding->HRSD edge already
      tagged, and 140/177/642/644 were already tagged). The remaining 2 have no clean PCF match and
      are scope-creep candidates, not mechanical lookups:
        - handoff 843 (Data Loss Prevention -> ITSM-INCIDENT-MGMT, trigger dlp_incident.blocked):
          a DLP block is a security control event, not an IT service incident; flagged as a misfit
          across multiple prior audits. No clean PCF.
        - handoff 901 (Healthcare Patient Operations -> ITSM-INCIDENT-MGMT, trigger
          clinical_order.placed): a clinical order placement is not an IT incident; the handoff
          target/payload (service_incidents) reads as scope creep from the source domain.
      Both publish payload service_incidents (47) onto module 38 after the 2026-05-29 backfill.
    action: >
      Do NOT bulk-assign PCF 1299. Defer both to Discover Pass 3 / source-domain scope review (DLP
      and Healthcare Patient Operations own whether the edge belongs to ITSM at all). If the edges
      survive scope review, tag at that point.
    affected_handoffs:
      - {handoff_id: 843, source_domain: DLP, target_domain: ITSM, trigger_event: dlp_incident.blocked}
      - {handoff_id: 901, source_domain: HEALTHCARE-PATIENT-OPS, target_domain: ITSM, trigger_event: clinical_order.placed}

  - id: B1A-SELF-CONTAIN
    summary: "2 contributor/required-consumer rows break module self-containment (M9): embed or relax each. DESTRUCTIVE (rewrites role/necessity on existing DMDO rows) - surfaced, not auto-applied."
    finding: "Live triage: 2 domain_module_data_objects rows on this domain's modules are role=contributor or role=consumer with necessity=required, pointing at an entity mastered by another domain and not embedded_master here. Each is an M9 self-containment violation. Fixing rewrites an existing non-empty row, so it is destructive and requires sign-off."
    action: "Per row, convert to embedded_master (carry a local shell) or set necessity=optional (presence-conditional), per references/modules.md section 5 and SKILL.md M9. Surfaced for user approval; not auto-applied."
    extra_m9_shapes:
      - {entity: asset_lifecycle_events, module: ITSM-INCIDENT-MGMT, role: contributor, master_domain: ITAM}
      - {entity: onboarding_tasks, module: ITSM-SERVICE-REQUEST, role: consumer, master_domain: ONBOARDING}

  - id: B1A-PHASE-P
    summary: "Multi-module (8 modules) with 0 personas post-Plan-3 (E1 fail): persona / RACI layer. DEFERRED (personas not auto-authored)."
    finding: "8 domain_modules and 0 domain_roles reaching this domain via role_modules. Plan 3 deleted the old _core personas, so E1 fires on this multi-module domain. NOTE: the 2026-05-31 audit recorded 4 roles (IT-SERVICE-DESK-AGENT, IT-SERVICE-DESK-MANAGER, IT-CHANGE-MANAGER, IT-KNOWLEDGE-AUTHOR) with 19 role_modules; reconcile whether Plan 3 deleted these or whether this finding is stale before authoring."
    action: "DEFERRED per audit contract (personas/RACI are not auto-authored). Candidate personas if/when authored: IT-SERVICE-DESK-AGENT, IT-SERVICE-DESK-MANAGER, IT-CHANGE-MANAGER, IT-PROBLEM-MANAGER, IT-KNOWLEDGE-AUTHOR, END-USER-REQUESTER. Wire role_modules across the 8 modules + process_raci rows. ATS is the worked reference (scripts/loaders/load_ats_personas_pilot.ts)."

b1b:
  - id: B1B-B9b
    summary: B9b intra-domain cross-module handoff rows absent for ITSM; new trigger_events required for each edge
    finding: ITSM has 8 modules but zero rows in handoffs with source_domain_id=target_domain_id=1. Eight expected intra-domain edges (incident->problem, problem->change, change->configuration_items, request->incident, sla->incident, sla->request, incident->knowledge, problem->knowledge). Each needs a new trigger_events row authored with event_name, event_category, payload data_object_id, and condition; this is editorial choice not mechanical.
    blocked_by:
      - {type: depends_on, ref: trigger-event-authoring-session, reason: creative pass; eight intra-domain trigger events need event_name + payload + condition editorial choice}

  - id: B1B-M7-MONITORING-ALERTS
    summary: monitoring_alerts canonical owner ambiguous between ITOM and RMM at the new domain_module_data_objects layer
    finding: At the legacy domain_data_objects layer, monitoring_alerts (id 85) has master rows in both ITOM (domain 2) and RMM (domain 130). At the new domain_module_data_objects layer there is no master row at all; only ITSM's embedded_master in ITSM-EVENT-MGMT (module 42) and consumer rows elsewhere. B5 passes via the legacy rollup (Rule #11) but the catalog-wide M7 single-master rule is at risk once ITOM and RMM finish modularization.
    blocked_by:
      - {type: domain_audit, blocking_domain: ITOM, milestone: m7-canonical-monitoring-alerts}
      - {type: domain_audit, blocking_domain: RMM, milestone: m7-canonical-monitoring-alerts}

  - id: B1B-ASSET-FAILURE
    summary: event 65 service_incident.asset_failure cross-domain attribution question (carry from 2026-05-29 B1-S7)
    finding: trigger_events.id=65 service_incident.asset_failure publishes on data_object_id=47 (service_incidents); handoff 31 attributes source_domain=1 ITSM -> ITAM. Open question whether the event correctly originates in ITSM or whether ITAM should author asset.failed as the publisher with ITSM as subscriber. Cross-domain ownership question; ITAM owns the resolution. NOTE 2026-06-07 handoff 31 was tagged to PCF 1312 "Maintain IT asset records" (additive, independent of the attribution decision); the attribution question itself stays blocked on ITAM.
    blocked_by:
      - {type: domain_audit, blocking_domain: ITAM, milestone: asset-failure-publisher-decision}
    affected_handoffs:
      - {handoff_id: 31, direction: outbound, source_domain: ITSM, target_domain: ITAM, trigger_event: service_incident.asset_failure, payload: asset_lifecycle_events}
    affected_events:
      - {id: 65, event_name: service_incident.asset_failure, current_category: state_change, proposed_category: state_change}

b2:
  - id: B2-PATTERN-FLAGS
    summary: pattern flag positive re-evaluation on 3 masters (carry from 2026-05-29 B2-S2)
    question: Per Rule #12 / B4 positive re-evaluation, should service_incidents.has_personal_content=true (incident descriptions routinely carry user PII)? knowledge_articles.has_personal_content=true (some KB articles reference customer cases)? service_changes.has_submit_lock=true (freeze change payload at CAB submission)?
    options:
      - Approve all three flips
      - Approve a subset
      - Decline all (flags stay false)
    why: Pattern flags are workflow-shape judgments the user owns; defaults to false do not establish review. Recording the consideration in notes is forbidden by Rule #15, so the answer lives only in the audit decisions. All three masters are now classified entity_type=operational_workflow (2026-06-07), so flag consideration is in scope.
    affected_masters:
      - {data_object_id: 47, name: service_incidents, target_module_id: 38, workflow: incident}
      - {data_object_id: 50, name: service_changes, target_module_id: 41, workflow: change}
      - {data_object_id: 51, name: knowledge_articles, target_module_id: 43, workflow: knowledge}

  - id: B2-SLA-LIFECYCLE
    summary: service_slas (id 53) has zero lifecycle states (carry from 2026-05-29 B2-S1)
    question: service_slas is now classified entity_type=catalog (2026-06-07), under which lifecycle states are permitted but not required (Rule #12), so B12 no longer fails on it. Leave it stateless (config-shape), or author a state machine (e.g. draft, published, active, expired, archived)?
    options:
      - Accept config-shape (catalog) classification; no state machine; B12 passes
      - Author a state machine; surface proposed states for approval
    why: B12 is satisfied by the catalog classification, but a published SLA does have an effective period (active vs expired) worth modeling as state if the workflow demands it. Either choice is defensible; the user owns the call.
    affected_masters:
      - {data_object_id: 53, name: service_slas, target_module_id: 45, workflow: sla-config}

  - id: B2-SKILL-NAMING
    summary: "RETIRED 2026-06-06 (per-domain-skill restoration). Originally: ITSM system skills used kebab itsm-<module>-system vs SKILL snake <module_code_lower>_agent."
    question: "SUPERSEDED. The per-module system-skill grain is retired (see header). The 8 kebab itsm-<module>-system skills are no longer the model; each domain now carries exactly ONE domain-grain system skill. Re-frame: confirm the ITSM domain-grain skill exists and the legacy per-module skills are dispositioned per audits/_modularization-backlog.md. No naming-convention decision is owed under the new model."
    options:
      - No action under the new model; track per-module skill disposition in audits/_modularization-backlog.md
    why: Retired item kept as a note so the supersession is traceable; not an open editorial decision.

b3:
  - id: B3-SERVICE-OUTAGES
    candidate: service_outages
    rationale: Flagship major-incident and status-page products model a customer-facing outage record distinct from the internal incident. Catalog likely needs the split.
    proposed_module: ITSM-INCIDENT-MGMT

  - id: B3-CHANGE-COLLISIONS
    candidate: change_collisions
    rationale: Change advisory tooling detects overlapping change windows and blackout periods; modeled as a first-class catalog entity in flagship products.
    proposed_module: ITSM-CHANGE-MGMT

  - id: B3-CAB-MEETINGS
    candidate: cab_meetings
    rationale: Change Advisory Board agenda and minutes are modeled as a first-class entity by flagship change products.
    proposed_module: ITSM-CHANGE-MGMT

  - id: B3-SERVICE-OFFERINGS-SPLIT
    candidate: service_offerings
    rationale: Flagship products distinguish the service definition (offering) from its catalog presentation (catalog_item); current model collapses both onto service_catalog_items.
    proposed_module: ITSM-SERVICE-REQUEST

  - id: B3-WALKUP-VISITS
    candidate: walkup_visits
    rationale: ITSM-WALKUP capability realizes in ITSM-AGENT-WORKSPACE but no walkup_visits or walkup_kiosks master exists; capability-orphan finding from prior audit.
    proposed_module: ITSM-AGENT-WORKSPACE

  - id: B3-VIRTUAL-AGENT-CONVERSATIONS
    candidate: virtual_agent_conversations
    rationale: ITSM-VIRTUAL-AGENT capability has no virtual_agent_conversations or chatbot_sessions master; capability-orphan finding from prior audit.
    proposed_module: ITSM-AGENT-WORKSPACE

  - id: B3-CHARGEBACK-INVOICES
    candidate: chargeback_invoices
    rationale: ITSM-CHARGEBACK capability has no chargeback_invoices or service_cost_records master; capability-orphan finding from prior audit.
    proposed_module: ITSM-SLA-MGMT
