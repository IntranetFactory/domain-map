---
status: feedback_needed
last_transition: 2026-05-30
last_transition_by: agent
open_questions: 27
---

# IPAAS - Audit History

## 2026-05-30 - Validate b1 (full 4-pass)

### Summary

- **Current footprint:** **0** `domain_modules` rows (M1 / M2 / M4 / M6 hard-fail), **0** capabilities, **6** `domain_data_objects` masters (`integration_recipes` 469, `integration_runs` 470, `integration_connectors` 471, `webhook_subscriptions` 472, `integration_triggers` 473, `integration_data_mappings` 474, all `role=master + necessity=required`), **4** solutions (all `coverage_level=primary`: ServiceNow Integration Hub, MuleSoft Anypoint Platform, Boomi Platform, Workato), **1** regulation (EU Data Act, applicability mandatory), **8** `trigger_events` on IPAAS masters, **6** outbound + **2** inbound cross-domain handoffs, **0** intra-domain handoffs, **0** `data_object_lifecycle_states` on any master, **1** outbound `data_object_relationships` (`test_cases is_triggered_by integration_data_mappings`, owner_side=target, from TEST-MGMT's master), **0** intra-domain relationships, **0** `data_object_aliases`, **0** roles touching IPAAS, **1** legacy `domain_id`-anchored system skill (`ipaas-system`, id 72) with 6 `query_*` `skill_tools` (all `coverage_tier=platform`), 0 mutate / side_effect / compute / fetch tools, **0** `handoff_processes` rows on any IPAAS handoff. **2** `business_function_domains` rows (IT Infrastructure owner, Software Engineering contributor; "IT Infrastructure" is a non-canonical spine name, see B2-S7).
- **Vendor-surface basis:** flagship enterprise iPaaS vendors confirmed in `solutions`: ServiceNow Integration Hub (workflow-bundled iPaaS), MuleSoft Anypoint Platform (API-led integration leader), Boomi Platform (cloud-native iPaaS pure-play), Workato (recipe-driven low-code iPaaS). Adjacent flagship vendors enumerated by Pass 2 but NOT in `solutions` and flagged as missing in B2-S8: Zapier (citizen / SMB iPaaS leader, plausibly belongs in a sibling CITIZEN-AUTO market, queued), Make.com (no-code, same cohort), Tray.io (mid-market recipe / blueprint iPaaS), Celigo (NetSuite-anchored iPaaS), Jitterbit (legacy on-prem to cloud iPaaS), IBM App Connect (enterprise suite-aligned), SnapLogic (data + app integration hybrid), Microsoft Power Automate (Microsoft 365-anchored iPaaS / citizen automation). Compliance specialist not separately enumerated (EU Data Act applies horizontally via cross-border data-movement obligations, not a vendor archetype).
- **Bucket 1 (in-scope, agent fixable):** 14 items.
- **Bucket 2 (surface-for-user, judgment):** 8 items.
- **Bucket 3 (Phase 0 pending, speculative):** 5 items.

**Neighbor discovery** (auto-derived from `handoffs` + cross-domain DMDO, ranked by edge weight):

| Neighbor | Out | In | DMDO cross-refs | Weight | Pass shape |
|---|---|---|---|---|---|
| OBS | 1 | 0 | 0 | 1 | Lightweight |
| APIM | 1 | 1 | 0 | 2 | Lightweight |
| TEST-MGMT | 1 | 0 | 0 (1 outbound relationship row, owner_side=target) | 1 | Lightweight |
| WORK-MGMT | 1 | 0 | 0 | 1 | Lightweight |
| VSDP | 1 | 0 | 0 | 1 | Lightweight |
| ITSM | 1 | 0 | 0 | 1 | Lightweight |
| LCAP | 0 | 1 | 0 | 1 | Lightweight |

No neighbor reaches edge weight >= 3. Per the per-neighbor pass rule (light-weight summary for weight 1-2), each neighbor gets a one-line summary in "Boundary findings per neighbor" below; no 5-section deep dive. All seven neighbors share the same blocker: handoff rows have NULL `source_domain_module_id` (always, because IPAAS has zero modules), so the full diff collapses behind B1-S1.

**Structural pass bands:**

- **S band (sweep)**: zero-row anomalies on `domain_modules` (M1), `capability_domains` (A2), `domain_module_capabilities` (M6 vacuous), `data_object_lifecycle_states` (B12), `data_object_aliases` (B11), `data_object_relationships` involving any IPAAS master in the intra-domain direction (B6), `users` edges (B7), outbound cross-domain edges from IPAAS masters (B8), `roles` (E1 blocked behind M1). S2 vacuous (no modules). S3 hard-fails on every master: zero states, zero aliases; trigger events distributed across all 6 masters (1-2 per master, total 8).
- **A band**: A1 passes (all 7 domain metadata fields populated: crud=45, business_logic non-empty, min_org_size=`30 m <2500`, cost_band=`$$$$`, certification_required=false, USA TAM 4500 / 2025). A2 hard-fails (zero capabilities). A3 passes count-wise (4 solutions, all primary, but the vendor list is incomplete: Zapier / Make.com / Tray.io / Celigo / IBM App Connect / SnapLogic / Power Automate absent; see B2-S8). A4 hard-fails (`catalog_tagline` empty, `catalog_description` empty). A5 skipped (not requested). **Pre-existing U+2014 em-dash in `domains.business_logic`** (between "delivery at scale" and "code-dominant by definition") flagged for the catalog-wide em-dash cleanup sweep, report-only.
- **M band**: **M1 / M2 / M4 / M6 hard-fail**. Zero modules, zero capabilities. M1 floor (>= 1 module per domain) is unconditional; vendor surface implies a 2-module split (see B1-S1 / B2-S1). M2 (>= 2 modules when capability_count >= 3) is currently vacuous on capability_count=0, but once capabilities load, M2 will require >= 2 modules. M4 / M6 vacuous given no modules and no capabilities; both unblock the moment M1 cures.
- **B band**: B1 passes (6 masters). B2 passes (every master has singular + plural labels; note `integration_connectors.singular_label='IPAAS Connector'` carries the domain prefix unusually, see B2-S3). B3 passes (all 6 names are `integration_<noun>` or `webhook_<noun>` prefixed; bare-word arbitration not invoked). B4 hard-fail (every pattern flag is the `false` default with zero positive re-evaluation; candidates listed in B1-S3). B5 vacuously passes (no `embedded_master` rows). **B6 hard-fail** (zero intra-domain `data_object_relationships`; 6 masters that obviously edge to each other: recipes contain triggers and connectors and mappings, runs are produced by recipes, webhooks are a subtype of trigger). **B7 hard-fail** (zero `users` edges; every IPAAS master has obvious user-typed actors: recipe_author, run_initiator, connector_owner, webhook_owner, mapping_designer). **B8 hard-fail** (6 outbound cross-domain handoffs but zero cross-domain `data_object_relationships` from IPAAS masters to target-domain masters; the one existing relationship row 572->474 is inbound from TEST-MGMT, owner_side=target, and is the TEST-MGMT side's row, not IPAAS-authored). **B9 partial-fail** (8 trigger_events exist but every `trigger_events.event_category` is the empty-string default, violating the catalog enum-or-empty rule for `lifecycle | state_change | threshold | signal`; the events themselves are coherent name-shaped). B9b vacuous (no modules; no intra-domain module-pairs to verify). **B10b hard-fail** (all 6 outbound handoffs have NULL `source_domain_module_id`, blocked behind M1; handoff 769 has `target_domain_module_id=149` (WORK-MGMT-TASK-EXEC) already set, handoff 765 has `target_domain_module_id=38` (ITSM-INCIDENT-MGMT) already set; the other 4 outbound rows have NULL on both module FKs because the target domains OBS / APIM / TEST-MGMT / VSDP have no modules either, report-only on the target side). Both inbound rows 705 (LCAP -> IPAAS) and 748 (APIM -> IPAAS) have NULL `source_domain_module_id` (source-side report-only) AND NULL `target_domain_module_id` (in-scope post-B1-S1). **B11 hard-fail** (zero aliases on any master; obvious vendor synonyms exist: Boomi "process" / "atom" / "connection", Workato "recipe" / "job" / "connection", MuleSoft "flow" / "transaction" / "connector" / "DataWeave mapping", ServiceNow Integration Hub "action" / "execution" / "spoke", Zapier "Zap" / "task" / "app connection"). **B12 hard-fail** (zero lifecycle states; 5 of 6 masters have obvious workflow shapes: `integration_recipes` draft -> tested -> published -> deprecated; `integration_runs` queued -> running -> succeeded / failed (or retrying) -> archived; `integration_connectors` configured -> authenticated -> active -> credential_expired -> rotated; `webhook_subscriptions` configured -> active -> degraded -> disabled; `integration_data_mappings` draft -> validated -> published; `integration_triggers` is plausibly config-shape with no workflow (lives or is disabled), surface for user in B2-S4).
- **C band**: **C1 partial-fail**. 2 rows exist (IT Infrastructure owner, Software Engineering contributor; both `responsibility_type` correct), but "IT Infrastructure" is NOT a canonical 20-function spine name (the spine is "IT Operations" per SKILL.md § Function spine). Either the spine entry was renamed in this catalog or the row was authored against a non-canonical custom function. The same row's `business_function_id=58` resolves to a separate row from the canonical `IT Operations` (id 27). See B2-S7 for the choice between rename / re-link. C2 vacuous (no capabilities loaded).
- **D band**: D1 not exercised (no fresh load).
- **E band**: E1 vacuous (M1 blocks role authoring; the 2-module floor cannot be met until modules exist).
- **F band**: **F1 hard-fail-pending** (legacy `domain_id`-anchored `system` skill `ipaas-system` id 72; F1 currently sits in transitional state, but Rule #17 sets target state as one system skill per module). **F2 hard-fail** (zero `domain_module_id`-anchored system skills, vacuously blocked by M1). F3 passes against the legacy skill (6 `query_*` tools linked). F4 passes (all 6 tools are `operation_kind=query` with `data_object_id` set). F5 strict_score against the legacy skill is 6/6 = 100%, but the headline number is meaningless until per-module system skills exist and mutate / workflow-gate tools are added (current 6 query-only is below the recommended 5-20 mix). F7 vacuous (no channel primitives linked).
- **H band**: **H1 hard-fail**. 8 cross-domain handoffs (6 outbound + 2 inbound); zero `handoff_processes` rows; zero agent_curated tags; zero deferrals recorded. Volume target for this audit: ~4 to 6 agent_curated tags proposed (0.5N to 0.8N for N=8). Audit proposes 6 confident tags + 2 deferred below.

**The headline finding:** IPAAS is a marketing-tier domains row in roughly the same shape APP-PAAS and LCAP were before their re-audits: `domains` metadata + 4 vendor solutions + 6 mastered `data_objects` with descriptions + 8 trigger events + 1 legacy domain-level system skill + 6 query-only tools, but the M / B-relationships / C-capability / E / F-module / H layers are essentially empty. There are no `domain_modules`, no `capabilities`, no `data_object_relationships` from IPAAS masters in any direction, no lifecycle states, no aliases, no roles, no APQC tags. The deployable surface is zero. Every downstream concern (per-module attribution on handoffs, role bundling, system-skill derivation, Semantius score at module granularity, fact-sheet emission) is uncomputable until M1 / A2 cure. Additionally the solutions list is materially incomplete (Zapier, Make.com, Tray.io, Celigo, SnapLogic, IBM App Connect, Power Automate all absent; ServiceNow Integration Hub is correctly in but is suite-aligned not pure-play).

### Bucket 1 - In-scope confirmed gaps

#### STRUCTURAL band failures

| ID | Band | Finding | Fix |
|---|---|---|---|
| B1-S1 | **M1 / M2 / M4 / M6 hard fail + A2 hard fail** | Zero `domain_modules`, zero `capabilities`. Rule #14 requires >= 1 full module per domain. Vendor surface implies a 2-module split: **IPAAS-RECIPE-DESIGN** (recipe authoring, connector configuration, data-mapping design, trigger / webhook subscription setup; realizes capabilities along the lines of `IPAAS-RECIPE-AUTHORING`, `IPAAS-CONNECTOR-LIBRARY`, `IPAAS-DATA-MAPPING`, `IPAAS-WEBHOOK-SUBSCRIPTION-MGMT`; masters `integration_recipes`, `integration_connectors`, `webhook_subscriptions`, `integration_triggers`, `integration_data_mappings`) and **IPAAS-RUNTIME** (recipe execution, retry, error handling, run history; realizes `IPAAS-RECIPE-EXECUTION`, `IPAAS-ERROR-HANDLING-AND-RETRY`, cross-cutting `INTEGRATION-OBSERVABILITY` candidate; masters `integration_runs`). Capability cross-cutting choice goes to Bucket 2 (B2-S1 / B2-S6). Once modules exist, every M-band check cures. | Author 2 `domain_modules` rows + 5-7 `capabilities` rows + `capability_domains` links + `domain_module_capabilities` links + 6 `domain_module_data_objects` master rows. Pending Bucket 2 B2-S1 / B2-S6 decision on the split and cross-cutting linkage. |
| B1-S2 | **A4 hard fail - empty catalog UX fields** | `catalog_tagline` and `catalog_description` are both empty strings. Rule #20 requires both populated in buyer voice for any domain with a vendor surface. IPAAS is a buyer-facing market (4 solutions today, 7+ expected once B2-S8 lands). | Draft both fields per Rule #20 voice rule (workflow + value, not market position + handoffs), surface to user for review BEFORE writing. Sample tagline: "Connect any SaaS app to any other without writing integration code." Sample description: "Build, run, and monitor integration flows that move data between cloud apps, on-prem systems, and event streams. Use a library of prebuilt connectors, drag-and-drop data mappings, retry-aware execution, and webhook subscriptions to ship integrations in hours instead of weeks, with audit-grade run history and credential rotation built in." User must approve exact wording. |
| B1-S3 | **B4 pattern flag re-evaluation (Rule #12)** | Every master has all three flags `false` (default). Audit needs positive re-evaluation. Candidate flips: `integration_recipes.has_submit_lock=true` (a published recipe is immutable; edits create a new version, not mutate the published one - same shape as `paas_deployments`); `integration_recipes.has_single_approver=true` (publishing typically gates on one designated approver, especially in regulated tenants); `integration_runs.has_submit_lock=true` (a completed run record is immutable, retries spawn new runs); `integration_connectors.has_personal_content=true` (credentials / OAuth tokens / API keys stored on the connector row, see B2-S3 for whether this belongs as connection vault metadata vs the connector itself); `integration_data_mappings.has_submit_lock=true` (published mapping immutable, edits version). Other masters: `webhook_subscriptions`, `integration_triggers` (no flag flips proposed). | Surface as Bucket 2 (B2-S3) because pattern flags are workflow-shape judgments the user owns. |
| B1-S4 | **B6 hard fail - zero intra-domain `data_object_relationships`** | IPAAS has 6 masters that all participate in a single integration-design-and-execute workflow chain. Minimum coherent edge set: `integration_recipes` contains `integration_triggers` (1:N, owner_side=integration_recipes, is_required=true; a recipe must have at least one trigger to be runnable); `integration_recipes` references `integration_connectors` (M:N via an inline references table, recipes use multiple connectors and connectors are reused across recipes); `integration_recipes` defines `integration_data_mappings` (1:N, owner_side=integration_recipes); `integration_triggers` produces `integration_runs` (1:N; each trigger fire spawns a run); `integration_runs` uses `integration_connectors` (M:N at execution time, run-side audit of which connectors were touched); `webhook_subscriptions` is_subtype_of `integration_triggers` (1:1 specialization OR is a sibling concept depending on the design - see B2-S4); `integration_runs` applies `integration_data_mappings` (1:N at execution time). | Author 6-7 `data_object_relationships` rows via the cluster-drafts pattern in [scripts/loaders/load_cluster_drafts.ts](../scripts/loaders/load_cluster_drafts.ts). Draft block goes through Bucket 1 approval before loading. The webhooks vs triggers subtype decision (B2-S4) shifts the row count by one. |
| B1-S5 | **B7 hard fail - zero `users` edges (Rule #10)** | IPAAS masters have obvious user-typed actors but no edge to the platform built-in `users` data_object (id 748). Minimum edge set: `users` authors `integration_recipes` (recipe_author / designer); `users` publishes `integration_recipes` (publisher, distinct from author per B1-S3 single-approver flip); `users` owns `integration_connectors` (connector_owner / credential holder); `users` subscribes `webhook_subscriptions` (subscription_owner); `users` initiates `integration_runs` (run_initiator for manually-triggered runs; scheduled / event-triggered runs may have no user actor); `users` designs `integration_data_mappings` (mapping_designer). | Insert 5-6 `data_object_relationships` rows from the platform built-in `users` row (data_object_id=748) to each IPAAS master with a user actor. Bundle with B1-S4 in the same cluster-drafts load. |
| B1-S6 | **B11 hard fail - zero `data_object_aliases`** | IPAAS masters carry strong vendor-specific synonyms across the 4 confirmed solutions plus the 7 absent vendors flagged in B2-S8. Boomi: "process" / "atom" / "connection" / "map"; Workato: "recipe" / "job" / "connection" / "lookup table"; MuleSoft: "flow" / "transaction" / "connector" / "DataWeave mapping" / "API spec"; ServiceNow Integration Hub: "action" / "spoke" / "execution"; Zapier: "Zap" / "task" / "app connection" / "Path"; Make.com: "scenario" / "operation" / "connection" / "router"; Tray.io: "workflow" / "connector" / "trigger"; SnapLogic: "pipeline" / "Snap" / "execution". Without aliases the architect agent can't bridge vendor terminology into the catalog's canonical names. | Draft alias rows (6 masters x ~4-6 aliases = ~24-32 rows) and load via the cluster-drafts pattern. |
| B1-S7 | **B12 hard fail - zero `data_object_lifecycle_states` (Rule #12)** | Every IPAAS master except possibly `integration_triggers` has an obvious state machine. Proposed states (sequence dependent on B1-S1 module attribution per M5): `integration_recipes` (draft -> tested -> published -> deprecated; `published` and `deprecated` have `requires_permission=true`); `integration_runs` (queued -> running -> retrying -> succeeded / failed -> archived; `running`, `succeeded`, `failed` all have `requires_permission=true` for the publish gate on outbound events 838 / 839); `integration_connectors` (configured -> authenticated -> active -> credential_expired -> rotated -> revoked; `active`, `credential_expired`, `revoked` have `requires_permission=true`); `webhook_subscriptions` (configured -> active -> degraded -> disabled; `active` and `disabled` have `requires_permission=true`, `degraded` is a derived state); `integration_data_mappings` (draft -> validated -> published; `published` has `requires_permission=true`); `integration_triggers` see B2-S4 (config-shape or thin workflow). `domain_module_id` on each lifecycle row must point at the realizing module (M5), so this load depends on B1-S1 having authored the modules first. | Draft state machines (5 masters x ~4-5 states = ~22-28 rows) and load via a focused loader. Sequence behind B1-S1. |
| B1-S8 | **F1 hard fail / F2 hard fail - legacy domain-level system skill** | `skills` row id 72 (`ipaas-system`, `skill_type=system`, `domain_id=36`, `domain_module_id=NULL`) is the legacy shape Rule #17 forbids once any module-level system skill exists. F1 currently sits in vacuous transitional state because no module-level skill has been authored; once B1-S1 lands and module-level system skills are authored, F1 hard-fails until the legacy row is retired. The 6 linked `query_*` `skill_tools` (533-538) should be re-anchored to the new per-module skills: `query_integration_recipes` + `query_connectors` + `query_webhook_subscriptions` + `query_integration_triggers` + `query_integration_data_mappings` -> IPAAS-RECIPE-DESIGN skill; `query_integration_runs` -> IPAAS-RUNTIME skill. Also the skill_name uses kebab `ipaas-system` against the snake `<module_code_lower>_agent` convention. | After B1-S1: author 2 new module-anchored system skills (e.g. `ipaas_recipe_design_agent`, `ipaas_runtime_agent`), move the 6 `skill_tools` to the new skills, then DELETE the legacy skill 72. Plus add mutate + workflow-gate tools per Rule #17 (typical floor 5-20 per skill; current 6 query-only is below the floor and 0 mutate / workflow-gate is the bigger gap). Proposed mutate additions: `publish_integration_recipe`, `deprecate_integration_recipe`, `rotate_connector_credentials`, `enable_webhook_subscription`, `disable_webhook_subscription`, `publish_integration_data_mapping`; proposed side_effect: `dispatch_outbound_event` (no `data_object_id` since outbound event is the surface); proposed fetch: `fetch_vendor_health_status` (no `data_object_id`, queries each connector's vendor API for health). |
| B1-S9 | **B10b hard fail - NULL module FKs on every cross-domain handoff** | All 6 outbound handoffs (766, 767, 768, 769, 801, 765) have NULL `source_domain_module_id`, blocked behind M1. After B1-S1 lands and the 6 masters are attributed to modules, `source_domain_module_id` derives cleanly per the B10b backfill rule: handoffs 766 (`integration_run.failed` -> OBS, payload `integration_runs`) and 765 (`integration_run.failed` -> ITSM, payload `service_incidents`) -> IPAAS-RUNTIME (masters `integration_runs`); handoff 767 (`integration_connector.credential_expired` -> APIM, payload `integration_connectors`) -> IPAAS-RECIPE-DESIGN (masters `integration_connectors`); handoff 768 (`integration_data_mapping.updated` -> TEST-MGMT, payload `integration_data_mappings`) -> IPAAS-RECIPE-DESIGN (masters `integration_data_mappings`); handoff 769 (`webhook_subscription.delivery_failed` -> WORK-MGMT, payload `webhook_subscriptions`) -> IPAAS-RECIPE-DESIGN (masters `webhook_subscriptions`); handoff 801 (`integration_recipe.published` -> VSDP, payload `integration_recipes`) -> IPAAS-RECIPE-DESIGN (masters `integration_recipes`). Note handoff 765 (`integration_run.failed` -> ITSM) is the asymmetric shape `trigger_event.data_object_id=470` (integration_runs) vs `handoffs.data_object_id=47` (service_incidents, ITSM-mastered); source resolves to IPAAS-RUNTIME via the trigger_event's data_object, target is already wired to ITSM-INCIDENT-MGMT id 38. Inbound 705 (LCAP -> IPAAS, payload `extend_workflows` 222, LCAP-mastered) and 748 (APIM -> IPAAS, payload `api_specifications` 457, APIM-mastered) both carry NULL `source_domain_module_id` (LCAP and APIM both have no modules per cross-check, report-only on each source side) AND NULL `target_domain_module_id` (in-scope post-B1-S1; target should attribute to IPAAS-RECIPE-DESIGN as the consumer of vendor-published workflow / API spec changes that drive connector or recipe updates). | Single PATCH pass on 6 outbound rows + target_module backfill on 2 inbound rows after B1-S1 lands. Reference: [scripts/loaders/backfill_ats_handoff_modules_2026_05_23.ts](../scripts/loaders/backfill_ats_handoff_modules_2026_05_23.ts). The 2 inbound rows also need a consumer DMDO on the receiving IPAAS module declaring `extend_workflows` (222) and `api_specifications` (457) as `consumer` or `embedded_master`, see B2-S5 for the design choice. |
| B1-S10 | **B8 outbound - missing cross-domain `data_object_relationships`** | 6 outbound IPAAS handoffs but zero cross-domain `data_object_relationships` exist where IPAAS masters edge to target-domain masters. (The one existing relationship 572 `is_triggered_by` 474 is TEST-MGMT-authored, owner_side=target, so it counts on TEST-MGMT's outbound side, not IPAAS.) Proposed mirrors: handoff 766 (`integration_run.failed` -> OBS) maps to `integration_runs emits service_incident_signals` or `integration_runs reports_to observability_baselines` (OBS target master TBD - one-query verification needed); handoff 767 (`integration_connector.credential_expired` -> APIM) maps to `integration_connectors federates_with api_specifications` (target master 457 confirmed); handoff 768 (`integration_data_mapping.updated` -> TEST-MGMT) maps to `integration_data_mappings exposes_to test_cases` (target master 572 confirmed; this is the catalog mirror of the existing inverse relationship 572->474); handoff 769 (`webhook_subscription.delivery_failed` -> WORK-MGMT) maps to `webhook_subscriptions opens work_tasks` (target master TBD - WORK-MGMT has WORK-MGMT-TASK-EXEC module 149 already wired, one-query verification needed); handoff 801 (`integration_recipe.published` -> VSDP) maps to `integration_recipes records_in software_deployments` or `integration_recipes signals value_streams` (target master TBD); handoff 765 (`integration_run.failed` -> ITSM) maps to `integration_runs opens service_incidents` (target master `service_incidents` 47 confirmed). | Author the outbound `data_object_relationships` rows after B1-S4 / B1-S5 land; 3 rows (765, 767, 768) are directly resolvable now; 3 rows (766, 769, 801) need a one-query check of the target domain's current masters before authoring. |
| B1-S11 | **B9b hard fail (vacuous) - intra-domain handoffs blocked by M1** | After B1-S1 lands and the 6-7 intra-domain `data_object_relationships` from B1-S4 are loaded, the cross-module pairs derive: `integration_recipe.published` (RECIPE-DESIGN) fires the moment a recipe becomes runnable, which arms the IPAAS-RUNTIME side for new run intake (RECIPE-DESIGN -> RUNTIME, lifecycle_progression); `integration_trigger.fired` event 842 (RECIPE-DESIGN, source master is `integration_triggers`) spawns `integration_runs` (RECIPE-DESIGN -> RUNTIME, lifecycle_progression); `webhook_subscription.delivery_failed` 841 plausibly also signals RUNTIME to spawn a retry run (RECIPE-DESIGN -> RUNTIME). Other intra-module candidates (`integration_recipe.deprecated` -> internal RECIPE-DESIGN; `integration_connector.credential_expired` -> RECIPE-DESIGN internal) stay within one module. Net: 3 cross-module intra-domain handoff rows. | Author 3 intra-domain `handoffs` rows after B1-S1 + B1-S4 + lifecycle states from B1-S7. `source_domain_id = target_domain_id = 36`, `integration_pattern: lifecycle_progression`, `friction_level: low`. |
| B1-S12 | **A2 - missing capabilities (covered in B1-S1 fix)** | Zero `capability_domains` rows. IPAAS as a market has ~6 buyer-recognizable capabilities: prebuilt connector library (overlaps with existing cross-cutting `LCAP-INTEGRATION-CONNECTORS` id 334 - cross-cutting decision), recipe / flow authoring, data-mapping and transformation, webhook subscription management, recipe execution / retry / error handling, integration run observability. Capability code shape is Bucket 2 (B2-S1 / B2-S6) because the cross-cutting decision is a Rule #18 / cross-cutting-capability-convention call. | Surface 5-7 candidates in Bucket 2 B2-S1 for the cross-cutting decision; then load via the focused `capabilities` + `capability_domains` + `domain_module_capabilities` loader pattern. |
| B1-S13 | **B9 enum hygiene - empty `event_category` on every IPAAS event** | All 8 `trigger_events` rows (836-843) have `event_category=''` (empty string), violating the four-value enum convention (`lifecycle`, `state_change`, `threshold`, `signal`). Recommended classification: 836 `integration_recipe.published` -> `state_change`; 837 `integration_recipe.deprecated` -> `state_change`; 838 `integration_run.succeeded` -> `lifecycle`; 839 `integration_run.failed` -> `lifecycle`; 840 `integration_connector.credential_expired` -> `threshold` (time-based credential expiry); 841 `webhook_subscription.delivery_failed` -> `signal`; 842 `integration_trigger.fired` -> `signal`; 843 `integration_data_mapping.updated` -> `state_change`. | Single PATCH pass over the 8 rows, no module dependency, can ship before B1-S1. |
| B1-H1 | **APQC TAGGING** | IPAAS cross-domain handoffs have zero `handoff_processes` tags. Volume target 0.5N to 0.8N (N=8) is 4 to 6 tags. Audit proposes 6 high-confidence tags + 2 deferred below. | INSERT into `handoff_processes` with `proposal_source='agent_curated'`, `record_status='new'`. |

##### B1-H1 detail - proposed APQC tags

| handoff_id | source -> target | trigger_event | payload | Proposed PCF | PCF id | Confidence |
|---|---|---|---|---|---|---|
| 766 | IPAAS -> OBS | integration_run.failed | integration_runs | Triage IT service delivery incidents | 1299 (L4, ext 20903) | confident L4 |
| 765 | IPAAS -> ITSM | integration_run.failed | service_incidents | Triage IT service delivery incidents | 1299 (L4, ext 20903) | confident L4 |
| 767 | IPAAS -> APIM | integration_connector.credential_expired | integration_connectors | Manage IT user identity and authorization | 273 (L3, ext 20756) | medium L3 (identity-and-credential lifecycle is the closest cross-industry parent; connector credentials are auth artifacts) |
| 769 | IPAAS -> WORK-MGMT | webhook_subscription.delivery_failed | webhook_subscriptions | Manage service/solution operations | 1947 (L5, ext 20822) | medium L5 (prefer L3 parent `Develop service/solution and integration strategy` 278 if reviewer prefers a higher-level anchor; delivery failures route to ops work) |
| 801 | IPAAS -> VSDP | integration_recipe.published | integration_recipes | Implement software change/release | 1262 (L4, ext 20853) | confident L4 |
| 768 | IPAAS -> TEST-MGMT | integration_data_mapping.updated | integration_data_mappings | Define and create deployment procedure workflow | 1240 (L4, ext 20828) | medium L4 (mapping updates feed downstream regression tests; alternative tag `Perform post-installation testing` 1263 if reviewer reads this as test-gate signaling) |

Deferred-to-Discover-Pass-3 (no clean cross-industry PCF match):

- handoff 705 (LCAP -> IPAAS, `extend_workflow.published`, payload `extend_workflows`): closest PCF candidates are L4 `Implement software change/release` 1262 or L3 `Develop service/solution and integration strategy` 278, but the semantic intent is "low-code authored workflow is now ready to be integrated into broader iPaaS surface", which sits between LCAP's authoring lane and IPAAS's connector / recipe lane. Defer pending LCAP audit alignment.
- handoff 748 (APIM -> IPAAS, `api_specification.updated`, payload `api_specifications`): plausible cross-industry candidate is L4 `Determine IT component integration requirements` 1230, but the semantic is API-contract refresh driving downstream connector update, which is closer to a custom IT-platform integration-maintenance verb than any single cross-industry PCF row captures. Defer.

#### Boundary findings per neighbor

All neighbors are weight 1-2; lightweight summaries only (no 5-section deep dive). All seven share the M1 blocker on the IPAAS side: every outbound handoff has NULL `source_domain_module_id` until B1-S1 lands.

- **IPAAS <-> OBS (weight 1):** 1 out (766, `integration_run.failed`). Section-5 mirror proposed in B1-S10 (target master TBD on OBS side; OBS audit will need a `observability_baselines` or `service_incident_signals` master to anchor). Boundary integrity: OBS has zero modules per cross-check; target-side B10b backfill blocks on OBS's own M1 fix (report-only).
- **IPAAS <-> APIM (weight 2):** 1 out (767, `integration_connector.credential_expired`, target master `api_specifications` 457) + 1 in (748, `api_specification.updated`, payload `api_specifications` 457). Section-5 mirror proposed in B1-S10 (`integration_connectors federates_with api_specifications`). APIM has zero modules per cross-check; target-side B10b blocks on APIM's M1 fix (report-only on APIM side).
- **IPAAS <-> TEST-MGMT (weight 1, plus 1 inbound relationship row):** 1 out (768). The existing relationship 572 `is_triggered_by` 474 (TEST-MGMT's `test_cases` is_triggered_by IPAAS's `integration_data_mappings`, owner_side=target) is the structural mirror authored from the TEST-MGMT side; IPAAS's B8 still owes the explicit outbound row `integration_data_mappings exposes_to test_cases` (B1-S10). TEST-MGMT has zero modules per cross-check.
- **IPAAS <-> WORK-MGMT (weight 1):** 1 out (769) wired to target_module 149 (WORK-MGMT-TASK-EXEC). Source-side NULL pending B1-S1.
- **IPAAS <-> VSDP (weight 1):** 1 out (801). VSDP has zero modules per cross-check; target-side B10b blocks on VSDP's M1 fix (report-only).
- **IPAAS <-> ITSM (weight 1):** 1 out (765, asymmetric payload shape: trigger fires on `integration_runs` but payload is `service_incidents`). Already wired to target_module 38 (ITSM-INCIDENT-MGMT). One-line summary: target side wired correctly, source side blocked behind B1-S1.
- **IPAAS <-> LCAP (weight 1, inbound only):** 1 in (705). Source side NULL on LCAP side; LCAP audit's M1 gap blocks. Target side IPAAS module backfill depends on B1-S1 + B2-S5 (consumer DMDO for `extend_workflows`).

### Bucket 2 - Surface-for-user (judgment calls)

1. **B2-S1 - Module split + capability shape decision.** Proposed 2-module split: **IPAAS-RECIPE-DESIGN** (recipe authoring, connector library, data mappings, webhooks and triggers; masters `integration_recipes`, `integration_connectors`, `webhook_subscriptions`, `integration_triggers`, `integration_data_mappings`) and **IPAAS-RUNTIME** (recipe execution and run history; masters `integration_runs`). Capability set: `IPAAS-RECIPE-AUTHORING`, `IPAAS-CONNECTOR-LIBRARY` (cross-cutting candidate, see B2-S6), `IPAAS-DATA-MAPPING`, `IPAAS-WEBHOOK-SUBSCRIPTION-MGMT` on the RECIPE-DESIGN module; `IPAAS-RECIPE-EXECUTION`, `IPAAS-ERROR-HANDLING-AND-RETRY` on the RUNTIME module. Alternative: 3-module split (split connectors off as IPAAS-CONNECTOR-MGMT given credential-rotation lifecycle, or split RUNTIME into RUNTIME + OBSERVABILITY). **Decisions needed:** (a) approve the 2-module split, or propose alternative (3-module, single-module, or other)? (b) confirm which connectors belong on which side if a 3-module split is chosen? Independent of Bucket 3.

2. **B2-S2 - Catalog UX field wording (Rule #20).** Draft tagline + description proposed in B1-S2. User must approve exact wording; sample is a starting point only. Marketing voice required (buyer workflow + value, not analyst voice). Independent.

3. **B2-S3 - B4 pattern flag flips + credential-storage shape.** Proposed flips listed in B1-S3 (`integration_recipes.has_submit_lock=true`, `integration_recipes.has_single_approver=true`, `integration_runs.has_submit_lock=true`, `integration_connectors.has_personal_content=true`, `integration_data_mappings.has_submit_lock=true`). User confirms each or rewrites. The connector `has_personal_content=true` flip is borderline: typically iPaaS vendors store credentials in a separate connection / vault entity (Workato "Connection", Boomi "Account") and the connector itself is just the integration template. If the catalog should model that split, queue an `integration_connections` master in Bucket 3 (see B3-S2 candidate). Independent of Bucket 3 except for the connection-vault split.

4. **B2-S4 - `integration_triggers` lifecycle shape + webhooks-as-subtype design.** Two distinct decisions on the trigger / webhook surface:
   - (a) `integration_triggers` shape: are triggers config-shape entities with no real workflow (per Rule #12 exemption), or do they carry a thin lifecycle (`configured -> active -> disabled`)? The `integration_trigger.fired` event 842 is the only published transition and it fires every time the trigger executes, not as a state change. Option A: treat as config-shape, surface the exemption in chat per Rule #15 (no notes write). Option B: load the 3-state machine with `active` requires_permission=true.
   - (b) Relationship between `integration_triggers` and `webhook_subscriptions`: is webhook a subtype of trigger (existing-vendor shape: Workato treats webhook as a trigger kind; Boomi separates them), or sibling concepts? Decision shifts the B1-S4 relationship row count by one and affects whether `webhook_subscriptions` should embed trigger fields. Independent of Bucket 3.

5. **B2-S5 - Inbound payload consumer-DMDO declarations.** IPAAS-RECIPE-DESIGN plausibly consumes LCAP's `extend_workflows` (222, as the source low-code workflow definitions that get integrated) and APIM's `api_specifications` (457, as the contracts whose updates drive connector refreshes). Decide: declare both as `consumer + optional` on IPAAS-RECIPE-DESIGN (captures the dependency in the catalog), declare both as `embedded_master + optional` (preserves standalone-deploy story if IPAAS could need a shell when LCAP / APIM are absent), or accept that these dependencies are domain-level only (no DMDO row). Depends on B1-S1 module shape.

6. **B2-S6 - Cross-cutting capability membership.** The existing `LCAP-INTEGRATION-CONNECTORS` capability id 334 was authored under LCAP. iPaaS connectors are exactly the same surface (the prebuilt connector library is the marquee iPaaS feature). Decide: (a) rename `LCAP-INTEGRATION-CONNECTORS` to `INTEGRATION-CONNECTORS` (drop the LCAP prefix per cross-cutting-capability convention) and link to BOTH LCAP and IPAAS via `capability_domains`; (b) leave the LCAP-prefixed capability alone and author a parallel `IPAAS-CONNECTOR-LIBRARY` (double-counts the concept); (c) wait until a third domain (IDP / DXP / DI) confirms the same connector-library shape and only then promote to cross-cutting. Per Rule #18 cross-cutting convention the >=3 domain threshold gates promotion; LCAP + IPAAS = 2 today, but the third domain is likely DI (Data Integration) which clearly shares the connector library. Depends on B2-S1.

7. **B2-S7 - C1 business function spine name.** `business_function_domains` row carries `business_function_name='IT Infrastructure'` (id 58) as owner, but the canonical 20-function spine in SKILL.md § Function spine specifies `IT Operations` (id 27). Two-pass decision: (a) Is "IT Infrastructure" a legitimate sub-function under IT Operations (the spine allows extending via `parent_business_function_id`), and the row should stay against id 58 with a parent link added? (b) Or was id 58 authored as a parallel-tier alternative to id 27, in which case the IPAAS owner row should re-link to id 27 and id 58 needs separate triage? Catalog-wide implication: if (a), confirm id 58 has the right `parent_business_function_id`; if (b), the catalog has parallel IT-function rows and this is a broader spine-hygiene gap. Independent.

8. **B2-S8 - Missing iPaaS solutions (A3 enrichment).** A3 passes count-wise (4 solutions) but the iPaaS market surface is materially broader. Proposed additions: Zapier (citizen-tier iPaaS leader, plausibly belongs in a separate CITIZEN-AUTO market per the queued candidate; user decides whether to also link to IPAAS or only to the new market), Make.com (same cohort), Tray.io (mid-market recipe iPaaS), Celigo (NetSuite-anchored), SnapLogic (data + app hybrid), IBM App Connect (enterprise suite-aligned), Microsoft Power Automate (Microsoft 365-anchored citizen / mid-market hybrid), Jitterbit (legacy on-prem). User picks which solutions to add and which `coverage_level` (primary vs secondary) per row. Vendor rows may need creating where the vendor is not yet in `vendors`. Independent.

### Bucket 3 - Phase 0 pending (speculative; vendor-research vetting needed)

Universal-or-near-universal iPaaS vendor entities surfaced by the audit pass. Phase 0 vetting (formal vendor-research protocol) would confirm or filter:

| Candidate | Proposed module | Vendor evidence |
|---|---|---|
| `integration_connections` (separate from `integration_connectors`) | IPAAS-RECIPE-DESIGN | Workato "Connection" (vs "Connector" the template), Boomi "Account" (vs "Connector"), MuleSoft "Connector configuration", Tray.io "Authentication". Distinct from connectors: a connector is a reusable template; a connection is the configured-with-credentials instance bound to a recipe |
| `integration_run_logs` / `integration_execution_steps` | IPAAS-RUNTIME | Universal: Workato per-step job log, Boomi process log, MuleSoft flow log, Zapier task history step. Sub-resource of `integration_runs`, but vendors model it as a first-class queryable entity given the audit and debugging use case |
| `integration_environments` | IPAAS-RECIPE-DESIGN OR cross-cutting | Workato workspace + environment, Boomi atom + molecule + environment, MuleSoft environment (dev / qa / prod), SnapLogic project space. Promotion across dev / test / prod is a core iPaaS workflow. May overlap with cross-cutting `<DOMAIN>-ENV-MGMT` concept that APP-PAAS / LCAP also have |
| `integration_error_records` / `dead_letter_queue_entries` | IPAAS-RUNTIME | Universal: Workato error monitor, Boomi process errors, MuleSoft DLQ, Workato lookup-table-based retry. Distinct from a failed run: a single run may produce multiple error records, and errors persist after the run row is archived |
| `integration_schedule_policies` | IPAAS-RECIPE-DESIGN OR config-shape on triggers | Workato scheduled triggers, Boomi process schedule, Zapier scheduled Zaps. May be config-shape on `integration_triggers` (depending on B2-S4 decision) or a separate master if the scheduling surface gets rich (cron, throttling, queue-priority) |

### Cross-bucket dependencies

- **Bucket 2 B2-S6 depends on B2-S1** (the cross-cutting capability rename decision lands when modules + capabilities are authored).
- **Bucket 2 B2-S5 depends on B2-S1** (consumer DMDO requires module FK).
- **Bucket 2 B2-S3 (connector personal-content flip) depends on Bucket 3 B3-S1** (`integration_connections` candidate): if Phase 0 confirms the connection / connector split, the `has_personal_content` flag belongs on `integration_connections`, not on `integration_connectors` itself.
- **Bucket 2 B2-S4 (integration_triggers lifecycle shape) depends on Bucket 3 B3-S5** (`integration_schedule_policies` candidate): if Phase 0 confirms a separate scheduling master, triggers lose half their proposed lifecycle to the new entity and the config-shape case strengthens.
- **Bucket 1 B1-S7, B1-S8, B1-S9, B1-S10, B1-S11 all depend on B1-S1.** Standard ordering: M1 (modules + capabilities) first, then DMDO master attribution, then everything else.
- **Bucket 1 B1-S2 (catalog UX) depends on B2-S2** wording approval.
- **Bucket 1 B1-S3 (pattern flags) depends on B2-S3** flag approvals.
- **Bucket 1 B1-S13 (event_category enum hygiene)** is independent of every other item and can ship first as a single PATCH pass.
- Buckets 1, 2, 3 are otherwise independent of each other and the user can resolve in any order.

### Per-bucket prompts

- **After Bucket 1 surface:** "Fix these now? Reply 'all' (load all 14 in-scope items in sequence: B1-S13 first since it has no dependency, then B1-S1 + B1-S12, then B1-S4 / B1-S5 / B1-S6, then B1-S7 / B1-S8 / B1-S9 / B1-S10 / B1-S11, then B1-H1), 'just <IDs>', or 'skip'. Note: B1-S2 (catalog UX fields) and B1-S3 / B1-S7 / B1-S8 require Bucket 2 sign-offs before the loader runs, so 'all' triggers the per-bucket review loop in order."
- **After Bucket 2 surface:** "What's your call on each of B2-S1 through B2-S8? I'll wait for your decision per item before acting. For B2-S2 (catalog UX field wording), please supply your exact text (Rule #20). For B2-S3 (pattern flag flips + credential-storage shape), confirm or rewrite each proposed flip and pick the connection / connector split or not. For B2-S4 (triggers + webhooks design), pick option (a) or (b) on each sub-decision. For B2-S7 (IT Infrastructure vs IT Operations), pick rename, re-link, or extend. For B2-S8, list which of Zapier / Make.com / Tray.io / Celigo / SnapLogic / IBM App Connect / Power Automate / Jitterbit should be added with which `coverage_level`."
- **After Bucket 3 surface:** "Vet via Phase 0 research (formal vendor-surface enumeration against MuleSoft / Boomi / Workato / Tray.io / Celigo / SnapLogic / Zapier / Make.com), or eyeball-mode? If eyeball, name which of `integration_connections`, `integration_run_logs`, `integration_environments`, `integration_error_records`, `integration_schedule_policies` ring true; named candidates become Bucket 1 items immediately."

### Report-only follow-ups (owed by other domains)

These items the IPAAS audit identified but the fix lives on another domain. They are observations the user can schedule audits for; they do not block IPAAS's pass.

- **OBS B10b owes:** handoff 766 (`integration_run.failed` -> OBS) has NULL `source_domain_module_id` on the IPAAS side (in-scope) AND NULL `target_domain_module_id` on the OBS side because OBS has zero modules per cross-check. OBS audit cures the target side.
- **APIM B10b owes:** handoff 767 (IPAAS -> APIM) and inbound 748 (APIM -> IPAAS) both have NULL module FKs on the APIM side because APIM has zero modules. APIM audit cures.
- **TEST-MGMT B10b owes:** handoff 768 (IPAAS -> TEST-MGMT) has NULL `target_domain_module_id` because TEST-MGMT has zero modules. TEST-MGMT audit cures.
- **VSDP B10b owes:** handoff 801 (IPAAS -> VSDP) has NULL `target_domain_module_id` because VSDP has zero modules per cross-check. VSDP audit cures.
- **LCAP B10b owes:** inbound handoff 705 (`extend_workflow.published` -> IPAAS) has NULL `source_domain_module_id` on the LCAP side because LCAP has zero modules per the LCAP audit 2026-05-30 (M1 hard-fail there as well). Re-surfaces when LCAP is re-audited post-M1 fix.
- **LCAP B8 owes:** the structural mirror for handoff 705. LCAP needs an outbound `data_object_relationships` row `extend_workflows feeds_into <ipaas_master>` once both sides have masters and modules wired.
- **APIM B8 owes:** structural mirror for handoff 748 (`api_specifications drives_refresh_of integration_connectors` or similar). APIM audit cures.
- **OBS B8 owes:** structural mirror for handoff 766 (`integration_runs reports_to <obs_master>`). OBS audit cures (needs its own master to exist first).
- **VSDP B8 owes:** structural mirror for handoff 801. VSDP audit cures.
- **TEST-MGMT B10b cross-check passes:** the existing relationship 572 `is_triggered_by` 474 is correctly authored owner_side=target on TEST-MGMT's outbound B8; this audit does not require TEST-MGMT to do anything further on the relationship surface for handoff 768. The handoff's NULL `target_domain_module_id` is the remaining gap, owed by TEST-MGMT's M1 fix.
- **Pre-existing U+2014 em-dash in `domains.business_logic`:** the U+2014 em-dash between "delivery at scale" and "code-dominant by definition" predates this audit (loaded in an earlier wave) and is not fixed here. Route to the catalog-wide em-dash cleanup sweep.

### Candidates queued to `audits/_missing-domains.md`

The audit surfaced 2 candidate domains that may warrant their own `domains` row pending point-solution-market triage. Both queued via the standard helper:

- **EVENT-BROKER-PAAS** (Event Broker Platform as a Service). Vendor evidence: Solace PubSub+, Confluent Cloud, AWS EventBridge, Ably, Pusher, Kafka-as-a-Service offerings. Adjacency: IPAAS, OBS, APIM, APP-PAAS. Candidate capabilities: managed event broker, topic and stream administration, pub-sub fan-out, event schema registry, replay and dead-letter handling. iPaaS handles point-to-point and recipe-driven integration; event-broker-PaaS handles persistent multi-subscriber event streams with replay semantics. The two markets overlap (some iPaaS vendors bolt on broker capabilities, some broker vendors layer iPaaS-like connectors) but the buyer evaluation criteria (broker throughput, message ordering, retention) diverge enough to plausibly warrant a separate market.
- **CITIZEN-AUTO** (Citizen Workflow Automation). Vendor evidence: Zapier, Make.com, IFTTT, Pipedream, n8n cloud, Pabbly Connect. Adjacency: IPAAS, RPA, LCAP. Candidate capabilities: no-code multi-step workflow, app trigger and action library, schedule and webhook triggers, light data transformation, end-user authored automations. Distinct from enterprise iPaaS (Boomi / MuleSoft / Workato) by buyer (end-user / business analyst vs IT integration team), pricing model (per-task / per-Zap vs per-connection / per-recipe), and connector library scope (longer-tail SaaS apps for citizen vs deeper enterprise connectors). The two markets share the recipe / flow shape but the personas and economics differ enough that several flagship vendors (Zapier especially) would feel mis-placed in an enterprise-iPaaS catalog row.

### Decisions

_(empty until the user reviews)_

### Fixes applied

_(empty until the user approves Bucket 1 items)_

### `domains.notes` pointer (if updated)

_not yet written; will require user-approved wording per Rule #15_

## 2026-05-31, Continuation: B1 technical fixes (residual)

Residual B1 pass applied via loader [.tmp_deploy/fix_ipaas_b1_technical_2026_05_31.ts](../.tmp_deploy/fix_ipaas_b1_technical_2026_05_31.ts). Scope: truly-technical residuals only (PATCH enum backfills, Rule #10 user-edges where audit pre-specified, handoff_processes inserts where audit pre-specified handoff_id + resolvable PCF). Loader is idempotent.

### Fixes applied

| Audit ID | Type | Result |
|---|---|---|
| B1-S13 | PATCH `trigger_events.event_category` (8 rows: 836-843) | patched=8, skipped=0. Categories: 836/837/843 state_change; 838/839 lifecycle; 840 threshold; 841/842 signal. |
| B1-S5 | INSERT `data_object_relationships` Rule #10 user-edges (5 rows) | inserted=5, skipped=0. `users` -> `integration_recipes` (authors), `integration_connectors` (owns), `webhook_subscriptions` (subscribes), `integration_runs` (initiates), `integration_data_mappings` (designs). Each: `data_object_id=748` (source), `owner_side=source`, `relationship_type=one_to_many`, `relationship_kind=reference`, `is_required=false`, `record_status` defaulted to `new`. The "publishes" edge proposed in B1-S5 is gated on B2-S3 (single-approver flag) and not applied. |
| B1-H1 | INSERT `handoff_processes` (5 rows) | inserted=5, skipped=0. Handoff 768 already tagged with process_id=1670 from a prior pass (skipped). Inserts: 766/765 -> 1299 (Triage IT service delivery incidents); 767 -> 273 (Manage IT user identity and authorization); 769 -> 1947 (Manage service/solution operations); 801 -> 1262 (Implement software change/release). Each: `role=implements`, `proposal_source=agent_curated`, `record_status` defaulted to `new`. Deferred per audit: 705 (LCAP -> IPAAS) and 748 (APIM -> IPAAS), both flagged for Discover Pass 3 in B1-H1 detail. |

### Deferred (out of scope for this technical residual pass)

- **B1-S1 / B1-S12** modules + capabilities + DMDOs (new entities; gated on B2-S1 module-split decision).
- **B1-S2** catalog UX fields (Rule #20; requires user-approved wording).
- **B1-S3** pattern flag flips (explicit defer; B2-S3 judgment call).
- **B1-S4** intra-domain `data_object_relationships` (not Rule #10 user-edges; audit cluster-drafts pending review).
- **B1-S6** aliases (audit suggests vendor terms but does not pre-specify exact (data_object_id, alias_name) tuples).
- **B1-S7** lifecycle states (depends on B1-S1 module FKs via M5).
- **B1-S8** legacy skill retire + per-module system skills (new entities; depends on B1-S1).
- **B1-S9** handoff `source_domain_module_id` PATCH (NOT derivable; IPAAS has zero modules until B1-S1).
- **B1-S10** outbound cross-domain `data_object_relationships` (depends on B1-S4/B1-S5; 3 of 6 targets unverified).
- **B1-S11** intra-domain handoffs (depends on B1-S1 + B1-S7).
- **B1-H1 deferred handoffs** 705 and 748 (no clean cross-industry PCF match per audit).
- All Bucket 2 / Bucket 3 items (user judgment / Phase 0 vetting).
