# === SUPERSEDED 2026-06-06: per-domain-skill restoration ===
# The per-module `system` skill grain is RETIRED (plans/per-domain-skill-restoration.md).
# Any open item that says "author/split a per-module system skill", "one system skill per
# domain_modules row", "add/PATCH skill_tools", or "<module>_agent per module" is CANCELED.
# New model: tool requirements live on `domain_module_tools` (author tools onto modules); each
# domain has exactly ONE domain-grain `system` skill (domain_id set, domain_module_id null) that
# DERIVES its toolset; starters keep their own module-anchored skill; FULL modules carry no skill;
# cross-domain value streams use `process_tools`. `skill_tools` is dropped. Per-module tool
# re-authoring is tracked in audits/_modularization-backlog.md. Do NOT author per-module skills.
# === end supersession note ===
schema_version: 2
domain: TELCO-BSS
status: feedback_needed
next_action_by: user
last_audit: "2026-06-13"

# UNBUILT domain: 0 domain_modules, 0 capability_domains (confirmed live 2026-06-07).
# Per Validate Rule #21 the build cascade is LEFT (do not scaffold). The 2026-06-07 pass
# executed only the module-independent additive/corrective items (entity_type, catalog UX,
# domain_aliases, data_object_aliases). Everything below is surfaced (b2 / build / sign-off)
# or blocked on the build (b1b) or backlog (b3).

b1a:
  - id: B1A-H2
    summary: "Surface the 7 `handoff_processes` rows for reviewer sign-off (destructive: flip record_status to approved)."
    finding: "All 7 `handoff_processes` rows on TELCO-BSS outbound handoffs (ids 458-464; handoffs 927-933) carry `proposal_source='agent_curated'`, `record_status='new'` (confirmed live 2026-06-07). H1 coverage = 7/7 = 100%. Catalog-quality side-bar (`record_status='approved'` count) = 0 until reviewer signs off. Rule #1 forbids agent-side approve; this is a destructive PATCH that only the user may authorize."
    action: "Surface the 7 `(handoff_id, process_id)` tuples to user; PATCH `record_status='approved'` per row only after explicit per-row user approval. AGENT MUST NOT APPLY."
    affected_handoffs:
      - {handoff_id: 927, process_id: 1299, target_domain: ITSM, trigger_event: service_trouble_ticket.opened}
      - {handoff_id: 928, process_id: 1299, target_domain: ITSM, trigger_event: service_provisioning.failed}
      - {handoff_id: 929, process_id: 302, target_domain: FIN, trigger_event: telco_customer_bill.issued}
      - {handoff_id: 930, process_id: 196, target_domain: CSM, trigger_event: telco_subscription.suspended}
      - {handoff_id: 931, process_id: 740, target_domain: CSM, trigger_event: telco_service_order.completed}
      - {handoff_id: 932, process_id: 1312, target_domain: ITSM, trigger_event: network_inventory.updated}
      - {handoff_id: 933, process_id: 1351, target_domain: FIN, trigger_event: telco_subscription.activated}

b1b:
  - id: B1B-BUILD
    summary: "Unbuilt domain: 0 domain_modules (M1 fail), 0 capability_domains awaiting a build."
    finding: "Live triage re-confirmed: this domain has 0 domain_modules rows (M1 fail) and 0 capability_domains rows, so it is not deployable today. It must be built before any persona / RACI / lifecycle / module-FK work applies. The build itself is a user decision (modularization shape, B2-2)."
    action: "On B2-2 decision, run Phase A then M then B then (per the post-2026-06-06 skill model) ONE domain-grain system skill that derives its toolset from `domain_module_tools`. If multi-module, run Phase P (personas, role_modules, process_raci, lifecycle.process_id) afterward. The 7 masters now carry `entity_type`, so B12 lifecycle authoring is ready to ride the build for the 6 `operational_workflow` masters."
    blocked_by:
      - {type: user_decision, ref: B2-2}
  - id: B1B-B9D-REALIZE
    summary: "Realize 3 outbound handoff payloads as TELCO-BSS-owned work once the build lands (B9d ORPHANs blocked on module-grain masters)."
    finding: "B9d resolver ran 2026-06-13 in both directions on every boundary. Results: 1 RESOLVED (`Invoice customer` pid 302, handoff 929 to FIN); 2 ORPHANs owned by ITSM (`Triage IT service delivery incidents` pid 1299 on handoffs 927/928, `Maintain IT asset records` pid 1312 on handoff 932) routed to ITSM's audit files this pass; 3 payloads (`telco_subscriptions` on handoffs 930 to CSM and 933 to FIN, `telco_service_orders` on handoff 931 to CSM) classified UNOWNED by the resolver ONLY because it reads `domain_module_data_objects` (module-grain), which is empty while this domain is unbuilt (M1 fail). These 3 entities ARE mastered by TELCO-BSS at the legacy `domain_data_objects` grain (ids 654/655), so they are NOT genuine unowned dependencies: once the build lands and module-grain master rows exist, the resolver will see TELCO-BSS as owner and these resolve to TELCO-BSS-owned ORPHANs needing persona/RACI here."
    action: "On the next audit pass AFTER the build lands (B1B-S2 modules + B1B-S6 lifecycle gates), re-run `scripts/analytics/b9d_resolver.ts TELCO-BSS --write`; it will route the 3 payloads to TELCO-BSS as owner and author the persona/RACI realization here at record_status='new'. No agent action possible before modules exist."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S2, reason: "resolver owner detection reads module-grain master rows; none exist until B1B-S2 lands"}
      - {type: user_decision, ref: B2-2}
  - id: B1B-S1
    summary: "Author 5-8 `capability_domains` rows aligned to TM Forum eTOM Level 2."
    finding: "A2 fails: 0 `capability_domains` rows on `domain_id=42`. Required floor is >=3."
    blocked_by:
      - {type: user_decision, ref: B2-2}
    extra_proposed_capabilities:
      - TELCO-ORDER-MGMT
      - TELCO-CATALOG-MGMT
      - TELCO-PROVISIONING
      - TELCO-INVENTORY-MGMT
      - TELCO-BILLING
      - TELCO-ASSURANCE
      - TELCO-SUBSCRIPTION-MGMT

  - id: B1B-S2
    summary: "Author 5-7 full `domain_modules` rows per the chosen modularization shape."
    finding: "M1 HARD FAIL: 0 `domain_modules` rows on `domain_id=42`. Cascades into M2, M4, M5, M6, M8, B9b, B10b source side, E1-E5, F2, F3, B12 module-FK attribution."
    blocked_by:
      - {type: user_decision, ref: B2-2}
    affected_entities:
      - telco_service_catalog
      - telco_service_orders
      - telco_subscriptions
      - service_provisioning_workflows
      - network_inventory_records
      - telco_customer_bills
      - service_trouble_tickets

  - id: B1B-S3
    summary: "SUPERSEDED 2026-06-06: per-module `system` skills + `skill_tools` are RETIRED."
    finding: "Original item asked for one `skill_type='system'` skill per `domain_modules` row with 5-12 `skill_tools`. Under the post-2026-06-06 model this is CANCELED: tool requirements live on `domain_module_tools`, and the domain has exactly ONE domain-grain `system` skill that derives its toolset. Reframed as a note; no per-module skill authoring."
    note: "On build, author module tools onto `domain_module_tools`; the single domain-grain skill derives from them. Do NOT author per-module skills."

  - id: B1B-S4
    summary: "SUPERSEDED 2026-06-06: legacy domain-grain skill 111 is now the CORRECT shape."
    finding: "Original item asked to RETIRE `skills` row id 111 (`telco-bss-system`, `domain_id=42`, `domain_module_id=NULL`) once per-module skills landed. Under the new model the ONE domain-grain `system` skill (domain_id set, domain_module_id null) is exactly the target shape. Row 111 already matches it. The only remaining cosmetic item is naming (kebab `telco-bss-system` vs snake convention); that is a non-blocking rename, NOT a retirement. No deletion."
    note: "Do NOT delete skill 111. Optional cosmetic rename only; surface if marketing cares."

  - id: B1B-S6
    summary: "Author `data_object_lifecycle_states` for the 6 `operational_workflow` masters (B12)."
    finding: "B12 HARD FAIL: zero lifecycle states on the workflow-bearing masters. entity_type is now classified (2026-06-07): 6 are `operational_workflow` (B12 applies), `telco_service_catalog` is `catalog` (B12 not required). Lifecycle states need `domain_module_id` per Rule #14, so this rides the build."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S2, reason: "lifecycle states need `domain_module_id` per Rule #14 permission materialization"}
      - {type: user_decision, ref: B2-5, reason: "pattern flag flips ride the same load"}
    affected_masters:
      - {data_object_id: 654, name: telco_service_orders, entity_type: operational_workflow, workflow: "submitted -> validated -> in_provisioning -> completed | cancelled"}
      - {data_object_id: 655, name: telco_subscriptions, entity_type: operational_workflow, workflow: "pending -> active -> suspended -> cancelled"}
      - {data_object_id: 656, name: service_provisioning_workflows, entity_type: operational_workflow, workflow: "queued -> executing -> completed | failed"}
      - {data_object_id: 657, name: network_inventory_records, entity_type: operational_workflow, workflow: "planned -> deployed -> decommissioned"}
      - {data_object_id: 658, name: telco_customer_bills, entity_type: operational_workflow, workflow: "draft -> issued -> paid | disputed | written_off"}
      - {data_object_id: 659, name: service_trouble_tickets, entity_type: operational_workflow, workflow: "opened -> triaged -> in_repair -> resolved -> closed"}

  - id: B1B-S7
    summary: "Author 7 intra-domain `data_object_relationships` edges between the 7 masters (B6)."
    finding: "B6 fails: zero intra-domain master-to-master edges. Expected: `telco_service_orders fulfills_via service_provisioning_workflows`, `telco_subscriptions provisioned_by service_provisioning_workflows`, `telco_service_orders results_in telco_subscriptions`, `telco_subscriptions billed_by telco_customer_bills`, `telco_subscriptions occupies network_inventory_records`, `service_trouble_tickets references telco_subscriptions`, `telco_service_catalog defines telco_service_orders`."
    blocked_by:
      - {type: user_decision, ref: B2-2, reason: "verb tuples (verb, inverse_verb, type, kind, owner_side, is_required) are not pre-specified in the audit; confirm before bulk load"}

  - id: B1B-S8
    summary: "Author 7 `users` edges to the 7 masters (Rule #10, B7)."
    finding: "B7 fails: zero `users` (id 748) edges on any of the 7 masters. Each carries at least one user-typed actor: order agent, billing specialist, network engineer, care agent, provisioning specialist, catalog manager, network inventory custodian."
    blocked_by:
      - {type: user_decision, ref: B2-2, reason: "exact `(verb, inverse_verb, type, kind, owner_side, is_required)` per master not pre-specified; needs confirmation"}

  - id: B1B-S10
    summary: "Backfill `handoffs.source_domain_module_id` on all 7 outbound rows (B10b)."
    finding: "B10b fails on the source side: 7 of 7 outbound handoffs (927-933) have `source_domain_module_id=NULL`. Target side: 3 ITSM rows correctly carry `target_domain_module_id=38`; 4 (2 CSM, 2 FIN) carry NULL target FK (owed by CSM/FIN audits, not this domain's fix)."
    blocked_by:
      - {type: prerequisite_entity, ref: B1B-S2, reason: "no source module exists to attribute to until B1B-S2 lands"}

  - id: B1B-S11a
    summary: "Resolve 2 trigger_events with no `handoffs` row: 1060 `telco_service_order.submitted`, 1068 `telco_service_catalog.updated`."
    finding: "B9 partial: events 1060 and 1068 have zero handoffs. Either treat as leaf and justify (e.g. `order.submitted` plausibly fires only the internal provisioning workflow) or author cross-domain handoff(s) (CSM agent-training, CRM offer-sync for catalog updates). This is B2-7 (leaf-vs-new is a user judgment)."
    blocked_by:
      - {type: user_decision, ref: B2-7, reason: "decide leaf vs new cross-domain rows; cross-domain rows also need B2-2 modularization to set `source_domain_module_id`"}
    affected_events:
      - {id: 1060, event_name: telco_service_order.submitted, current_category: state_change}
      - {id: 1068, event_name: telco_service_catalog.updated, current_category: state_change}

b2:
  - id: B2-1
    summary: "Em-dash present in `domains.business_logic` for TELCO-BSS (project rule forbids em-dashes). Destructive: overwriting a non-empty value."
    question: "Supply replacement wording for `business_logic`, or approve agent to draft and review per row?"
    options:
      - "User supplies exact replacement string"
      - "Agent drafts; user reviews and approves before PATCH"
    why: "Project rule forbids em-dashes; current value contains U+2014 in `business_logic`. `description` is clean. Overwriting a populated field is destructive and needs sign-off."
    extra_current_value: "Rating engine and online charging system — high-throughput algorithmic kernel; the rest is catalog, order, and customer records."
    proposed_replacement: "Rating engine and online charging system: a high-throughput algorithmic kernel; the rest is catalog, order, and customer records."

  - id: B2-2
    summary: "Modularization shape for TELCO-BSS (gates the entire build: B1A-BUILD, B1B-S1, B1B-S2, B1B-S6, B1B-S7, B1B-S8, B1B-S10, B1B-S11a)."
    question: "Pick one of three candidate module splits, or propose another. Also decide whether to rename the domain (OSS scope) or promote candidate TELCO-NMS separately."
    options:
      - "eTOM-aligned 7-module: TELCO-BSS-ORDER-MGMT, TELCO-BSS-CATALOG, TELCO-BSS-PROVISIONING, TELCO-BSS-INVENTORY, TELCO-BSS-BILLING, TELCO-BSS-ASSURANCE, TELCO-BSS-SUBSCRIPTIONS"
      - "5-module commercial-vs-operational split"
      - "3-module starter set: TELCO-BSS-ORDER-TO-CASH, TELCO-BSS-PROVISIONING-AND-INVENTORY, TELCO-BSS-ASSURANCE"
    why: "M1 hard fail blocks every downstream concern. Single decision unlocks the largest set of b1b items."

  - id: B2-3
    summary: "Add US-side telco regulations (FCC CPNI, FCC LNP, STIR/SHAKEN, CALEA, TCPA, SOX)."
    question: "Which to add as mandatory vs advisory? Which need new `regulations` rows in the catalog?"
    options:
      - "Add all 6 as mandatory `domain_regulations` rows"
      - "Add CPNI + STIR/SHAKEN + CALEA mandatory, LNP + TCPA + SOX advisory"
      - "Skip; current EU-leaning set is sufficient"
    why: "Pure-play US telco vendors universally surface these; current catalog leans EU. New `regulations` rows are a Phase-A market-shape addition the agent will not create unprompted (scope discipline)."

  - id: B2-4
    summary: "Role personas (B1A-PHASE-P): DEFERRED. Persona/RACI authoring is not done by the agent; this records candidates only."
    question: "Confirm subset of 6 candidate personas to author when the build lands (multi-module only)."
    options:
      - "OPERATIONS-ORDER-MANAGER"
      - "OPERATIONS-NETWORK-ENGINEER"
      - "CUSTOMER-SERVICE-CARE-AGENT"
      - "FINANCE-BILLING-SPECIALIST"
      - "MARKETING-CATALOG-MANAGER"
      - "OPERATIONS-PROVISIONING-SPECIALIST"
    why: "E1 vacuous until M1 lands; persona set rides the build. DEFERRED per Validate persona rule."

  - id: B2-5
    summary: "Pattern-flag re-evaluation on the workflow masters (Rule #12 B4). Destructive: flips a default-false flag on a populated row."
    question: "Which of 3 candidate flags to flip during the B1B-S6 build load?"
    options:
      - "telco_customer_bills.has_submit_lock=true (issued bill immutable)"
      - "service_trouble_tickets.has_personal_content=true (customer narratives)"
      - "telco_service_orders.has_submit_lock=true (submitted order locks before provisioning)"
    why: "B4 audit must positively re-evaluate; default-false is not the same as false-after-review. Flag flips are surfaced (judgment), not auto-applied."

  - id: B2-7
    summary: "Decide whether events 1060 / 1068 are leaf or need new cross-domain handoffs (= B1B-S11a)."
    question: "For trigger_event 1060 (`telco_service_order.submitted`) and 1068 (`telco_service_catalog.updated`), justify as leaf or author new cross-domain handoffs?"
    options:
      - "1060 leaf (fires only intra-domain provisioning); 1068 needs CSM + CRM cross-domain handoffs"
      - "Both leaf with explicit justification"
      - "Both need new cross-domain handoffs"
    why: "B9 partial: 2 of 9 events have no handoff. Leaf classification needs explicit per-event justification; new handoffs also need B2-2 modularization to set source_domain_module_id."

b3:
  - id: B3-1
    candidate: usage_event_records
    rationale: "CDR / EDR / IPDR records produced by mediation and consumed by rating engines. The domain's `business_logic` names rating/charging as the substrate; the entities are absent."
    proposed_module: TELCO-BSS-BILLING

  - id: B3-2
    candidate: convergent_charging_sessions
    rationale: "Online-charging-system session state for real-time rating. Standard across mobile / convergent BSS."
    proposed_module: TELCO-BSS-BILLING

  - id: B3-3
    candidate: mediation_records
    rationale: "Network-to-billing data normalization records."
    proposed_module: TELCO-BSS-BILLING

  - id: B3-4
    candidate: product_offering_bundles
    rationale: "TM Forum SID Product Offering split from Product Specification. Current `telco_service_catalog` collapses these."
    proposed_module: TELCO-BSS-CATALOG

  - id: B3-5
    candidate: tariff_plans
    rationale: "Distinct from catalog: pricing tier definitions referenced by subscriptions."
    proposed_module: TELCO-BSS-CATALOG

  - id: B3-6
    candidate: customer_quotes
    rationale: "Pre-order pricing artifact; bridges CPQ-like surface and order entry."
    proposed_module: TELCO-BSS-ORDER-MGMT

  - id: B3-7
    candidate: msisdn_inventories
    rationale: "Number inventory for any mobile or convergent operator."
    proposed_module: TELCO-BSS-INVENTORY

  - id: B3-8
    candidate: sim_inventories
    rationale: "SIM + eSIM profile inventory for mobile operators."
    proposed_module: TELCO-BSS-INVENTORY

  - id: B3-9
    candidate: port_in_requests
    rationale: "Local Number Portability workflow."
    proposed_module: TELCO-BSS-ORDER-MGMT

  - id: B3-10
    candidate: equipment_swap_requests
    rationale: "Device upgrade workflow standard at mobile operators."
    proposed_module: TELCO-BSS-ORDER-MGMT

  - id: B3-11
    candidate: dunning_cases
    rationale: "Collections and payment-arrangement workflow."
    proposed_module: TELCO-BSS-BILLING

  - id: B3-12
    candidate: qos_kpi_records
    rationale: "QoS / SLA-credit telemetry for assurance."
    proposed_module: TELCO-BSS-ASSURANCE

  - id: B3-13
    candidate: revenue_assurance_alerts
    rationale: "Revenue assurance + fraud cases. May route to a queued TELCO-RAFM domain if user promotes it."
    proposed_module: TELCO-BSS-ASSURANCE
